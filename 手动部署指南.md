# 易享生活后端服务手动部署指南

## 前提条件
- 已完成代码编译，生成JAR文件：`backend/target/easy-joy-life-system-1.0.0.jar`
- 服务器SSH访问权限
- 服务器已安装Java 21

## 部署步骤

### 1. 连接服务器
```bash
ssh root@xx.aieo.cn
```

### 2. 创建部署目录
```bash
mkdir -p /opt/easyjoylife/backup
mkdir -p /opt/easyjoylife/logs
cd /opt/easyjoylife
```

### 3. 停止现有服务
```bash
# 查找Java进程
ps aux | grep easy-joy-life-system

# 停止进程（替换PID为实际进程ID）
kill -15 <PID>

# 如果进程仍在运行，强制停止
kill -9 <PID>
```

### 4. 备份现有JAR文件（如果存在）
```bash
if [ -f easy-joy-life-system-1.0.0.jar ]; then
    cp easy-joy-life-system-1.0.0.jar backup/easy-joy-life-system-1.0.0.jar.backup.$(date +%Y%m%d_%H%M%S)
fi
```

### 5. 上传新的JAR文件
在本地执行：
```bash
# Windows (使用Git Bash或WSL)
scp backend/target/easy-joy-life-system-1.0.0.jar root@xx.aieo.cn:/opt/easyjoylife/

# Linux/Mac
scp backend/target/easy-joy-life-system-1.0.0.jar root@xx.aieo.cn:/opt/easyjoylife/
```

### 6. 启动新服务
在服务器上执行：
```bash
cd /opt/easyjoylife

# 设置Java环境（如果需要）
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk
export PATH=$JAVA_HOME/bin:$PATH

# 启动服务
nohup java -jar \
    -Xms512m -Xmx1024m \
    -Dspring.profiles.active=prod \
    -Dserver.port=8081 \
    easy-joy-life-system-1.0.0.jar \
    > logs/app.log 2>&1 &

echo "服务启动完成，进程ID: $!"
```

### 7. 验证部署
```bash
# 检查进程是否运行
ps aux | grep easy-joy-life-system

# 检查端口是否监听
netstat -tlnp | grep :8081

# 查看启动日志
tail -f logs/app.log

# 测试健康检查接口
curl http://localhost:8081/actuator/health
```

## 常用运维命令

### 查看服务状态
```bash
ps aux | grep easy-joy-life-system
```

### 查看实时日志
```bash
tail -f /opt/easyjoylife/logs/app.log
```

### 重启服务
```bash
# 停止服务
pkill -f easy-joy-life-system

# 启动服务
cd /opt/easyjoylife
nohup java -jar \
    -Dspring.profiles.active=prod \
    -Dserver.port=8081 \
    easy-joy-life-system-1.0.0.jar \
    > logs/app.log 2>&1 &
```

### 查看系统资源使用
```bash
# 查看内存使用
free -h

# 查看磁盘使用
df -h

# 查看CPU使用
top
```

## 测试修复效果

### 1. 在本地测试
```bash
node test-wechat-jsapi-fix.js
```

### 2. 手动测试API
```bash
# 创建充值订单
curl -X POST http://xx.aieo.cn:8081/api/payment/recharge/create \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "amount": 1.00,
    "paymentMethod": "WECHAT"
  }'

# 创建微信支付（需要先获取订单号）
curl -X POST http://xx.aieo.cn:8081/api/payment/wechat/pay \
  -H "Content-Type: application/json" \
  -d '{
    "orderNo": "订单号",
    "openid": "test_openid"
  }'
```

## 故障排查

### 服务无法启动
1. 检查Java版本：`java -version`
2. 检查端口占用：`netstat -tlnp | grep 8081`
3. 查看启动日志：`tail -n 50 /opt/easyjoylife/logs/app.log`
4. 检查磁盘空间：`df -h`
5. 检查内存使用：`free -h`

### 微信支付仍然报错
1. 检查微信支付配置是否正确
2. 确认证书文件是否存在：`ls -la /opt/easyjoylife/cert/`
3. 查看微信支付相关日志
4. 验证SDK版本是否更新到0.2.17

### 网络连接问题
1. 检查防火墙设置：`iptables -L`
2. 检查端口是否对外开放
3. 测试内网连接：`curl http://localhost:8081/actuator/health`
4. 测试外网连接：`curl http://xx.aieo.cn:8081/actuator/health`

## 回滚方案

如果新版本有问题，可以快速回滚：

```bash
cd /opt/easyjoylife

# 停止当前服务
pkill -f easy-joy-life-system

# 恢复备份文件
cp backup/easy-joy-life-system-1.0.0.jar.backup.* easy-joy-life-system-1.0.0.jar

# 启动备份版本
nohup java -jar \
    -Dspring.profiles.active=prod \
    -Dserver.port=8081 \
    easy-joy-life-system-1.0.0.jar \
    > logs/app.log 2>&1 &
```

## 修复内容总结

本次部署包含以下修复：
- ✅ 修复微信JSAPI支付total_fee参数错误
- ✅ 更新微信支付SDK到0.2.17版本
- ✅ 增强参数验证和错误处理
- ✅ 添加详细的日志输出
- ✅ 完善微信支付服务初始化逻辑

部署完成后，微信支付功能应该能够正常工作，不再出现"缺少参数：total_fee"的错误。