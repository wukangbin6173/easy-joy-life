# 微信支付集成指南

## 🎯 功能概述

已成功集成微信小程序支付功能，包括：

- ✅ 微信支付SDK集成
- ✅ 小程序JSAPI支付
- ✅ 微信登录获取openid
- ✅ 支付回调处理
- ✅ 用户管理系统
- ✅ 完整支付流程

## 📋 已实现功能

### 后端功能
1. **微信支付服务** - `WechatPayService` 小程序支付处理
2. **用户认证系统** - `AuthController` 微信登录
3. **用户管理** - `User` 实体类和Repository
4. **支付回调处理** - 异步通知验证和处理
5. **订单查询** - 支付状态查询接口

### 前端功能
1. **微信登录** - 获取用户openid
2. **小程序支付** - 调用wx.requestPayment
3. **支付状态检查** - 自动验证支付结果
4. **用户体验优化** - 完整的支付流程

## 🔧 配置说明

### 1. 微信支付商户配置

需要在微信商户平台配置以下信息：

```yaml
# application.yml
wechat:
  pay:
    app-id: wx01184ae7c701390e  # 小程序AppID
    mch-id: 1234567890  # 商户号
    private-key-path: classpath:cert/apiclient_key.pem  # 商户私钥
    merchant-serial-number: 1234567890ABCDEF  # 商户证书序列号
    api-v3-key: your-api-v3-key-32-characters  # API v3密钥
    notify-url: https://xx.aieo.cn/api/payment/wechat/notify
  miniprogram:
    app-secret: your-miniprogram-app-secret  # 小程序AppSecret
```

### 2. 获取微信支付配置

1. **申请微信支付**
   - 登录微信商户平台：https://pay.weixin.qq.com/
   - 申请小程序支付功能
   - 获取商户号(mch_id)

2. **生成API证书**
   ```bash
   # 下载商户API证书
   # 包含：apiclient_cert.pem, apiclient_key.pem
   ```

3. **配置API v3密钥**
   - 在商户平台设置32位API v3密钥
   - 用于回调数据解密

### 3. 小程序配置

1. **获取AppID和AppSecret**
   - 登录微信小程序后台：https://mp.weixin.qq.com/
   - 开发 → 开发管理 → 开发设置
   - 获取AppID和AppSecret

2. **配置服务器域名**
   ```
   request合法域名：
   https://xx.aieo.cn
   https://api.mch.weixin.qq.com
   ```

## 🚀 API接口说明

### 微信登录
```http
POST /api/auth/wechat/login
Content-Type: application/json

{
  "code": "wx_login_code"
}
```

### 微信支付
```http
POST /api/payment/wechat/pay
Content-Type: application/json

{
  "orderNo": "PAY20260123001",
  "openid": "user_openid"
}
```

### 支付回调
```http
POST /api/payment/wechat/notify
Content-Type: application/json
Wechatpay-Signature: signature
Wechatpay-Timestamp: timestamp
Wechatpay-Nonce: nonce
Wechatpay-Serial: serial

{回调数据}
```

## 💡 支付流程

### 小程序支付流程
1. 用户选择微信支付
2. 小程序调用wx.login获取code
3. 后端通过code获取用户openid
4. 创建支付订单并调用微信统一下单
5. 返回支付参数给小程序
6. 小程序调用wx.requestPayment发起支付
7. 用户完成支付
8. 微信异步回调通知后端
9. 后端更新订单状态和用户余额

### 关键代码示例

#### 小程序端支付调用
```javascript
// 发起微信支付
wx.requestPayment({
  timeStamp: payParams.timeStamp,
  nonceStr: payParams.nonceStr,
  package: payParams.package,
  signType: payParams.signType,
  paySign: payParams.paySign,
  success: () => {
    // 支付成功
  },
  fail: (error) => {
    // 支付失败
  }
});
```

#### 后端统一下单
```java
// 创建支付订单
PrepayRequest request = new PrepayRequest();
request.setAppid(appId);
request.setMchid(mchId);
request.setDescription(order.getSubject());
request.setOutTradeNo(order.getOrderNo());
request.setNotifyUrl(notifyUrl);

// 设置金额（单位：分）
Amount amount = new Amount();
amount.setTotal(order.getAmount().multiply(new BigDecimal("100")).intValue());
request.setAmount(amount);

// 设置支付者
Payer payer = new Payer();
payer.setOpenid(openid);
request.setPayer(payer);
```

## 🔒 安全措施

### 1. 签名验证
- 所有微信支付回调都进行签名验证
- 使用微信支付平台证书验证

### 2. 数据加密
- 回调数据使用API v3密钥解密
- 敏感信息加密传输

### 3. 防重放攻击
- 验证时间戳防止重放
- 订单状态检查防止重复处理

## 📊 数据库表结构

### users (用户表)
- `openid` - 微信用户唯一标识
- `nickname` - 用户昵称
- `avatar` - 头像URL
- `phone` - 手机号

### payment_orders (支付订单表)
- 支持微信支付类型
- 记录微信交易号
- 完整的订单状态管理

## 🛠️ 部署注意事项

### 1. 证书文件部署
```bash
# 将证书文件放到resources/cert目录
src/main/resources/cert/
├── apiclient_key.pem      # 商户私钥
├── apiclient_cert.pem     # 商户证书
└── wechatpay_certificate.pem  # 微信支付平台证书
```

### 2. 生产环境配置
- 使用真实的商户号和证书
- 配置正确的回调域名
- 确保HTTPS访问

### 3. 小程序发布
- 在小程序后台配置支付功能
- 添加服务器域名白名单
- 提交审核时说明支付用途

## 🔍 测试验证

### 1. 开发环境测试
- 使用微信开发者工具测试
- 模拟支付流程验证
- 检查回调处理逻辑

### 2. 真机测试
- 在真实设备上测试支付
- 验证不同网络环境
- 测试异常情况处理

## 📞 技术支持

### 开发文档
- [微信支付开发文档](https://pay.weixin.qq.com/wiki/doc/apiv3/index.shtml)
- [小程序支付接入](https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/payment.html)
- [微信支付SDK](https://github.com/wechatpay-apiv3/wechatpay-java)

### 常见问题
1. **签名验证失败** - 检查证书配置和时间同步
2. **openid获取失败** - 检查AppSecret配置
3. **支付参数错误** - 检查商户号和API密钥

---

## ✅ 部署完成清单

- [x] 微信支付SDK集成完成
- [x] 小程序支付接口开发完成
- [x] 用户登录系统完成
- [x] 支付回调处理完成
- [x] 数据库表结构完成
- [x] 前端支付页面完成
- [ ] 微信支付商户配置（需要申请）
- [ ] 证书文件部署
- [ ] 生产环境测试

**当前状态**: 开发完成，等待微信支付商户配置和证书部署 🎉

## 🆚 支付方式对比

| 功能 | 支付宝支付 | 微信支付 |
|------|------------|----------|
| 集成方式 | H5网页支付 | 小程序原生支付 |
| 用户体验 | 需要跳转 | 无缝集成 |
| 开发复杂度 | 中等 | 较高 |
| 证书管理 | RSA密钥对 | API证书+v3密钥 |
| 回调验证 | RSA签名 | 平台证书验证 |
| 适用场景 | 通用H5支付 | 小程序专用 |

两种支付方式都已完整实现，用户可以根据需要选择使用！