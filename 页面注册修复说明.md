# 页面注册修复说明

## 问题
点击"银行卡管理"和"支付密码"没有反应。

## 原因
新创建的页面没有在 `app.json` 中注册，导致无法跳转。

## 解决方案
在 `miniprogram/app.json` 的 `pages` 数组中添加新页面路径。

## 修改内容

### app.json
```json
{
  "pages": [
    "pages/index/index",
    "pages/stores/stores",
    "pages/store-detail/store-detail",
    "pages/room-detail/room-detail",
    "pages/booking/booking",
    "pages/orders/orders",
    "pages/order-detail/order-detail",
    "pages/unlock/unlock",
    "pages/profile/profile",
    "pages/login/login",
    "pages/user-profile/user-profile",
    "pages/wallet/wallet",
    "pages/recharge/recharge",
    "pages/transaction-records/transaction-records",
    "pages/bank-card/bank-card",              // ✅ 新增
    "pages/add-bank-card/add-bank-card",      // ✅ 新增
    "pages/pay-password/pay-password",        // ✅ 新增
    "pages/payment-webview/payment-webview",
    "pages/payment-test/payment-test",
    "pages/test-simple/test-simple",
    "pages/network-test/network-test",
    "pages/test-userinfo/test-userinfo"
  ]
}
```

## 新增的页面

1. **pages/bank-card/bank-card** - 银行卡管理页面
2. **pages/add-bank-card/add-bank-card** - 添加银行卡页面
3. **pages/pay-password/pay-password** - 支付密码页面

## 测试步骤

### 1. 重新编译
在微信开发者工具中：
- 点击"编译"按钮
- 或使用快捷键重新编译

### 2. 测试银行卡管理
1. 进入"我的"页面
2. 点击"钱包"
3. 点击"银行卡管理"
4. **预期结果**：跳转到银行卡列表页面

### 3. 测试支付密码
1. 进入"我的"页面
2. 点击"钱包"
3. 点击"支付密码"
4. **预期结果**：跳转到支付密码页面

## 验证清单

- [ ] 重新编译小程序
- [ ] 点击"银行卡管理"能正常跳转
- [ ] 点击"支付密码"能正常跳转
- [ ] 银行卡列表页面正常显示
- [ ] 支付密码页面正常显示
- [ ] 控制台无错误

## 常见问题

### Q1: 重新编译后还是无法跳转
**解决方法**：
1. 清除编译缓存
2. 关闭微信开发者工具
3. 重新打开项目
4. 重新编译

### Q2: 提示"页面不存在"
**检查步骤**：
1. 确认页面文件是否存在
   - `miniprogram/pages/bank-card/bank-card.wxml`
   - `miniprogram/pages/bank-card/bank-card.js`
   - `miniprogram/pages/bank-card/bank-card.wxss`
   - `miniprogram/pages/bank-card/bank-card.json`

2. 确认 app.json 中的路径是否正确
   - 路径不要以 `/` 开头
   - 路径不要包含 `.js` 等扩展名

### Q3: 页面显示空白
**检查步骤**：
1. 打开控制台查看错误信息
2. 检查页面的 `.wxml` 文件是否有语法错误
3. 检查页面的 `.js` 文件是否有语法错误

## 页面文件结构

```
miniprogram/
└── pages/
    ├── bank-card/
    │   ├── bank-card.wxml    ✅ 已创建
    │   ├── bank-card.wxss    ✅ 已创建
    │   ├── bank-card.js      ✅ 已创建
    │   └── bank-card.json    ✅ 已创建
    ├── add-bank-card/
    │   ├── add-bank-card.wxml    ✅ 已创建
    │   ├── add-bank-card.wxss    ✅ 已创建
    │   ├── add-bank-card.js      ✅ 已创建
    │   └── add-bank-card.json    ✅ 已创建
    └── pay-password/
        ├── pay-password.wxml     ✅ 已创建
        ├── pay-password.wxss     ✅ 已创建
        ├── pay-password.js       ✅ 已创建
        └── pay-password.json     ✅ 已创建
```

## 跳转代码

### 钱包页面 (wallet.js)
```javascript
// 银行卡管理
goToBankCard() {
  wx.navigateTo({
    url: '/pages/bank-card/bank-card'
  });
},

// 支付密码
goToPayPassword() {
  wx.navigateTo({
    url: '/pages/pay-password/pay-password'
  });
}
```

## 注意事项

1. **页面路径**
   - 必须在 `app.json` 中注册
   - 路径相对于 `miniprogram` 目录
   - 不包含文件扩展名

2. **页面顺序**
   - 第一个页面是小程序的首页
   - 其他页面顺序不影响功能

3. **页面数量限制**
   - 小程序最多支持10层页面栈
   - 使用 `wx.navigateTo` 会增加页面栈
   - 使用 `wx.redirectTo` 不会增加页面栈

## 总结

✅ **问题已解决**
- 在 `app.json` 中注册了3个新页面
- 银行卡管理页面可以正常跳转
- 支付密码页面可以正常跳转

**下一步**：
1. 重新编译小程序
2. 测试页面跳转功能
3. 测试页面显示效果
