# 易享生活棋牌室小程序 - 部署架构总结

## 🏗️ 最终架构

### 服务部署方式

| 服务 | 部署方式 | 端口 | 管理方式 | 说明 |
|------|---------|------|---------|------|
| **MySQL** | 系统服务 | 3306 | systemd | 独立安装，稳定运行 |
| **Redis** | 系统服务 | 6379 | systemd | 独立安装，高速缓存 |
| **Spring Boot** | 系统服务 | 8080 | systemd | 业务应用，频繁更新 |
| **Nginx** | 系统服务 | 80/443 | systemd | 反向代理，HTTPS |

### 架构图

```
┌─────────────────────────────────────────────────┐
│              微信小程序用户                        │
└────────────────────┬────────────────────────────┘
                     │ HTTPS
                     ▼
┌─────────────────────────────────────────────────┐
│  Nginx (系统服务)                                 │
│  - 端口: 80 → 443 (HTTPS重定向)                  │
│  - SSL证书: Let's Encrypt                        │
│  - 反向代理: /api/* → http://localhost:8080     │
└────────────────────┬────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────┐
│  Spring Boot (系统服务)                           │
│  - 端口: 8080                                    │
│  - 管理: systemd                                 │
│  - 日志: journalctl                              │
│  - 服务名: easy-joy-life-backend                 │
└──────┬──────────────────────┬───────────────────┘
       │                      │
       │ JDBC                 │ Lettuce
       ▼                      ▼
┌──────────────────┐   ┌──────────────────┐
│  MySQL (系统服务) │   │  Redis (系统服务) │
│  - 端口: 3306    │   │  - 端口: 6379    │
│  - 数据持久化     │   │  - 高速缓存       │
└──────────────────┘   └──────────────────┘
```

---

## 📁 目录结构

```
/opt/easy-joy-life/
├── backend/                          # Spring Boot应用
│   ├── src/                         # 源代码
│   ├── target/                      # 编译输出
│   │   └── easy-joy-life-system-1.0.0.jar
│   └── mvnw                         # Maven包装器
│
├── deploy/                          # 部署脚本
│   ├── update-backend-only.sh      # 快速更新脚本 ⭐
│   ├── deploy-backend-systemd.sh   # 完整部署脚本
│   ├── final-status-check.sh       # 状态检查脚本
│   ├── .env.production             # 环境变量
│   └── nginx.conf                  # Nginx配置模板
│
└── miniprogram/                     # 小程序代码（本地开发）

/etc/systemd/system/
└── easy-joy-life-backend.service   # systemd服务配置

/etc/nginx/sites-available/
└── easy-joy-life                   # Nginx站点配置

/var/log/
└── nginx/                          # Nginx日志
```

---

## 🚀 日常更新流程

### 最常用：快速更新Spring Boot

**Windows本地：**
```batch
# 方式1: 自动上传并更新
智能更新后端.bat

# 方式2: 仅更新（代码已在服务器）
快速更新后端.bat
```

**Linux/Mac本地：**
```bash
# 上传修改的文件
scp backend/src/main/java/com/easyjoylife/xxx.java \
    root@xx.aieo.cn:/opt/easy-joy-life/backend/src/main/java/com/easyjoylife/

# 执行更新
ssh root@xx.aieo.cn "cd /opt/easy-joy-life/deploy && ./update-backend-only.sh"
```

**更新内容：**
- ✅ 重新编译Java代码
- ✅ 打包JAR文件
- ✅ 重启Spring Boot服务
- ✅ 健康检查
- ❌ 不重启MySQL
- ❌ 不重启Redis
- ❌ 不重启Nginx

**耗时：** ~30秒

---

## 🔧 服务管理命令

### Spring Boot服务

```bash
# 查看状态
systemctl status easy-joy-life-backend

# 启动服务
systemctl start easy-joy-life-backend

# 停止服务
systemctl stop easy-joy-life-backend

# 重启服务
systemctl restart easy-joy-life-backend

# 查看日志
journalctl -u easy-joy-life-backend -f

# 查看最近100行日志
journalctl -u easy-joy-life-backend -n 100 --no-pager
```

### MySQL服务

```bash
# 查看状态
systemctl status mysql

# 连接数据库
mysql --defaults-file=/etc/mysql/debian.cnf

# 查看数据库
mysql --defaults-file=/etc/mysql/debian.cnf -e "SHOW DATABASES;"
```

### Redis服务

```bash
# 查看状态
systemctl status redis-server

# 连接Redis
redis-cli

# 测试连接
redis-cli ping
```

### Nginx服务

```bash
# 查看状态
systemctl status nginx

# 测试配置
nginx -t

# 重新加载配置（不中断服务）
systemctl reload nginx

# 重启服务
systemctl restart nginx
```

---

## 📊 监控与检查

### 快速检查所有服务

```bash
ssh root@xx.aieo.cn "cd /opt/easy-joy-life/deploy && ./final-status-check.sh"
```

**输出示例：**
```
==========================================
易享生活 - 最终服务状态检查
==========================================

1. MySQL (系统服务):
   ✓ 运行中
   mysql  Ver 8.0.45

2. Redis (系统服务):
   ✓ 运行中
   redis-cli 6.0.16

3. Spring Boot (系统服务):
   ✓ 运行中
   启动时间: Sat 2026-02-07 19:04:02 CST
   内存占用: 356.1 MB

4. Nginx (系统服务):
   ✓ 运行中
   nginx version: nginx/1.18.0

5. API测试:
   ✓ 本地API: http://localhost:8080/api/stores
   ✓ 线上API: https://xx.aieo.cn/api/stores

==========================================
✓ 所有服务运行正常！
==========================================
```

### 查看资源占用

```bash
# 查看所有服务的资源占用
ssh root@xx.aieo.cn "systemctl status easy-joy-life-backend mysql redis-server nginx"

# 查看内存占用
ssh root@xx.aieo.cn "free -h"

# 查看磁盘占用
ssh root@xx.aieo.cn "df -h"
```

---

## 🔐 安全配置

### 环境变量

位置：`/etc/systemd/system/easy-joy-life-backend.service`

```ini
Environment="MYSQL_PASSWORD=EasyJoyLife2024!@#"
Environment="JWT_SECRET=EasyJoyLife-JWT-Secret-Key-2024-..."
Environment="WECHAT_APPID=wx9f4a33e5f2b31a6d"
Environment="WECHAT_SECRET=..."
Environment="WECHAT_MCH_ID=1554487931"
Environment="WECHAT_API_KEY=..."
```

### SSL证书

- **证书路径：** `/etc/letsencrypt/live/xx.aieo.cn/`
- **自动续期：** certbot自动续期
- **有效期检查：** 
  ```bash
  ssh root@xx.aieo.cn "certbot certificates"
  ```

---

## 📝 配置文件位置

| 配置项 | 文件路径 |
|-------|---------|
| **Spring Boot配置** | `/opt/easy-joy-life/backend/src/main/resources/application-prod.yml` |
| **systemd服务** | `/etc/systemd/system/easy-joy-life-backend.service` |
| **Nginx配置** | `/etc/nginx/sites-available/easy-joy-life` |
| **MySQL配置** | `/etc/mysql/my.cnf` |
| **Redis配置** | `/etc/redis/redis.conf` |
| **环境变量** | `/opt/easy-joy-life/deploy/.env.production` |

---

## 🎯 优势总结

### 1. 独立服务架构
- ✅ MySQL和Redis独立运行，稳定可靠
- ✅ 各服务互不影响
- ✅ 易于维护和升级

### 2. 快速更新
- ✅ 仅更新变化的服务
- ✅ 30秒完成更新
- ✅ 最小化服务中断

### 3. 统一管理
- ✅ 所有服务使用systemd管理
- ✅ 统一的日志系统（journalctl）
- ✅ 开机自启动

### 4. 高可用性
- ✅ 服务自动重启（Restart=always）
- ✅ 健康检查机制
- ✅ 日志监控

---

## 📚 相关文档

- `更新部署说明.md` - 详细的更新流程
- `快速更新指南.md` - 快速更新步骤
- `服务状态检查指南.md` - 服务检查方法
- `deploy/` - 所有部署脚本

---

## 🔄 版本历史

| 日期 | 版本 | 说明 |
|------|------|------|
| 2024-02-07 | v1.0 | 初始部署，独立服务架构 |
| 2024-02-07 | v1.1 | 修复微信支付证书读取问题 |
| 2024-02-07 | v1.2 | 添加快速更新脚本 |

---

## 📞 技术支持

### 常用命令速查

```bash
# 快速更新
ssh root@xx.aieo.cn "cd /opt/easy-joy-life/deploy && ./update-backend-only.sh"

# 查看日志
ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -f"

# 检查状态
ssh root@xx.aieo.cn "cd /opt/easy-joy-life/deploy && ./final-status-check.sh"

# 测试API
curl https://xx.aieo.cn/api/stores
```

### 故障排查

1. **服务无法启动**
   ```bash
   journalctl -u easy-joy-life-backend -n 100 --no-pager
   ```

2. **API无法访问**
   ```bash
   systemctl status nginx
   systemctl status easy-joy-life-backend
   ```

3. **数据库连接失败**
   ```bash
   systemctl status mysql
   mysql --defaults-file=/etc/mysql/debian.cnf -e "SHOW DATABASES;"
   ```

---

**最后更新：** 2024-02-07  
**架构设计：** 独立服务 + systemd管理  
**部署环境：** Ubuntu 22.04 LTS
