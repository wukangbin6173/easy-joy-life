# 微信支付证书配置指南

## 🔐 证书获取步骤

### 1. 登录微信商户平台
- 访问：https://pay.weixin.qq.com/
- 使用商户号和密码登录

### 2. 下载API证书
```
路径：账户中心 → API安全 → API证书 → 下载证书
```

下载的压缩包包含：
- `apiclient_cert.pem` - 商户证书公钥
- `apiclient_key.pem` - 商户私钥 ⭐ (重要)
- `apiclient_cert.p12` - PKCS12格式证书

### 3. 获取商户证书序列号
在API证书页面可以看到证书序列号，格式类似：
```
1234567890ABCDEF1234567890ABCDEF12345678
```

### 4. 设置API v3密钥
```
路径：账户中心 → API安全 → API v3密钥 → 设置密钥
```
- 密钥长度：32位
- 建议使用随机生成的强密钥
- 示例：`your-api-v3-key-32-characters-long`

## 📁 证书文件部署

### 1. 创建证书目录
```bash
mkdir -p backend/src/main/resources/cert
```

### 2. 放置证书文件
将下载的证书文件放到项目中：
```
backend/src/main/resources/cert/
├── apiclient_key.pem      # 商户私钥
├── apiclient_cert.pem     # 商户证书
└── wechatpay_certificate.pem  # 微信支付平台证书(可选)
```

### 3. 配置文件权限
```bash
# Linux/Mac 环境下设置证书文件权限
chmod 600 backend/src/main/resources/cert/apiclient_key.pem
chmod 644 backend/src/main/resources/cert/apiclient_cert.pem
```

## ⚙️ 应用配置

### application.yml 配置
```yaml
wechat:
  pay:
    app-id: wx01184ae7c701390e  # 小程序AppID
    mch-id: 1234567890  # 商户号
    private-key-path: classpath:cert/apiclient_key.pem  # 私钥路径
    merchant-serial-number: 1234567890ABCDEF1234567890ABCDEF12345678  # 证书序列号
    api-v3-key: your-api-v3-key-32-characters-long  # API v3密钥
    notify-url: https://xx.aieo.cn/api/payment/wechat/notify  # 回调地址
```

## 🔍 验证配置

### 1. 证书文件检查
```bash
# 检查私钥文件格式
openssl rsa -in apiclient_key.pem -check -noout

# 检查证书文件格式
openssl x509 -in apiclient_cert.pem -text -noout

# 验证证书和私钥匹配
openssl x509 -noout -modulus -in apiclient_cert.pem | openssl md5
openssl rsa -noout -modulus -in apiclient_key.pem | openssl md5
# 两个MD5值应该相同
```

### 2. Java代码验证
```java
@PostConstruct
public void validateCertificate() {
    try {
        // 验证私钥文件是否可读
        Resource resource = new ClassPathResource("cert/apiclient_key.pem");
        if (!resource.exists()) {
            throw new RuntimeException("商户私钥文件不存在");
        }
        
        // 验证配置参数
        if (StringUtils.isEmpty(wechatPayConfig.getMchId())) {
            throw new RuntimeException("商户号未配置");
        }
        
        log.info("微信支付证书配置验证通过");
    } catch (Exception e) {
        log.error("微信支付证书配置验证失败", e);
    }
}
```

## 🚨 安全注意事项

### 1. 私钥保护
- ❌ 不要将私钥文件提交到版本控制系统
- ❌ 不要在日志中输出私钥内容
- ✅ 使用环境变量或配置中心管理敏感信息
- ✅ 定期轮换API密钥

### 2. .gitignore 配置
```gitignore
# 微信支付证书文件
backend/src/main/resources/cert/apiclient_key.pem
backend/src/main/resources/cert/apiclient_cert.pem
backend/src/main/resources/cert/*.p12

# 配置文件中的敏感信息
application-prod.yml
```

### 3. 生产环境配置
```yaml
# 使用环境变量
wechat:
  pay:
    mch-id: ${WECHAT_MCH_ID}
    api-v3-key: ${WECHAT_API_V3_KEY}
    private-key-path: ${WECHAT_PRIVATE_KEY_PATH}
```

## 🔧 常见问题解决

### 1. 证书格式错误
```
错误：java.security.spec.InvalidKeySpecException
解决：确保使用PEM格式的证书文件
```

### 2. 证书路径找不到
```
错误：FileNotFoundException
解决：检查classpath路径是否正确
```

### 3. 签名验证失败
```
错误：签名验证失败
解决：检查商户证书序列号是否正确
```

### 4. API v3密钥错误
```
错误：解密失败
解决：确认API v3密钥是否为32位且正确设置
```

## 📝 配置检查清单

- [ ] 已从微信商户平台下载API证书
- [ ] 证书文件已放置到正确目录
- [ ] 商户证书序列号已正确配置
- [ ] API v3密钥已设置(32位)
- [ ] 小程序AppID已配置
- [ ] 商户号已配置
- [ ] 回调地址已配置且可访问
- [ ] 证书文件权限已设置
- [ ] 敏感文件已添加到.gitignore
- [ ] 配置验证代码已通过

## 🎯 测试验证

### 1. 单元测试
```java
@Test
void testWechatPayConfig() {
    assertNotNull(wechatPayConfig.getMchId());
    assertNotNull(wechatPayConfig.getApiV3Key());
    assertTrue(wechatPayConfig.getApiV3Key().length() == 32);
}
```

### 2. 集成测试
创建一个小额测试订单验证整个支付流程。

---

**重要提醒**：
1. 证书文件包含敏感信息，请妥善保管
2. 定期检查证书有效期，及时更新
3. 生产环境使用环境变量管理敏感配置
4. 遵循微信支付安全规范