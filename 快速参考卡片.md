# 易享生活棋牌室小程序 - 快速参考卡片

## 🚀 一分钟快速启动

### 后端启动
```bash
cd backend
mvn spring-boot:run
# 访问: http://localhost:8080
```

### 小程序启动
```bash
# 1. 打开微信开发者工具
# 2. 导入 miniprogram 目录
# 3. AppID: wx9f4a33e5f2b31a6d
# 4. 点击编译
```

---

## 📊 核心信息速查

### 服务器信息
| 项目 | 值 |
|------|-----|
| IP | 121.43.96.127 |
| 域名 | https://xx.aieo.cn |
| SSH | ssh root@121.43.96.127 |

### 数据库信息
| 项目 | 值 |
|------|-----|
| 主机 | 121.43.96.127 |
| 端口 | 3306 |
| 数据库 | easy_joy_life_db |
| 用户 | root |
| 密码 | EasyJoyLife2024!@# |

### 微信支付信息
| 项目 | 值 |
|------|-----|
| AppID | wx9f4a33e5f2b31a6d |
| 商户号 | 1671050768 |
| 回调地址 | https://xx.aieo.cn/api/payment/wechat/notify |

---

## 🔧 常用命令

### 服务管理
```bash
# 查看状态
systemctl status easy-joy-life-backend

# 重启服务
systemctl restart easy-joy-life-backend

# 查看日志
tail -f /opt/easy-joy-life/logs/application.log
```

### 数据库查询
```bash
# 连接数据库
mysql -h 121.43.96.127 -P 3306 -u root -p'EasyJoyLife2024!@#' easy_joy_life_db

# 查看钱包
SELECT * FROM user_wallets WHERE user_id = 1;

# 查看交易记录
SELECT * FROM wallet_transactions WHERE user_id = 1 ORDER BY created_time DESC LIMIT 5;

# 查看支付订单
SELECT * FROM payment_orders WHERE user_id = 1 ORDER BY created_time DESC LIMIT 5;
```

### Git 操作
```bash
# 拉取代码
git pull origin main

# 提交代码
git add .
git commit -m "feat: 描述"
git push origin main
```

---

## 📱 核心 API

### 用户登录
```http
POST /api/users/wechat/login
Body: { "code": "微信code" }
```

### 获取钱包
```http
GET /api/payment/wallet/{userId}
```

### 创建充值订单
```http
POST /api/payment/recharge
Body: { "userId": 1, "amount": 100, "paymentMethod": "WECHAT" }
```

### 获取交易记录
```http
GET /api/payment/transactions/{userId}
```

---

## 🗂️ 数据库表

| 表名 | 说明 |
|------|------|
| users | 用户表 |
| stores | 门店表 |
| rooms | 房间表 |
| user_wallets | 用户钱包表 |
| payment_orders | 支付订单表 |
| wallet_transactions | 钱包交易记录表 |

---

## 🐛 问题排查

### 后端无法启动
1. 检查 MySQL 是否启动
2. 检查端口 8080 是否被占用
3. 查看日志：`tail -f logs/application.log`

### 小程序无法调用接口
1. 检查后端是否启动
2. 检查 utils/config.js 配置
3. 勾选"不校验合法域名"

### 支付失败
1. 使用真机测试
2. 检查证书文件是否存在
3. 查看后端日志

---

## 📞 紧急联系

- **微信支付客服**: 95017
- **服务器问题**: 检查 systemctl status
- **数据库问题**: 检查 MySQL 日志

---

**详细文档请查看：新人开发指南.md**
