# 钱包功能快速参考

## 页面路径

| 功能 | 路径 | 说明 |
|------|------|------|
| 钱包首页 | `/pages/wallet/wallet` | 显示余额、交易记录 |
| 银行卡管理 | `/pages/bank-card/bank-card` | 银行卡列表 |
| 添加银行卡 | `/pages/add-bank-card/add-bank-card` | 添加新银行卡 |
| 支付密码 | `/pages/pay-password/pay-password` | 设置/修改密码 |

## 主要功能

### 1. 钱包首页
- 显示余额、冻结金额、累计充值
- 快捷操作：充值、提现、转账、明细
- 最近交易记录（5条）
- 银行卡管理入口
- 支付密码入口

### 2. 银行卡管理
- 查看所有银行卡
- 卡号脱敏显示
- 设置默认银行卡
- 删除银行卡
- 添加新银行卡

### 3. 添加银行卡
- 输入持卡人姓名
- 输入银行卡号（自动识别）
- 手机验证码验证
- 设置默认选项

### 4. 支付密码
- 首次设置6位密码
- 修改密码（需原密码）
- 简单密码检测
- 密码点状显示

## API接口

### 银行卡
```
GET    /api/user/bank-cards/{userId}           # 获取列表
POST   /api/user/bank-cards                    # 添加银行卡
POST   /api/user/bank-cards/set-default        # 设为默认
DELETE /api/user/bank-cards/{cardId}           # 删除
POST   /api/bank/identify                      # 识别银行
```

### 支付密码
```
GET  /api/user/has-pay-password/{userId}       # 检查是否设置
POST /api/user/pay-password                    # 设置/修改
```

### 短信
```
POST /api/sms/send-code                        # 发送验证码
```

## 数据格式

### 银行卡
```javascript
{
  id: 1,
  userId: 1,
  holderName: "张三",
  cardNo: "6222021234567890",
  cardNoMasked: "6222 **** **** 7890",
  bankName: "工商银行",
  bankCode: "ICBC",
  cardType: "储蓄卡",
  phone: "13800138000",
  isDefault: true,
  status: "ACTIVE"
}
```

### 支付密码
```javascript
{
  userId: 1,
  hasPassword: true,
  oldPassword: "123456",  // 修改时
  newPassword: "654321"
}
```

## 使用示例

### 跳转到银行卡管理
```javascript
wx.navigateTo({
  url: '/pages/bank-card/bank-card'
});
```

### 跳转到支付密码
```javascript
wx.navigateTo({
  url: '/pages/pay-password/pay-password'
});
```

### 获取银行卡列表
```javascript
const app = getApp();
wx.request({
  url: `${app.globalData.apiBaseUrl}/api/user/bank-cards/${userId}`,
  method: 'GET',
  success: (res) => {
    console.log(res.data.cards);
  }
});
```

## 注意事项

1. **安全性**
   - 银行卡号加密存储
   - 支付密码加盐哈希
   - 验证码有效期5分钟

2. **用户体验**
   - 表单实时验证
   - 操作结果及时反馈
   - 加载状态清晰

3. **业务规则**
   - 最多5张银行卡
   - 必须有默认卡
   - 密码6位数字

## 测试账号

```
用户ID: 1
手机号: 13800138000
验证码: 123456 (测试环境)
```

## 常见问题

**Q: 如何测试银行卡功能？**
A: 使用测试卡号 6222021234567890

**Q: 验证码收不到？**
A: 测试环境使用固定验证码 123456

**Q: 支付密码忘记了？**
A: 联系客服重置

## 更新日志

**2026-02-08**
- ✅ 移除自动充值功能
- ✅ 新增银行卡管理
- ✅ 新增支付密码设置
- ✅ 完善UI和交互
