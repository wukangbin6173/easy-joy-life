# 测试短信配置

## 问题诊断

### 错误信息
```
code=isv.SMS_SIGNATURE_ILLEGAL
message=该账号下找不到对应签名
```

### 可能原因

1. **签名未审核通过**
   - 签名还在审核中
   - 签名审核被拒绝

2. **签名名称不正确**
   - 配置的签名名称与阿里云控制台不一致
   - 签名名称有空格或特殊字符

3. **AccessKey 权限问题**
   - AccessKey 没有短信服务权限
   - 使用了错误的 AccessKey

## 解决步骤

### 1. 检查签名状态

登录阿里云控制台：https://dysms.console.aliyun.com

1. 点击"国内消息" → "签名管理"
2. 查看签名状态：
   - ✅ **审核通过** - 可以使用
   - ⏳ **审核中** - 需要等待（2-24小时）
   - ❌ **审核不通过** - 需要修改重新提交

### 2. 确认签名名称

在签名管理页面，复制**准确的签名名称**（不包括【】符号）

例如：
- 如果显示：【东阳市耕琅坊包装材料】
- 配置应该是：`东阳市耕琅坊包装材料`

### 3. 检查模板状态

1. 点击"国内消息" → "模板管理"
2. 找到模板CODE：`SMS_501750341`
3. 确认状态为"审核通过"

### 4. 验证 AccessKey

1. 访问：https://ram.console.aliyun.com/manage/ak
2. 确认 AccessKey ID 是否正确
3. 检查权限：是否有 `AliyunDysmsFullAccess` 权限

## 临时解决方案

### 方案一：使用开发模式（推荐测试）

如果签名还在审核中，可以暂时切换回开发模式：

**修改 SmsController.java：**
```java
private static final boolean DEV_MODE = true;  // 改为 true
```

**修改 BankCardController.java：**
```java
private static final boolean DEV_MODE = true;  // 改为 true
```

然后重新编译部署。

### 方案二：等待签名审核

- **工作日**：2小时内
- **非工作日**：24小时内

可以联系阿里云客服加急审核：95187

### 方案三：重新创建签名

如果签名审核不通过，需要：
1. 查看审核意见
2. 修改签名或更换证明材料
3. 重新提交审核

## 当前配置

### 服务器配置文件
位置：`/opt/easy-joy-life/backend/src/main/resources/application-prod.yml`

```yaml
aliyun:
  sms:
    access-key-id: YOUR_ACCESS_KEY_ID
    access-key-secret: YOUR_ACCESS_KEY_SECRET
    sign-name: 东阳市耕琅坊包装材料
    template-code: SMS_501750341
    region-id: cn-hangzhou
```

## 测试步骤

### 1. 再次测试发送

在小程序中：
1. 进入"添加银行卡"
2. 填写手机号
3. 点击"发送验证码"

### 2. 查看日志

```bash
ssh root@121.43.96.127 "journalctl -u easy-joy-life-backend -f | grep -i sms"
```

### 3. 分析错误码

**成功：**
```
短信发送成功: phone=138****8000, requestId=xxx
```

**失败：**
```
短信发送失败: phone=138****8000, code=xxx, message=xxx
```

## 常见错误码

| 错误码 | 说明 | 解决方法 |
|--------|------|---------|
| isv.SMS_SIGNATURE_ILLEGAL | 签名不合法 | 检查签名是否审核通过 |
| isv.SMS_TEMPLATE_ILLEGAL | 模板不合法 | 检查模板是否审核通过 |
| isv.BUSINESS_LIMIT_CONTROL | 触发流控 | 等待60秒 |
| isv.AMOUNT_NOT_ENOUGH | 余额不足 | 充值 |
| isv.INVALID_PARAMETERS | 参数错误 | 检查配置 |

## 联系支持

### 阿里云客服
- **电话**：95187
- **在线客服**：控制台右下角
- **工单**：https://workorder.console.aliyun.com

### 查询内容
1. 签名审核状态
2. 模板审核状态
3. AccessKey 权限
4. 错误码详细说明

## 下一步

1. ✅ 服务已重启，配置已更新
2. ⏳ 请检查阿里云控制台签名状态
3. ⏳ 如果签名已审核通过，再次测试
4. ⏳ 如果签名还在审核中，等待或使用开发模式

**请告诉我签名的审核状态，我会帮你进一步处理！**
