# 银行卡和支付密码后端实现完成

## 已创建的文件

### 1. 实体类 (Entity)
- ✅ `BankCard.java` - 银行卡实体
- ✅ `PayPassword.java` - 支付密码实体

### 2. 数据访问层 (Repository)
- ✅ `BankCardRepository.java` - 银行卡数据访问
- ✅ `PayPasswordRepository.java` - 支付密码数据访问

### 3. 服务层 (Service)
- ✅ `BankCardService.java` - 银行卡业务逻辑
- ✅ `PayPasswordService.java` - 支付密码业务逻辑

### 4. 控制器 (Controller)
- ✅ `BankCardController.java` - 银行卡API接口
- ✅ `PayPasswordController.java` - 支付密码API接口

### 5. 数据库脚本
- ✅ `db-init-bank-card-paypassword.sql` - 数据库初始化脚本

---

## 功能特性

### 银行卡管理

#### 1. 获取银行卡列表
```
GET /api/user/bank-cards/{userId}
```
- 返回用户所有银行卡
- 卡号自动脱敏显示
- 按默认卡和创建时间排序

#### 2. 添加银行卡
```
POST /api/user/bank-cards
{
  "userId": 1,
  "holderName": "张三",
  "cardNo": "6222021234567890",
  "phone": "13800138000",
  "isDefault": false
}
```
- 自动识别银行
- 卡号AES加密存储
- 最多5张银行卡
- 自动处理默认卡逻辑

#### 3. 设置默认银行卡
```
POST /api/user/bank-cards/set-default
{
  "userId": 1,
  "cardId": 1
}
```

#### 4. 删除银行卡
```
DELETE /api/user/bank-cards/{cardId}?userId=1
```
- 自动处理默认卡切换

#### 5. 识别银行
```
POST /api/user/bank-cards/identify
{
  "cardNo": "622202"
}
```
- 根据卡号前6位识别银行

### 支付密码管理

#### 1. 检查是否设置密码
```
GET /api/user/has-pay-password/{userId}
```

#### 2. 设置/修改密码
```
POST /api/user/pay-password
{
  "userId": 1,
  "oldPassword": "123456",  // 修改时需要
  "newPassword": "654321"
}
```
- 首次设置无需原密码
- 修改需要验证原密码
- 密码SHA-256加盐加密
- 错误5次锁定30分钟

#### 3. 验证密码
```
POST /api/user/verify-pay-password
{
  "userId": 1,
  "password": "123456"
}
```

#### 4. 重置密码（管理员）
```
POST /api/user/reset-pay-password/{userId}
```

---

## 安全特性

### 银行卡安全
1. **卡号加密**
   - 使用AES加密算法
   - 密钥独立管理
   - 前端仅显示脱敏卡号

2. **数据验证**
   - 卡号格式验证
   - 手机号格式验证
   - 重复卡号检测

3. **权限控制**
   - 用户只能操作自己的银行卡
   - 删除需要二次确认

### 支付密码安全
1. **密码加密**
   - SHA-256哈希算法
   - 随机盐值
   - 不可逆加密

2. **防暴力破解**
   - 错误次数限制（5次）
   - 自动锁定机制（30分钟）
   - 验证成功自动重置

3. **密码规则**
   - 必须6位数字
   - 不允许简单密码

---

## 数据库表结构

### user_bank_cards（银行卡表）
```sql
CREATE TABLE user_bank_cards (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    holder_name VARCHAR(50) NOT NULL,
    card_no VARCHAR(128) NOT NULL,      -- 加密存储
    bank_name VARCHAR(50) NOT NULL,
    bank_code VARCHAR(20) NOT NULL,
    card_type VARCHAR(20) NOT NULL,     -- DEBIT/CREDIT
    phone VARCHAR(20) NOT NULL,
    is_default BOOLEAN DEFAULT FALSE,
    status VARCHAR(20) DEFAULT 'ACTIVE',
    created_time DATETIME NOT NULL,
    updated_time DATETIME NOT NULL,
    INDEX idx_user_id (user_id),
    INDEX idx_card_no (card_no)
);
```

### user_pay_passwords（支付密码表）
```sql
CREATE TABLE user_pay_passwords (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,     -- 加密存储
    salt VARCHAR(64) NOT NULL,
    error_count INT DEFAULT 0,
    locked_until DATETIME,
    created_time DATETIME NOT NULL,
    updated_time DATETIME NOT NULL,
    INDEX idx_user_id (user_id)
);
```

---

## 部署步骤

### 1. 执行数据库脚本
```bash
# 连接到MySQL
mysql -u root -p easy_joy_life_db

# 执行初始化脚本
source backend/src/main/resources/db-init-bank-card-paypassword.sql
```

或者在MySQL客户端中：
```sql
USE easy_joy_life_db;
SOURCE /path/to/db-init-bank-card-paypassword.sql;
```

### 2. 验证表创建
```sql
-- 查看表
SHOW TABLES LIKE 'user_%';

-- 查看表结构
DESC user_bank_cards;
DESC user_pay_passwords;

-- 查看表注释
SELECT TABLE_NAME, TABLE_COMMENT 
FROM information_schema.TABLES 
WHERE TABLE_SCHEMA = 'easy_joy_life_db' 
AND TABLE_NAME IN ('user_bank_cards', 'user_pay_passwords');
```

### 3. 重启后端服务
```bash
cd backend

# 停止服务
./stop.sh

# 启动服务
./start-dev.bat
# 或
java -jar target/easyjoylife-backend.jar
```

### 4. 验证API
```bash
# 检查是否设置支付密码
curl http://localhost:8080/api/user/has-pay-password/1

# 获取银行卡列表
curl http://localhost:8080/api/user/bank-cards/1
```

---

## 测试步骤

### 1. 测试支付密码

#### 设置密码
```bash
curl -X POST http://localhost:8080/api/user/pay-password \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "newPassword": "123456"
  }'
```

预期响应：
```json
{
  "success": true,
  "message": "设置成功"
}
```

#### 修改密码
```bash
curl -X POST http://localhost:8080/api/user/pay-password \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "oldPassword": "123456",
    "newPassword": "654321"
  }'
```

#### 验证密码
```bash
curl -X POST http://localhost:8080/api/user/verify-pay-password \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "password": "654321"
  }'
```

### 2. 测试银行卡管理

#### 添加银行卡
```bash
curl -X POST http://localhost:8080/api/user/bank-cards \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "holderName": "张三",
    "cardNo": "6222021234567890",
    "phone": "13800138000",
    "isDefault": true
  }'
```

#### 获取银行卡列表
```bash
curl http://localhost:8080/api/user/bank-cards/1
```

预期响应：
```json
{
  "success": true,
  "cards": [
    {
      "id": 1,
      "holderName": "张三",
      "cardNo": "6222 **** **** 7890",
      "bankName": "工商银行",
      "bankCode": "ICBC",
      "cardType": "储蓄卡",
      "isDefault": true,
      "status": "ACTIVE",
      "createdTime": "2026-02-08T10:30:00"
    }
  ]
}
```

#### 设置默认银行卡
```bash
curl -X POST http://localhost:8080/api/user/bank-cards/set-default \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "cardId": 1
  }'
```

#### 删除银行卡
```bash
curl -X DELETE "http://localhost:8080/api/user/bank-cards/1?userId=1"
```

---

## 小程序测试

### 1. 重新编译小程序
在微信开发者工具中点击"编译"

### 2. 测试银行卡管理
1. 进入"我的" -> "钱包"
2. 点击"银行卡管理"
3. 点击"添加银行卡"
4. 填写信息并提交
5. 查看银行卡列表
6. 测试设置默认和删除功能

### 3. 测试支付密码
1. 进入"我的" -> "钱包"
2. 点击"支付密码"
3. 设置6位数字密码
4. 确认密码
5. 提交
6. 再次进入测试修改密码

---

## 常见问题

### Q1: 表创建失败
**检查**：
- 数据库是否存在
- 用户是否有CREATE权限
- 表名是否冲突

**解决**：
```sql
-- 删除旧表（谨慎操作）
DROP TABLE IF EXISTS user_bank_cards;
DROP TABLE IF EXISTS user_pay_passwords;

-- 重新执行创建脚本
SOURCE db-init-bank-card-paypassword.sql;
```

### Q2: API返回404
**检查**：
- 后端服务是否启动
- 端口是否正确（8080）
- Controller路径是否正确

**解决**：
```bash
# 查看后端日志
tail -f backend/logs/spring.log

# 检查端口
netstat -ano | findstr 8080
```

### Q3: 卡号加密失败
**检查**：
- AES密钥长度（必须16字节）
- Java加密库是否可用

**解决**：
修改 `BankCardService.java` 中的 `AES_KEY`

### Q4: 密码验证失败
**检查**：
- 密码是否6位数字
- 是否被锁定
- 错误次数是否超限

**解决**：
```sql
-- 重置错误次数
UPDATE user_pay_passwords 
SET error_count = 0, locked_until = NULL 
WHERE user_id = 1;
```

---

## 配置说明

### AES加密密钥
在 `BankCardService.java` 中：
```java
private static final String AES_KEY = "EasyJoyLife12345"; // 16字节
```

**生产环境建议**：
- 从配置文件读取
- 使用环境变量
- 定期更换密钥

### 密码锁定配置
在 `PayPasswordService.java` 中：
```java
private static final int MAX_ERROR_COUNT = 5;           // 最大错误次数
private static final int LOCK_DURATION_MINUTES = 30;    // 锁定时长
```

---

## 性能优化建议

### 1. 数据库索引
```sql
-- 已创建的索引
CREATE INDEX idx_user_id ON user_bank_cards(user_id);
CREATE INDEX idx_card_no ON user_bank_cards(card_no);
CREATE INDEX idx_user_id ON user_pay_passwords(user_id);
```

### 2. 缓存策略
- 银行卡列表可以缓存5分钟
- 支付密码验证结果不缓存

### 3. 并发控制
- 使用 `@Transactional` 保证事务一致性
- 设置默认卡时使用数据库锁

---

## 安全建议

### 1. 生产环境
- 更换AES密钥
- 启用HTTPS
- 添加请求签名验证
- 记录操作日志

### 2. 敏感数据
- 卡号加密存储
- 密码加盐哈希
- 日志脱敏处理

### 3. 访问控制
- 添加用户认证
- 验证用户权限
- 限制请求频率

---

## 总结

✅ **已完成**：
1. 创建实体类（BankCard, PayPassword）
2. 创建Repository接口
3. 实现Service业务逻辑
4. 实现Controller API接口
5. 创建数据库初始化脚本
6. 添加安全加密功能
7. 实现防暴力破解机制

⏳ **待测试**：
1. 执行数据库脚本
2. 重启后端服务
3. 测试API接口
4. 小程序端到端测试

**下一步**：
1. 执行 `db-init-bank-card-paypassword.sql`
2. 重启后端服务
3. 在小程序中测试功能
