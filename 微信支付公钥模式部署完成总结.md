# 微信支付平台证书过期问题 - 最终解决方案

## 🔍 问题分析

### 当前错误
```
{"code":"NOT_ENOUGH","message":"平台证书已过期失效，请联系微信支付技术支持。"}
```

### 根本原因
- 微信支付**平台证书**已过期（5年有效期）
- 当前SDK使用`RSAAutoCertificateConfig`自动下载证书
- 由于平台证书过期，SDK无法自动下载新证书

### 重要概念区分

| 概念 | 文件 | 用途 | 是否过期 |
|------|------|------|---------|
| **商户私钥** | `apiclient_key.pem` | 商户签名请求 | ✅ 不过期 |
| **商户证书** | `apiclient_cert.pem` | 商户身份证明 | ✅ 有效 |
| **微信支付公钥** | `wechatpay_public_key.pem` | 验证微信签名（仅验签） | ✅ 有效 |
| **微信支付平台证书** | 自动下载 | 完整的证书链（签名+验签） | ❌ 已过期 |

**关键发现**：
- 用户已配置的`wechatpay_public_key.pem`是**微信支付公钥**，用于验证签名
- SDK的`RSAConfig`需要的是**平台证书**（X.509格式），不是公钥
- 公钥和平台证书是不同的东西，不能互相替代

## ✅ 解决方案

### 方案1：联系微信支付技术支持（推荐）⭐⭐⭐⭐⭐

这是**最简单、最快速、最安全**的解决方案。

#### 操作步骤

1. **登录微信支付商户平台**
   ```
   网址：https://pay.weixin.qq.com
   账号：使用超级管理员账号登录
   ```

2. **联系技术支持**
   - 方式1：在线客服（商户平台右下角）
   - 方式2：拨打客服电话：95017
   - 方式3：提交工单

3. **说明问题**
   ```
   商户号：1554487931
   问题：平台证书已过期，错误代码 NOT_ENOUGH
   请求：更新微信支付平台证书
   ```

4. **等待处理**
   - 通常24小时内处理完成
   - 微信会自动更新平台证书

5. **重启服务**
   ```bash
   ssh root@xx.aieo.cn "systemctl restart easy-joy-life-backend"
   ```

#### 优点
- ✅ 无需修改代码
- ✅ 无需升级SDK
- ✅ 处理速度快（24小时内）
- ✅ 由官方处理，安全可靠
- ✅ 零技术风险

---

### 方案2：手动下载平台证书（备选方案）⭐⭐⭐

如果方案1无法解决，可以尝试手动下载平台证书。

#### 前提条件
- 需要有效的商户私钥和API v3密钥
- 需要编写代码调用微信支付API

#### 操作步骤

1. **使用微信支付API下载证书**
   - API地址：`https://api.mch.weixin.qq.com/v3/certificates`
   - 需要使用商户私钥签名请求
   - 响应会返回加密的证书内容

2. **解密证书**
   - 使用API v3密钥解密证书内容
   - 保存为PEM格式文件

3. **修改代码使用本地证书**
   ```java
   // 使用RSAConfig加载本地证书
   this.config = new RSAConfig.Builder()
           .merchantId(wechatPayConfig.getMchId())
           .privateKeyFromPath(wechatPayConfig.getPrivateKeyPath())
           .merchantSerialNumber(wechatPayConfig.getMerchantSerialNumber())
           .wechatPayCertificatesFromPath(certificatePath)  // 本地证书路径
           .build();
   ```

#### 缺点
- ⚠️ 需要编写额外代码
- ⚠️ 证书仍会过期（5年后需要再次更新）
- ⚠️ 技术复杂度较高

---

### 方案3：升级到最新SDK并使用公钥模式（长期方案）⭐⭐⭐⭐

**注意**：经过研究发现，当前SDK版本（0.2.17）的"公钥模式"仅用于**验证签名**，不能完全替代平台证书。

#### 当前限制
- `wechatpay_public_key.pem`只能用于验证微信的响应签名
- 不能用于完整的支付流程配置
- SDK仍然需要平台证书来处理完整的加密/解密流程

#### 未来可能性
- 关注微信支付SDK更新
- 等待官方推出完整的公钥模式支持
- 目前不推荐作为主要解决方案

---

## 🚀 推荐执行流程

### 立即执行（方案1）

```bash
# 步骤1：登录商户平台
浏览器打开: https://pay.weixin.qq.com

# 步骤2：联系技术支持
说明: 商户号 1554487931 的平台证书已过期
错误: {"code":"NOT_ENOUGH","message":"平台证书已过期失效"}
请求: 更新平台证书

# 步骤3：等待处理（通常24小时内）

# 步骤4：证书更新后重启服务
ssh root@xx.aieo.cn "systemctl restart easy-joy-life-backend"

# 步骤5：测试支付功能
# 在小程序中发起支付测试
```

---

## 📊 验证方法

### 1. 检查服务日志

```bash
# 查看微信支付初始化日志
ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -n 100 --no-pager | grep -i '微信支付'"

# 查看错误日志
ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -p err --no-pager | tail -50"
```

### 2. 测试支付功能

1. 打开小程序
2. 进入充值页面
3. 选择充值金额
4. 点击"微信支付"
5. 观察是否还有证书过期错误

### 3. 预期结果

**成功标志**：
- ✅ 不再出现"NOT_ENOUGH"错误
- ✅ 不再出现"平台证书已过期"错误
- ✅ 支付流程正常完成
- ✅ 可以成功调起微信支付

---

## 📞 联系方式

### 微信支付商户平台
- **网址**：https://pay.weixin.qq.com
- **客服电话**：95017
- **在线客服**：商户平台右下角聊天图标

### 商户信息
- **商户号**：1554487931
- **小程序AppID**：wx9f4a33e5f2b31a6d
- **问题类型**：平台证书过期

---

## 💡 为什么推荐方案1？

| 对比项 | 方案1（联系微信） | 方案2（手动下载） | 方案3（公钥模式） |
|--------|------------------|------------------|------------------|
| **难度** | ⭐ 非常简单 | ⭐⭐⭐ 复杂 | ⭐⭐⭐⭐ 很复杂 |
| **耗时** | 24小时 | 1-2天 | 3-5天 |
| **效果** | 永久解决 | 临时解决（5年） | 目前不可行 |
| **风险** | 零风险 | 中等风险 | 高风险 |
| **推荐度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |

---

## 🎯 后续优化建议

证书问题解决后，建议：

1. **监控证书有效期**
   - 定期检查平台证书状态
   - 提前3个月续期

2. **完善错误处理**
   - 添加更友好的错误提示
   - 记录详细的错误日志

3. **关注SDK更新**
   - 定期查看wechatpay-java更新日志
   - 关注是否有新的证书管理方案

---

## ✅ 检查清单

- [ ] 已登录微信支付商户平台
- [ ] 已联系技术支持
- [ ] 已说明商户号和问题
- [ ] 等待微信处理（24小时内）
- [ ] 证书更新后重启服务
- [ ] 测试支付功能是否恢复
- [ ] 记录问题和解决过程

---

## 📝 总结

**当前状态**：
- ❌ 微信支付平台证书已过期
- ✅ 商户私钥和证书有效
- ✅ 微信支付公钥有效（但不能替代平台证书）
- ✅ SDK配置正确

**推荐方案**：
- 🎯 **方案1**：联系微信支付技术支持更新平台证书（强烈推荐）
- 🔄 **方案2**：手动下载平台证书（备选方案）
- ⏳ **方案3**：等待SDK支持完整公钥模式（长期方案）

**预计解决时间**：24小时内

---

**创建时间**：2026-02-07  
**问题状态**：待处理  
**负责人**：商户管理员  
**优先级**：高

