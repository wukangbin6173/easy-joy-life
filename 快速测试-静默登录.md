# 快速测试 - 静默登录修复

## 测试前准备

### 1. 清除缓存
在微信开发者工具中：
- 点击"清缓存" -> "清除数据缓存"
- 或在控制台执行：
  ```javascript
  wx.clearStorageSync();
  ```

### 2. 重新编译
- 点击"编译"按钮
- 或使用快捷键重新编译

---

## 测试步骤

### 步骤1: 查看启动日志

打开控制台（Console），应该看到：

```
✅ 预期日志：
小程序启动
API地址: https://xx.aieo.cn
开始自动静默登录...
获取微信登录code成功: 071xxx...
微信登录API响应: {success: true, openid: "oXXX...", user: {...}}
========================================
✅ 静默登录成功！
OpenId: oXXX...
用户ID: 1
用户昵称: 微信用户xxx
========================================
```

❌ **如果看到错误**：
- "静默登录API调用失败" → 检查网络和后端服务
- "微信登录失败" → 检查 AppId 和 AppSecret 配置

---

### 步骤2: 验证登录状态

在控制台执行：
```javascript
const app = getApp();
console.log('用户ID:', app.globalData.userId);
console.log('OpenId:', app.globalData.openid);
console.log('用户信息:', app.globalData.userInfo);
```

✅ **预期结果**：
```javascript
用户ID: 1  // 或其他数字
OpenId: oXXXX...  // 微信openid
用户信息: {
  id: 1,
  nickname: "微信用户xxx",
  avatar: "/images/default-avatar.png",
  isLogin: true
}
```

❌ **如果 userId 为 null 或 undefined**：
- 查看步骤1的日志，确认静默登录是否成功
- 检查后端返回的数据结构

---

### 步骤3: 测试钱包页面

1. 点击底部导航"我的"
2. 点击"钱包"卡片

✅ **预期结果**：
- 显示余额（如：¥0.30）
- 显示"最近交易"列表
- 无"用户未登录"提示

❌ **如果提示"用户未登录"**：
- 在控制台检查 `getApp().globalData.userId`
- 查看页面的 console 日志

---

### 步骤4: 测试交易记录页面

1. 在钱包页面点击"查看全部"
2. 或进入"我的" -> "交易记录"

✅ **预期结果**：
- 显示交易记录列表
- 可以切换"全部/收入/支出"
- 按日期分组显示

❌ **如果无数据**：
- 检查数据库是否有交易记录
- 查看 Network 标签的 API 请求

---

### 步骤5: 测试充值页面

1. 进入"我的" -> "钱包"
2. 点击"充值"按钮

✅ **预期结果**：
- 显示当前余额
- 可以选择充值金额
- 无"用户未登录"提示

---

### 步骤6: 测试个人中心

1. 进入"我的"页面
2. 查看用户统计信息

✅ **预期结果**：
- 显示钱包余额
- 显示用户统计
- 无"用户未登录"提示

---

## 控制台检查命令

### 查看完整登录状态
```javascript
const app = getApp();
console.log('========== 登录状态 ==========');
console.log('userId:', app.globalData.userId);
console.log('openid:', app.globalData.openid);
console.log('sessionKey:', app.globalData.sessionKey);
console.log('userInfo:', app.globalData.userInfo);
console.log('apiBaseUrl:', app.globalData.apiBaseUrl);
console.log('==============================');
```

### 查看缓存
```javascript
console.log('========== 缓存数据 ==========');
console.log('userId:', wx.getStorageSync('userId'));
console.log('openid:', wx.getStorageSync('openid'));
console.log('userInfo:', wx.getStorageSync('userInfo'));
console.log('==============================');
```

### 手动触发登录
```javascript
const app = getApp();
app.autoSilentLogin();
```

---

## 测试清单

### 基础功能
- [ ] 小程序启动时自动静默登录
- [ ] 控制台显示"✅ 静默登录成功！"
- [ ] `app.globalData.userId` 有值
- [ ] `app.globalData.openid` 有值

### 页面功能
- [ ] 钱包页面正常显示余额
- [ ] 钱包页面显示最近交易
- [ ] 交易记录页面显示完整列表
- [ ] 充值页面显示当前余额
- [ ] 个人中心显示用户统计

### 缓存功能
- [ ] 关闭小程序后重新打开，无需重新登录
- [ ] 从缓存恢复登录状态
- [ ] 控制台显示"从缓存恢复登录状态"

### 错误处理
- [ ] 网络错误时有友好提示
- [ ] 登录失败时使用默认信息
- [ ] 不会阻塞小程序启动

---

## 常见问题排查

### 问题1: 控制台无"静默登录成功"日志

**排查步骤**：
1. 检查是否有错误日志
2. 查看网络请求是否成功
3. 检查后端服务是否运行

```bash
# 测试后端服务
curl https://xx.aieo.cn/api/health
```

---

### 问题2: userId 为 undefined

**排查步骤**：
1. 查看后端返回数据
   ```javascript
   // 在 app.js 的 autoSilentLogin 中添加
   console.log('后端返回:', res);
   console.log('user对象:', res.user);
   console.log('user.id:', res.user.id);
   ```

2. 检查后端日志
   ```bash
   tail -f backend/logs/spring.log | grep "微信登录"
   ```

---

### 问题3: 页面还是提示"用户未登录"

**排查步骤**：
1. 确认静默登录成功
   ```javascript
   getApp().globalData.userId  // 应该有值
   ```

2. 检查页面代码
   ```javascript
   // 在页面的 onLoad 中添加
   console.log('当前userId:', app.globalData.userId);
   ```

3. 确认页面使用正确的变量名
   - 应该使用 `app.globalData.userId`
   - 不是 `app.globalData.user_id`

---

### 问题4: 缓存恢复失败

**排查步骤**：
1. 查看缓存内容
   ```javascript
   console.log('缓存userId:', wx.getStorageSync('userId'));
   ```

2. 清除缓存重新登录
   ```javascript
   wx.clearStorageSync();
   // 然后重新编译
   ```

---

## 成功标志

当看到以下情况时，说明修复成功：

✅ **控制台日志**
```
✅ 静默登录成功！
用户ID: 1
```

✅ **页面正常显示**
- 钱包余额正常
- 交易记录正常
- 无"用户未登录"提示

✅ **缓存正常工作**
- 重启小程序无需重新登录
- 从缓存恢复登录状态

---

## 下一步

测试通过后：
1. 真机测试
2. 提交代码
3. 上传新版本
4. 版本号：1.0.7
5. 更新说明：优化静默登录，自动设置用户ID

---

**测试时间**: _______
**测试人员**: _______
**测试结果**: ⏳ 待测试
