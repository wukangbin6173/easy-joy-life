# 小程序数据来源说明

## 当前配置状态 ✅

### 小程序数据模式
- **当前设置**: `mockMode: false` (真实API模式)
- **数据来源**: 后端API → H2数据库

### 后端数据库
- **数据库类型**: H2内存数据库 (不是MySQL)
- **数据持久化**: 内存存储，重启后重新初始化
- **数据初始化**: 通过DataInitializer自动创建测试数据

## 数据来源对比 📊

### 模拟数据模式 (`mockMode: true`)
```
小程序 → mockData.js → 静态数据
```
- ✅ 优点: 不依赖网络，响应快速
- ❌ 缺点: 数据固定，无法通过管理后台修改

### 真实API模式 (`mockMode: false`) - 当前模式
```
小程序 → wx.request → 后端API → H2数据库
```
- ✅ 优点: 数据可通过管理后台实时修改
- ✅ 优点: 完整的业务逻辑
- ⚠️ 注意: 需要后端服务运行

## 数据库详情 🗄️

### H2数据库特点
- **类型**: 内存数据库
- **访问地址**: http://localhost:8080/h2-console
- **用户名**: sa
- **密码**: (空)
- **JDBC URL**: jdbc:h2:mem:testdb

### 数据内容
- **门店数量**: 5个
- **房间数量**: 13个
- **数据完整性**: 包含所有业务字段

## 验证数据来源 🔍

### 方法1: 查看网络请求
在微信开发者工具控制台查看：
```javascript
// 如果看到网络请求，说明使用真实API
wx.request调用 → 后端API

// 如果没有网络请求，说明使用模拟数据
直接返回mockData
```

### 方法2: 修改管理后台数据
1. 访问: http://localhost:8080/admin.html
2. 修改任意门店信息
3. 刷新小程序查看是否同步更新

### 方法3: 查看H2数据库
1. 访问: http://localhost:8080/h2-console
2. 连接数据库
3. 执行SQL: `SELECT * FROM STORE`

## 切换数据模式 🔄

### 切换到模拟数据
```javascript
// miniprogram/app.js
globalData: {
  mockMode: true // 使用模拟数据
}
```

### 切换到真实API (当前)
```javascript
// miniprogram/app.js
globalData: {
  mockMode: false // 使用真实API
}
```

## 升级到MySQL数据库 🚀

### 步骤1: 添加MySQL依赖
```xml
<!-- backend/pom.xml -->
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
    <scope>runtime</scope>
</dependency>
```

### 步骤2: 修改配置文件
```yaml
# backend/src/main/resources/application.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qiupai_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: your_password
```

### 步骤3: 创建数据库
```sql
CREATE DATABASE qiupai_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

## 当前推荐配置 ⭐

### 开发环境
- **小程序**: `mockMode: false` (真实API)
- **后端**: H2数据库 (快速开发)
- **管理**: 通过管理后台修改数据

### 生产环境
- **小程序**: `mockMode: false` (真实API)
- **后端**: MySQL数据库 (数据持久化)
- **部署**: 云服务器 + HTTPS域名

## 总结 📝

### 当前状态
- ✅ 小程序已切换到真实API模式
- ✅ 数据来自H2数据库 (不是MySQL)
- ✅ 可通过管理后台实时修改数据
- ✅ 支持完整的业务逻辑

### 数据流向
```
用户操作 → 小程序 → API请求 → Spring Boot → JPA → H2数据库
管理员 → 管理后台 → API请求 → Spring Boot → JPA → H2数据库
```

---
**更新时间**: 2025年1月19日
**当前模式**: 真实API + H2数据库
**数据同步**: 小程序 ↔ 管理后台 实时同步