# 微信支付平台证书过期解决方案

## 问题描述

微信支付返回错误：`{"code":"NOT_ENOUGH","message":"平台证书已过期失效，请联系微信支付技术支持。"}`

## 根本原因

微信支付平台证书有5年有效期，证书过期后会导致支付失败。微信官方推荐切换到**微信支付公钥模式**来避免证书过期问题。

## 解决方案：切换到微信支付公钥模式

### 步骤1：获取微信支付公钥

1. 登录微信支付商户平台 (https://pay.weixin.qq.com)
2. 进入【账户中心】-> 【API安全】
3. 找到【微信支付公钥】部分，点击【申请公钥】
4. 下载得到：
   - 公钥文件 (`wechatpay_public_key.pem`)
   - 公钥ID (格式: `PUB_KEY_ID_xxxxxxxxxxxxxxxxx`)

### 步骤2：配置公钥文件

将下载的公钥文件放置到：
```
backend/src/main/resources/cert/wechatpay_public_key.pem
```

### 步骤3：更新配置文件

在 `backend/src/main/resources/application.yml` 中添加公钥配置：

```yaml
wechat:
  pay:
    # 现有配置保持不变...
    # 新增公钥配置
    public-key-path: classpath:cert/wechatpay_public_key.pem
    public-key-id: PUB_KEY_ID_你的实际公钥ID  # 替换为实际的公钥ID
```

### 步骤4：代码已自动适配

我们的代码已经更新为智能模式：
- 优先使用微信支付公钥模式（如果配置了公钥）
- 回退到自动证书管理模式（如果未配置公钥）

### 步骤5：重新部署

1. 停止后端服务
2. 重新编译：`mvn clean package`
3. 重新部署到服务器
4. 测试微信支付功能

## 验证步骤

### 1. 运行公钥获取工具
```bash
python download-wechat-public-key.py
```

### 2. 检查后端启动日志
查看是否显示：
```
使用微信支付公钥模式初始化...
公钥路径: classpath:cert/wechatpay_public_key.pem
公钥ID: PUB_KEY_ID_xxxxxxxxx
微信支付公钥模式配置创建成功
```

### 3. 测试支付功能
```bash
node test-wechat-pay-config.js
```

## 优势对比

| 模式 | 平台证书模式 | 微信支付公钥模式 |
|------|-------------|-----------------|
| 证书过期 | 每5年需要更新 | 无过期问题 |
| 维护成本 | 需要定期维护 | 一次配置长期使用 |
| 稳定性 | 可能因证书过期中断 | 更加稳定 |
| 官方推荐 | 逐步淘汰 | 官方推荐 |

## 常见问题

### Q1: 商户平台找不到微信支付公钥选项？
A: 新商户号默认支持公钥模式。老商户号可能需要联系微信支付技术支持开通。

### Q2: 公钥ID格式是什么？
A: 固定格式为 `PUB_KEY_ID_` 开头，后跟一串数字和字母。

### Q3: 是否需要同时保留证书文件？
A: 建议保留，代码会智能选择最佳模式。

### Q4: 切换后是否影响现有业务？
A: 不影响，代码向下兼容，平滑切换。

## 技术细节

### 代码更新内容

1. **WechatPayService.java**：
   - 导入 `RSAPublicKeyConfig`
   - 智能选择配置模式
   - 优先使用公钥模式

2. **WechatPayConfig.java**：
   - 新增 `publicKeyPath` 配置
   - 新增 `publicKeyId` 配置

3. **application.yml**：
   - 新增公钥相关配置项

### 配置优先级

1. 如果配置了公钥路径和公钥ID → 使用公钥模式
2. 如果未配置公钥 → 使用自动证书管理模式

## 部署脚本

可以使用现有的部署脚本：
```bash
# Windows
go.bat

# Linux
./go.sh
```

## 监控和日志

部署后注意观察：
1. 后端启动日志中的微信支付初始化信息
2. 支付接口调用是否正常
3. 错误日志中是否还有证书相关错误

## 总结

通过切换到微信支付公钥模式，可以彻底解决平台证书过期问题，提高系统稳定性。这是微信官方推荐的最佳实践，建议所有商户尽快完成切换。