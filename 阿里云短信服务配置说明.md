# 阿里云短信服务配置说明

## ✅ 配置完成

### 1. 配置信息
- **短信签名**: `东阳市耕琅坊包装材料`
- **模板CODE**: `SMS_501750341`
- **模板名称**: `易享生活小程序短信验证`
- **AccessKey**: 已配置在服务器环境变量中

### 2. 已完成的配置

#### ✅ application.yml
```yaml
# 阿里云短信配置
aliyun:
  sms:
    access-key-id: ${ALIYUN_SMS_ACCESS_KEY_ID:}
    access-key-secret: ${ALIYUN_SMS_ACCESS_KEY_SECRET:}
    sign-name: 东阳市耕琅坊包装材料
    template-code: SMS_501750341
    region-id: cn-hangzhou
```

#### ✅ application-prod.yml（服务器专用）
生产环境配置文件，包含真实的 AccessKey，仅存在于服务器上，不提交到 Git。

#### ✅ pom.xml
添加了阿里云短信SDK依赖：
```xml
<!-- Aliyun SMS SDK -->
<dependency>
    <groupId>com.aliyun</groupId>
    <artifactId>dysmsapi20170525</artifactId>
    <version>2.0.24</version>
</dependency>
```

#### ✅ AliyunSmsService.java
创建了阿里云短信服务类，提供 `sendSms(phone, code)` 方法

#### ✅ SmsController.java
- 集成了 `AliyunSmsService`
- 切换到生产模式：`DEV_MODE = false`
- 真实短信发送已启用

#### ✅ BankCardController.java
- 切换到生产模式：`DEV_MODE = false`
- 添加银行卡时必须验证短信验证码

---

## 🚀 服务器配置

### 在服务器上配置 AccessKey

**方法一：使用 application-prod.yml（推荐）**

在服务器上创建 `backend/src/main/resources/application-prod.yml`：
```yaml
# 生产环境配置
aliyun:
  sms:
    access-key-id: YOUR_ACCESS_KEY_ID
    access-key-secret: YOUR_ACCESS_KEY_SECRET
```

启动时指定配置文件：
```bash
java -jar -Dspring.profiles.active=prod easy-joy-life-system-1.0.0.jar
```

**方法二：使用环境变量**

在服务器上设置环境变量：
```bash
export ALIYUN_SMS_ACCESS_KEY_ID="YOUR_ACCESS_KEY_ID"
export ALIYUN_SMS_ACCESS_KEY_SECRET="YOUR_ACCESS_KEY_SECRET"
```

或在 systemd 服务文件中配置：
```ini
[Service]
Environment="ALIYUN_SMS_ACCESS_KEY_ID=YOUR_ACCESS_KEY_ID"
Environment="ALIYUN_SMS_ACCESS_KEY_SECRET=YOUR_ACCESS_KEY_SECRET"
```

---

## 📱 测试步骤

### 1. 测试短信发送

**在小程序中测试：**
1. 打开小程序
2. 进入"添加银行卡"页面
3. 填写手机号（真实手机号）
4. 点击"发送验证码"
5. 检查手机是否收到短信

**预期短信内容：**
```
【东阳市耕琅坊包装材料】您的验证码为：123456，该验证码5分钟内有效。
```

### 2. 查看后端日志

```bash
ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -f | grep -i sms"
```

**成功日志示例：**
```
短信发送成功: phone=138****8000, requestId=xxx-xxx-xxx
```

---

## 🔍 常见问题排查

### Q1: 短信发送失败

**检查项：**
1. AccessKey 是否正确配置
2. 签名和模板是否审核通过
3. 账户余额是否充足
4. 手机号格式是否正确

**查看错误码：**
```bash
ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -n 100 | grep -i 'sms'"
```

**常见错误码：**
- `isv.BUSINESS_LIMIT_CONTROL`: 触发流控（60秒内只能发送一次）
- `isv.MOBILE_NUMBER_ILLEGAL`: 手机号格式错误
- `isv.AMOUNT_NOT_ENOUGH`: 余额不足
- `isv.INVALID_PARAMETERS`: 参数错误

---

## 💰 费用说明

### 短信价格
- **验证码短信**: 0.045元/条
- **通知短信**: 0.045元/条

### 月度成本估算
| 日发送量 | 月发送量 | 月费用 |
|---------|---------|--------|
| 10条 | 300条 | 13.5元 |
| 30条 | 900条 | 40.5元 |
| 100条 | 3000条 | 135元 |

---

## 🔒 安全建议

### 1. AccessKey 安全
- ✅ 不要提交到 Git 仓库
- ✅ 使用环境变量或生产配置文件
- ✅ 定期轮换 AccessKey
- ✅ 使用 RAM 子用户（生产环境）

### 2. 防刷措施
已实现的防护：
- ✅ 60秒发送频率限制
- ✅ 验证码5分钟有效期
- ✅ 验证后立即删除

---

## 📝 部署说明

### 本地构建
```bash
cd backend
.\mvnw.cmd clean package -DskipTests
```

### 部署到服务器
```bash
# 上传JAR包
scp backend/target/easy-joy-life-system-1.0.0.jar root@xx.aieo.cn:/opt/easy-joy-life/backend/target/

# 确保服务器上有 application-prod.yml 配置文件
# 或者配置了环境变量

# 重启服务
ssh root@xx.aieo.cn "systemctl restart easy-joy-life-backend"

# 查看日志
ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -f"
```

---

## 🎉 配置完成

阿里云短信服务已成功配置并集成到项目中！

**配置时间**: 2026-02-08
**配置状态**: ✅ 完成
**安全状态**: ✅ 敏感信息已保护

**下一步**: 部署到服务器并测试真实短信发送
