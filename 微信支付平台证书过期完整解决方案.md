# 微信支付平台证书过期完整解决方案

## 🚨 问题现状

**错误信息**: `{"code":"NOT_ENOUGH","message":"平台证书已过期失效，请联系微信支付技术支持。"}`

**根本原因**: 微信支付平台证书有5年有效期，证书过期导致支付功能失效。

## 🎯 解决方案：切换到微信支付公钥模式

根据最新的微信支付官方文档和网络搜索结果，**微信支付公钥模式**是官方推荐的解决方案，可以彻底避免证书过期问题。

## 📋 详细操作步骤

### 步骤1：获取微信支付公钥

1. **登录商户平台**
   - 访问：https://pay.weixin.qq.com
   - 使用超级管理员或安全联系人账号登录

2. **申请公钥**
   - 进入【账户中心】→ 【API安全】
   - 找到【微信支付公钥】部分
   - 点击【申请公钥】或【下载公钥】

3. **获取文件**
   - 公钥文件：`wechatpay_public_key.pem`
   - 公钥ID：格式为 `PUB_KEY_ID_xxxxxxxxxxxxxxxxx`

### 步骤2：部署公钥文件

将下载的公钥文件放置到：
```
backend/src/main/resources/cert/wechatpay_public_key.pem
```

### 步骤3：更新配置文件

在 `backend/src/main/resources/application.yml` 中添加：

```yaml
wechat:
  pay:
    # 现有配置保持不变
    app-id: wx9f4a33e5f2b31a6d
    mch-id: 1554487931
    private-key-path: classpath:cert/apiclient_key.pem
    merchant-serial-number: 68EFFAD8D54020146A1372141B6EEBA1B52D6B64
    api-v3-key: idNmbaZ0EPgwbvYbJDGp2EGlwQDezU6d
    notify-url: https://xx.aieo.cn/api/payment/wechat/notify
    
    # 新增公钥配置
    public-key-path: classpath:cert/wechatpay_public_key.pem
    public-key-id: PUB_KEY_ID_你的实际公钥ID  # 替换为从商户平台获取的真实公钥ID
```

### 步骤4：更新Java代码

#### 4.1 更新 WechatPayConfig.java

在 `backend/src/main/java/com/easyjoylife/config/WechatPayConfig.java` 中添加：

```java
/**
 * 微信支付公钥路径（推荐使用，避免证书过期问题）
 */
private String publicKeyPath;

/**
 * 微信支付公钥ID（推荐使用，避免证书过期问题）
 */
private String publicKeyId;
```

#### 4.2 更新 WechatPayService.java

在 `backend/src/main/java/com/easyjoylife/service/WechatPayService.java` 中：

1. **添加导入**：
```java
import com.wechat.pay.java.core.RSAPublicKeyConfig;
```

2. **更新初始化方法**：
```java
@PostConstruct
public void init() {
    try {
        log.info("开始初始化微信支付服务...");
        
        // 检查基础配置...
        
        // 优先使用微信支付公钥模式
        if (wechatPayConfig.getPublicKeyPath() != null && !wechatPayConfig.getPublicKeyPath().isEmpty() &&
            wechatPayConfig.getPublicKeyId() != null && !wechatPayConfig.getPublicKeyId().isEmpty()) {
            
            log.info("使用微信支付公钥模式初始化...");
            
            this.config = new RSAPublicKeyConfig.Builder()
                    .merchantId(wechatPayConfig.getMchId())
                    .privateKeyFromPath(wechatPayConfig.getPrivateKeyPath())
                    .publicKeyFromPath(wechatPayConfig.getPublicKeyPath())
                    .publicKeyId(wechatPayConfig.getPublicKeyId())
                    .merchantSerialNumber(wechatPayConfig.getMerchantSerialNumber())
                    .apiV3Key(wechatPayConfig.getApiV3Key())
                    .build();
                    
            log.info("微信支付公钥模式配置创建成功");
            
        } else {
            // 回退到自动证书管理模式
            log.warn("未配置微信支付公钥，使用自动证书管理模式");
            
            this.config = new RSAAutoCertificateConfig.Builder()
                    .merchantId(wechatPayConfig.getMchId())
                    .privateKeyFromPath(wechatPayConfig.getPrivateKeyPath())
                    .merchantSerialNumber(wechatPayConfig.getMerchantSerialNumber())
                    .apiV3Key(wechatPayConfig.getApiV3Key())
                    .build();
        }

        // 初始化JSAPI服务
        this.jsapiService = new JsapiServiceExtension.Builder()
                .config(config)
                .build();

        log.info("微信支付服务初始化成功");
    } catch (Exception e) {
        log.error("微信支付服务初始化失败: {}", e.getMessage(), e);
        this.config = null;
        this.jsapiService = null;
    }
}
```

### 步骤5：编译和部署

由于当前环境Java版本兼容性问题，建议：

1. **手动编译**（如果有Java 11环境）：
```bash
# 切换到Java 11
export JAVA_HOME=/path/to/java11
mvn clean package -DskipTests
```

2. **或者在服务器上编译**：
```bash
# 上传代码到服务器
scp -r backend/ user@server:/path/to/project/
# 在服务器上编译
ssh user@server "cd /path/to/project/backend && mvn clean package -DskipTests"
```

3. **部署**：
```bash
# 使用现有部署脚本
./go.sh
```

## 🔍 验证步骤

### 1. 检查后端启动日志

成功的日志应该显示：
```
使用微信支付公钥模式初始化...
公钥路径: classpath:cert/wechatpay_public_key.pem
公钥ID: PUB_KEY_ID_xxxxxxxxx
微信支付公钥模式配置创建成功
微信支付服务初始化成功
```

### 2. 测试支付功能

运行测试脚本：
```bash
node test-wechat-pay-config.js
```

期望结果：不再出现 "NOT_ENOUGH" 或 "平台证书已过期" 错误。

## 📊 方案对比

| 特性 | 平台证书模式 | 微信支付公钥模式 |
|------|-------------|-----------------|
| 证书过期风险 | 每5年需更新 | 无过期问题 |
| 维护复杂度 | 高 | 低 |
| 官方推荐度 | 逐步淘汰 | 强烈推荐 |
| 稳定性 | 可能中断 | 更稳定 |

## ⚠️ 重要注意事项

1. **公钥ID格式**：必须是 `PUB_KEY_ID_` 开头的完整字符串
2. **文件路径**：确保公钥文件路径正确，文件存在
3. **配置同步**：所有使用该商户号的系统都需要更新
4. **测试验证**：更新后务必进行充分测试

## 🚀 快速执行清单

- [ ] 登录微信支付商户平台
- [ ] 下载微信支付公钥文件和公钥ID
- [ ] 将公钥文件放到 `backend/src/main/resources/cert/`
- [ ] 更新 `application.yml` 配置
- [ ] 更新 `WechatPayConfig.java` 代码
- [ ] 更新 `WechatPayService.java` 代码
- [ ] 重新编译部署
- [ ] 验证支付功能

## 📞 技术支持

如果遇到问题：
1. 检查微信支付商户平台是否支持公钥模式
2. 确认公钥文件格式正确（PEM格式）
3. 验证公钥ID配置无误
4. 查看后端启动日志排查问题

## 🎉 预期效果

完成切换后：
- ✅ 彻底解决平台证书过期问题
- ✅ 微信支付功能恢复正常
- ✅ 系统稳定性显著提升
- ✅ 减少后续维护工作量

---

**总结**：微信支付公钥模式是官方推荐的最佳解决方案，一次配置即可长期使用，无需担心证书过期问题。