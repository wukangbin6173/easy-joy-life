# 首页用户信息展示功能 - 部署指南

## 更新内容

在小程序首页 banner 下方添加了用户信息展示区域，显示：
- 用户微信头像（圆形）
- 用户昵称
- 欢迎语

## 已完成步骤

✅ **1. 代码更新**
- 修改了 `miniprogram/pages/index/index.wxml` - 添加用户信息卡片
- 修改了 `miniprogram/pages/index/index.wxss` - 添加样式
- 修改了 `miniprogram/pages/index/index.js` - 添加用户信息获取逻辑

✅ **2. Git 提交**
- 提交信息: "feat: 在首页banner下添加用户头像和昵称展示"
- 已推送到 GitHub: https://github.com/wukangbin6173/easy-joy-life

## 部署步骤

### 方式一：服务器更新（如果有后端改动）

1. **连接服务器**
   ```bash
   ssh root@121.43.96.127
   # 密码: Easy8286173
   ```

2. **更新代码**
   ```bash
   cd /opt/easy-joy-life
   git pull origin main
   ```

3. **如果 git pull 失败（网络问题）**
   ```bash
   # 配置 git 超时设置
   git config --global http.postBuffer 524288000
   git config --global http.lowSpeedLimit 0
   git config --global http.lowSpeedTime 999999
   
   # 重试
   git pull origin main
   
   # 如果还是失败，重新克隆
   cd /opt
   rm -rf easy-joy-life
   git clone https://github.com/wukangbin6173/easy-joy-life.git
   ```

### 方式二：小程序直接上传（推荐）

**本次更新只涉及小程序前端，无需更新服务器！**

1. **打开微信开发者工具**
   - 打开项目目录中的 `miniprogram` 文件夹

2. **预览测试**
   - 点击"预览"按钮
   - 用手机扫码查看效果
   - 确认用户头像和昵称正常显示

3. **上传代码**
   - 点击"上传"按钮
   - 填写版本号：例如 `1.0.1`
   - 填写项目备注：`添加首页用户信息展示`

4. **提交审核**
   - 登录微信公众平台：https://mp.weixin.qq.com
   - 进入"版本管理"
   - 选择刚上传的版本
   - 点击"提交审核"
   - 填写审核信息

## 功能说明

### 显示逻辑
- 用户登录后，首页会自动显示用户头像和昵称
- 如果用户未登录，该区域会自动隐藏
- 每次进入首页都会刷新用户信息

### 样式特点
- 卡片式设计，带圆角和阴影
- 头像圆形显示，带浅色边框
- 昵称加粗显示
- 欢迎语使用灰色字体

### 数据来源
- 从 `app.globalData.userInfo` 获取用户信息
- 包含 `avatarUrl`（头像）和 `nickName`（昵称）

## 测试要点

1. **登录状态测试**
   - 已登录用户：应显示头像和昵称
   - 未登录用户：不显示该区域

2. **头像显示测试**
   - 正常头像：显示用户微信头像
   - 无头像：显示默认头像

3. **昵称显示测试**
   - 有昵称：显示用户昵称
   - 无昵称：显示"微信用户"

## 注意事项

1. 本次更新只涉及小程序前端，后端无需更新
2. 确保用户已授权获取头像和昵称
3. 如果用户信息不显示，检查 `app.js` 中的登录逻辑

## 访问地址

- 小程序：搜索"易享生活棋牌室"
- 后端 API: https://xx.aieo.cn/api
- 管理后台: https://xx.aieo.cn/admin.html

## 问题排查

如果用户信息不显示：

1. 检查用户是否已登录
2. 检查 `app.globalData.userInfo` 是否有数据
3. 查看控制台日志
4. 确认用户已授权获取信息

---

**部署时间**: 2026-01-28  
**更新人**: Kiro AI Assistant  
**版本**: v1.0.1
