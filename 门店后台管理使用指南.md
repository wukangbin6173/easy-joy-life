# 门店后台管理使用指南

## 访问地址
http://localhost:8080/admin.html

## 新增功能：房间管理按门店筛选

### 功能概述
在房间管理页面新增了门店筛选功能，管理员可以按门店查看和管理房间，提高管理效率。

### 功能特性

#### 1. 门店筛选下拉框
- 位置：房间管理页面顶部，"新增房间"按钮左侧
- 选项：
  - "全部门店"：显示所有门店的房间
  - 各个门店名称：只显示选中门店的房间

#### 2. 实时统计信息
- 显示当前筛选条件下的房间总数
- 显示当前筛选的门店名称
- 例如：
  - "共 13 个房间（显示全部门店）"
  - "共 4 个房间（筛选门店：我们自己的棋牌室(万达店)）"

#### 3. 智能筛选
- 筛选后保持当前页面状态
- 新增、编辑、删除房间后自动保持筛选状态
- 空结果时显示友好提示信息

### 使用方法

#### 查看所有房间
1. 进入"房间管理"标签页
2. 筛选下拉框选择"全部门店"
3. 查看所有门店的房间列表

#### 按门店筛选房间
1. 进入"房间管理"标签页
2. 在筛选下拉框中选择具体门店
3. 系统自动显示该门店的房间
4. 统计信息会实时更新

#### 管理特定门店的房间
1. 选择目标门店进行筛选
2. 在筛选结果中进行房间的增删改操作
3. 操作完成后筛选状态自动保持

### 数据统计

#### 当前系统数据
- **总门店数**: 5个
- **总房间数**: 13个
- **各门店房间分布**:
  - 我们自己的棋牌室(万达店): 4个房间
  - 雀胜棋牌室(中心店): 3个房间
  - 雀胜棋牌室(西单店): 2个房间
  - 雀胜棋牌室(国贸店): 2个房间
  - 雀胜棋牌室(三里屯店): 2个房间

### 技术实现

#### 前端功能
- 门店筛选下拉框
- 实时统计显示
- 筛选状态保持
- 友好的用户界面

#### 后端API
- `GET /api/rooms/admin/all` - 获取所有房间
- `GET /api/rooms/admin/store/{storeId}` - 获取指定门店的房间
- `GET /api/stores/admin/all` - 获取所有门店

#### 数据流程
1. 页面加载时获取所有门店和房间数据
2. 填充门店筛选下拉框
3. 用户选择门店时进行前端筛选
4. 实时更新显示结果和统计信息

### 界面截图说明

#### 筛选前（全部门店）
- 显示："共 13 个房间（显示全部门店）"
- 表格显示所有门店的房间

#### 筛选后（特定门店）
- 显示："共 4 个房间（筛选门店：我们自己的棋牌室(万达店)）"
- 表格只显示该门店的房间

### 注意事项

1. **数据同步**: 筛选功能基于实时数据，确保显示最新状态
2. **状态保持**: 进行房间操作后会保持当前筛选状态
3. **性能优化**: 筛选在前端进行，响应速度快
4. **用户体验**: 提供清晰的统计信息和筛选状态提示

### 后续扩展建议

1. **多条件筛选**: 可添加房间类型、状态等筛选条件
2. **搜索功能**: 支持房间号、房间名称搜索
3. **批量操作**: 支持批量启用/禁用房间
4. **导出功能**: 支持筛选结果导出Excel

### 故障排除

#### 筛选下拉框为空
- 检查门店数据是否正常加载
- 确认后端API `/api/stores/admin/all` 正常

#### 筛选无效果
- 检查浏览器控制台是否有JavaScript错误
- 确认房间数据已正常加载

#### 统计信息不准确
- 刷新页面重新加载数据
- 检查数据库中的房间和门店关联关系

---

**更新时间**: 2026-01-20  
**版本**: v1.1  
**功能状态**: ✅ 已完成并测试通过