# 微信小程序网络配置指南

## 🚨 问题现象
小程序切换到真实API后看不到门店数据，网络请求失败。

## 🔍 问题原因
微信小程序有严格的网络安全限制：
1. 不能访问localhost
2. 需要配置域名白名单（生产环境）
3. 开发环境需要特殊设置

## 🛠️ 解决方案

### 方案1: 微信开发者工具配置 (推荐)

#### 步骤1: 打开项目设置
1. 在微信开发者工具中打开项目
2. 点击右上角的"详情"按钮
3. 进入"本地设置"选项卡

#### 步骤2: 关键配置项
请确保勾选以下选项：
- ✅ **不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书**
- ✅ **启用调试模式**
- ✅ **不校验 Secure Domain**

#### 步骤3: 网络配置
在"项目配置"中：
- 网络超时时间: 60000ms
- 启用ES6转ES5: 勾选
- 启用增强编译: 勾选

### 方案2: 小程序代码配置

#### 当前配置 ✅
```javascript
// miniprogram/app.js
globalData: {
  baseUrl: 'http://192.168.110.28:8080', // 使用IP地址
  mockMode: false // 使用真实API
}
```

#### API测试
后端API已验证可通过IP访问：
```bash
GET http://192.168.110.28:8080/api/stores
Status: 200 OK
```

### 方案3: 网络请求调试

#### 在小程序中添加调试代码
```javascript
// 在 miniprogram/pages/stores/stores.js 的 loadStores 方法中添加
console.log('API请求地址:', app.globalData.baseUrl + '/api/stores');
console.log('mockMode状态:', app.globalData.mockMode);

// 在网络请求的 fail 回调中添加详细日志
fail: (err) => {
  console.error('网络请求详细错误:', err);
  console.error('错误类型:', err.errMsg);
  console.error('状态码:', err.statusCode);
}
```

## 🔧 故障排除步骤

### 步骤1: 验证微信开发者工具设置
1. 确认"不校验合法域名"已勾选
2. 重新编译小程序项目
3. 查看控制台是否有网络请求日志

### 步骤2: 测试网络连通性
```javascript
// 在小程序控制台中测试
wx.request({
  url: 'http://192.168.110.28:8080/api/stores',
  method: 'GET',
  success: (res) => {
    console.log('网络测试成功:', res);
  },
  fail: (err) => {
    console.error('网络测试失败:', err);
  }
});
```

### 步骤3: 检查防火墙设置
确保Windows防火墙允许8080端口访问：
```bash
# 管理员权限运行
netsh advfirewall firewall add rule name="Spring Boot 8080" dir=in action=allow protocol=TCP localport=8080
```

## 🚀 验证方法

### 成功标志
1. **控制台日志**: 看到API请求成功的日志
2. **门店显示**: 小程序显示5个门店
3. **网络面板**: 微信开发者工具网络面板显示成功的请求

### 失败排查
1. **请求失败**: 检查微信开发者工具设置
2. **超时错误**: 检查后端服务状态
3. **跨域错误**: 确认后端CORS配置

## 📋 配置检查清单

### 微信开发者工具 ✅
- [ ] 不校验合法域名: 已勾选
- [ ] 启用调试模式: 已勾选
- [ ] 项目重新编译: 已完成

### 小程序代码 ✅
- [ ] baseUrl使用IP地址: http://192.168.110.28:8080
- [ ] mockMode设置为false: 使用真实API
- [ ] 网络请求错误处理: 已添加日志

### 后端服务 ✅
- [ ] 服务正在运行: 端口8080
- [ ] API可通过IP访问: 已验证
- [ ] CORS配置正确: 已配置

## 🎯 立即行动步骤

1. **打开微信开发者工具**
2. **点击"详情" → "本地设置"**
3. **勾选"不校验合法域名..."**
4. **重新编译项目**
5. **查看控制台网络请求日志**

如果仍有问题，请提供微信开发者工具控制台的错误信息。

---
**配置时间**: 2026年1月20日
**IP地址**: 192.168.110.28:8080
**状态**: 等待微信开发者工具配置验证