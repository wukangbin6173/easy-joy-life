# 钱包功能完善总结

## 修改内容

### 1. 移除自动充值功能 ✅

#### 修改的文件
- `miniprogram/pages/wallet/wallet.wxml` - 移除自动充值UI
- `miniprogram/pages/wallet/wallet.js` - 移除相关代码和数据
- `miniprogram/pages/wallet/wallet.wxss` - 样式保持不变

#### 具体改动
**移除的UI元素**:
```xml
<!-- 已删除 -->
<view class="feature-item" bindtap="goToAutoRecharge">
  <view class="feature-icon">
    <image src="/images/auto-recharge-icon.png"></image>
  </view>
  <view class="feature-info">
    <text class="feature-title">自动充值</text>
    <text class="feature-desc">余额不足时自动充值</text>
  </view>
  <view class="feature-switch">
    <switch checked="{{wallet.autoRecharge}}" bindchange="toggleAutoRecharge" />
  </view>
</view>
```

**移除的代码**:
- `wallet.autoRecharge` 数据字段
- `goToAutoRecharge()` 方法
- `toggleAutoRecharge()` 方法

---

### 2. 新增银行卡管理功能 ✅

#### 创建的页面

##### 2.1 银行卡列表页面
**文件**: `miniprogram/pages/bank-card/`
- `bank-card.wxml` - 页面结构
- `bank-card.wxss` - 页面样式
- `bank-card.js` - 页面逻辑
- `bank-card.json` - 页面配置

**功能特性**:
- ✅ 显示所有银行卡列表
- ✅ 卡号脱敏显示（前4后4）
- ✅ 显示银行名称和卡类型
- ✅ 标记默认银行卡
- ✅ 设置默认银行卡
- ✅ 删除银行卡（带确认）
- ✅ 空状态提示
- ✅ 加载状态
- ✅ 温馨提示

**API接口**:
```javascript
// 获取银行卡列表
GET /api/user/bank-cards/{userId}

// 设置默认银行卡
POST /api/user/bank-cards/set-default
{
  userId: number,
  cardId: number
}

// 删除银行卡
DELETE /api/user/bank-cards/{cardId}
```

##### 2.2 添加银行卡页面
**文件**: `miniprogram/pages/add-bank-card/`
- `add-bank-card.wxml` - 页面结构
- `add-bank-card.wxss` - 页面样式
- `add-bank-card.js` - 页面逻辑
- `add-bank-card.json` - 页面配置

**功能特性**:
- ✅ 输入持卡人姓名
- ✅ 输入银行卡号（自动识别银行）
- ✅ 输入预留手机号
- ✅ 发送验证码（60秒倒计时）
- ✅ 输入验证码
- ✅ 设置为默认卡选项
- ✅ 表单验证
- ✅ 安全提示

**API接口**:
```javascript
// 识别银行
POST /api/bank/identify
{
  cardNo: string (前6位)
}

// 发送验证码
POST /api/sms/send-code
{
  phone: string,
  type: 'BIND_CARD'
}

// 添加银行卡
POST /api/user/bank-cards
{
  userId: number,
  holderName: string,
  cardNo: string,
  phone: string,
  code: string,
  isDefault: boolean
}
```

---

### 3. 新增支付密码功能 ✅

#### 创建的页面
**文件**: `miniprogram/pages/pay-password/`
- `pay-password.wxml` - 页面结构
- `pay-password.wxss` - 页面样式
- `pay-password.js` - 页面逻辑
- `pay-password.json` - 页面配置

**功能特性**:
- ✅ 检测是否已设置密码
- ✅ 设置密码（首次）
- ✅ 修改密码（需输入原密码）
- ✅ 6位数字密码输入
- ✅ 密码点状显示
- ✅ 密码确认
- ✅ 简单密码检测
- ✅ 忘记密码提示
- ✅ 安全提示

**密码输入UI**:
- 6个密码框
- 点状显示已输入位数
- 自动聚焦
- 数字键盘

**密码安全检查**:
- 必须6位数字
- 不允许简单密码（123456、111111等）
- 不允许连续数字（123456、654321等）
- 两次输入必须一致

**API接口**:
```javascript
// 检查是否已设置密码
GET /api/user/has-pay-password/{userId}

// 设置/修改支付密码
POST /api/user/pay-password
{
  userId: number,
  oldPassword: string | null,  // 修改时需要
  newPassword: string
}
```

---

## 页面导航关系

```
钱包页面 (wallet)
├── 银行卡管理 (bank-card)
│   └── 添加银行卡 (add-bank-card)
└── 支付密码 (pay-password)
```

---

## UI设计特点

### 1. 银行卡列表
- 渐变色卡片背景
- 卡号脱敏显示
- 默认标记
- 操作按钮（设为默认、删除）

### 2. 添加银行卡
- 清晰的表单布局
- 自动识别银行
- 验证码倒计时
- 实时表单验证

### 3. 支付密码
- 6位密码点状输入
- 视觉反馈清晰
- 安全提示明显
- 简单密码拦截

---

## 数据结构

### 银行卡数据
```javascript
{
  id: number,
  userId: number,
  holderName: string,
  cardNo: string,
  cardNoMasked: string,  // 前端生成
  bankName: string,
  bankCode: string,
  cardType: string,  // '储蓄卡' | '信用卡'
  phone: string,
  isDefault: boolean,
  status: string,
  createdTime: string
}
```

### 支付密码数据
```javascript
{
  userId: number,
  hasPassword: boolean,
  oldPassword: string,  // 仅修改时
  newPassword: string
}
```

---

## 安全措施

### 1. 银行卡安全
- ✅ 卡号脱敏显示
- ✅ 手机验证码验证
- ✅ 仅支持本人银行卡
- ✅ 数据加密传输
- ✅ 删除需二次确认

### 2. 支付密码安全
- ✅ 6位数字密码
- ✅ 简单密码检测
- ✅ 密码加密存储
- ✅ 修改需原密码
- ✅ 忘记密码引导

---

## 后端需要实现的API

### 1. 银行卡相关
```java
// BankCardController.java

@GetMapping("/api/user/bank-cards/{userId}")
public ResponseEntity<Map<String, Object>> getBankCards(@PathVariable Long userId)

@PostMapping("/api/user/bank-cards")
public ResponseEntity<Map<String, Object>> addBankCard(@RequestBody BankCardRequest request)

@PostMapping("/api/user/bank-cards/set-default")
public ResponseEntity<Map<String, Object>> setDefaultCard(@RequestBody SetDefaultRequest request)

@DeleteMapping("/api/user/bank-cards/{cardId}")
public ResponseEntity<Map<String, Object>> deleteBankCard(@PathVariable Long cardId)

@PostMapping("/api/bank/identify")
public ResponseEntity<Map<String, Object>> identifyBank(@RequestBody IdentifyRequest request)
```

### 2. 支付密码相关
```java
// PayPasswordController.java

@GetMapping("/api/user/has-pay-password/{userId}")
public ResponseEntity<Map<String, Object>> hasPayPassword(@PathVariable Long userId)

@PostMapping("/api/user/pay-password")
public ResponseEntity<Map<String, Object>> setPayPassword(@RequestBody PayPasswordRequest request)
```

### 3. 短信验证码
```java
// SmsController.java

@PostMapping("/api/sms/send-code")
public ResponseEntity<Map<String, Object>> sendCode(@RequestBody SmsRequest request)
```

---

## 数据库表设计

### 1. 银行卡表 (user_bank_cards)
```sql
CREATE TABLE user_bank_cards (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '用户ID',
  holder_name VARCHAR(50) NOT NULL COMMENT '持卡人姓名',
  card_no VARCHAR(32) NOT NULL COMMENT '银行卡号（加密）',
  bank_name VARCHAR(50) NOT NULL COMMENT '银行名称',
  bank_code VARCHAR(20) NOT NULL COMMENT '银行代码',
  card_type VARCHAR(20) NOT NULL COMMENT '卡类型',
  phone VARCHAR(20) NOT NULL COMMENT '预留手机号',
  is_default BOOLEAN DEFAULT FALSE COMMENT '是否默认',
  status VARCHAR(20) DEFAULT 'ACTIVE' COMMENT '状态',
  created_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_user_id (user_id),
  INDEX idx_card_no (card_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户银行卡表';
```

### 2. 支付密码表 (user_pay_passwords)
```sql
CREATE TABLE user_pay_passwords (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL UNIQUE COMMENT '用户ID',
  password VARCHAR(255) NOT NULL COMMENT '支付密码（加密）',
  salt VARCHAR(64) NOT NULL COMMENT '盐值',
  created_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户支付密码表';
```

---

## 测试清单

### 银行卡管理
- [ ] 查看银行卡列表
- [ ] 添加银行卡
- [ ] 自动识别银行
- [ ] 发送验证码
- [ ] 验证码倒计时
- [ ] 设置默认银行卡
- [ ] 删除银行卡
- [ ] 空状态显示

### 支付密码
- [ ] 首次设置密码
- [ ] 修改密码
- [ ] 简单密码拦截
- [ ] 密码不一致提示
- [ ] 原密码错误提示
- [ ] 忘记密码引导

---

## 使用流程

### 添加银行卡流程
1. 进入钱包页面
2. 点击"银行卡管理"
3. 点击"添加银行卡"
4. 填写持卡人姓名
5. 输入银行卡号（自动识别银行）
6. 输入预留手机号
7. 点击"获取验证码"
8. 输入验证码
9. 选择是否设为默认
10. 点击"确认添加"

### 设置支付密码流程
1. 进入钱包页面
2. 点击"支付密码"
3. 输入6位数字密码
4. 确认密码
5. 点击"确认"

### 修改支付密码流程
1. 进入钱包页面
2. 点击"支付密码"
3. 输入原密码
4. 输入新密码
5. 确认新密码
6. 点击"确认"

---

## 注意事项

### 1. 安全性
- 银行卡号必须加密存储
- 支付密码必须加盐哈希
- 验证码有效期控制
- 操作日志记录

### 2. 用户体验
- 表单验证及时反馈
- 操作结果明确提示
- 加载状态清晰显示
- 错误信息友好提示

### 3. 业务规则
- 每个用户最多绑定5张银行卡
- 必须有一张默认银行卡
- 删除默认卡需先设置其他卡为默认
- 支付密码错误次数限制

---

## 下一步工作

### 1. 后端开发
- [ ] 实现银行卡相关API
- [ ] 实现支付密码API
- [ ] 实现短信验证码API
- [ ] 创建数据库表
- [ ] 编写单元测试

### 2. 前端优化
- [ ] 添加银行logo图片
- [ ] 优化加载动画
- [ ] 添加骨架屏
- [ ] 完善错误处理

### 3. 测试
- [ ] 功能测试
- [ ] 安全测试
- [ ] 性能测试
- [ ] 兼容性测试

---

## 总结

✅ **已完成**:
1. 移除自动充值功能
2. 创建银行卡管理页面（列表、添加）
3. 创建支付密码页面（设置、修改）
4. 完善UI设计和交互
5. 添加安全措施和验证

⏳ **待完成**:
1. 后端API实现
2. 数据库表创建
3. 功能测试
4. 真机测试

**预计工作量**:
- 前端开发: ✅ 已完成
- 后端开发: ⏳ 2-3天
- 测试: ⏳ 1天
- 上线: ⏳ 待定
