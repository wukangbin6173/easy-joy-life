# 获取阿里云短信配置信息指南

## 你已有的信息 ✅
- 模板CODE：`SMS_501750341`
- 短信服务已开通

## 还需要获取的信息

### 1️⃣ 获取短信签名

**步骤：**
1. 登录阿里云控制台：https://dysms.console.aliyun.com
2. 点击左侧菜单"国内消息" → "签名管理"
3. 查看已审核通过的签名
4. 复制"签名名称"（不包括【】符号）

**示例：**
- 如果短信显示为：【易享生活】您的验证码为：123456
- 那么签名名称就是：`易享生活`

**如果还没有签名：**
- 点击"添加签名"
- 填写签名信息并提交审核
- 审核时间：2-24小时

---

### 2️⃣ 获取 AccessKey ID 和 AccessKey Secret

**重要提示：** AccessKey Secret 只在创建时显示一次，如果忘记了需要重新创建！

#### 方法一：查看已有的 AccessKey（如果之前创建过）

**步骤：**
1. 登录阿里云控制台
2. 点击右上角头像
3. 选择"AccessKey管理"
4. 或直接访问：https://ram.console.aliyun.com/manage/ak

**查看 AccessKey ID：**
- 在列表中可以看到 AccessKey ID（24位字符）
- 格式类似：`LTAI5txxxxxxxxxxxxxxxxxx`

**查看 AccessKey Secret：**
- ⚠️ Secret 不会显示！
- 如果忘记了，只能删除旧的，创建新的

#### 方法二：创建新的 AccessKey

**步骤：**
1. 访问：https://ram.console.aliyun.com/manage/ak
2. 系统会提示安全建议，选择：
   - "继续使用AccessKey"（快速方式）
   - 或"使用子用户AccessKey"（安全方式，推荐生产环境）

**如果选择"继续使用AccessKey"：**
1. 验证手机号
2. 点击"创建AccessKey"
3. **立即保存！** 会显示：
   ```
   AccessKeyId: LTAI5txxxxxxxxxxxxxxxxxx
   AccessKeySecret: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
4. ⚠️ **Secret只显示一次，务必保存！**

**如果选择"使用子用户AccessKey"（推荐）：**
1. 点击"创建用户"
2. 填写信息：
   - 登录名称：`sms-user`
   - 显示名称：`短信服务用户`
   - 勾选"编程访问"
3. 点击"确定"
4. **立即保存 AccessKey！**
5. 返回用户列表，点击用户名
6. 点击"添加权限"
7. 搜索"短信"，选择"AliyunDysmsFullAccess"
8. 点击"确定"

---

### 3️⃣ 验证模板信息

**步骤：**
1. 登录短信服务控制台：https://dysms.console.aliyun.com
2. 点击"国内消息" → "模板管理"
3. 找到模板CODE：`SMS_501750341`
4. 查看模板内容和变量

**需要确认：**
- 模板状态：必须是"审核通过"
- 模板内容：例如 `您的验证码为：${code}，该验证码5分钟内有效。`
- 变量名称：例如 `code`

---

## 配置信息汇总表

请填写以下信息：

```
阿里云短信配置信息
==================

1. AccessKey ID: _________________________________
   (24位字符，格式：LTAI5txxxxxxxxxxxxxxxxxx)

2. AccessKey Secret: _________________________________
   (30位字符，只显示一次，务必保存！)

3. 短信签名: _________________________________
   (例如：易享生活)

4. 模板CODE: SMS_501750341 ✅
   (已有)

5. 模板变量: _________________________________
   (例如：code)
```

---

## 快速获取步骤

### 最快方式（5分钟）

1. **获取签名**（1分钟）
   ```
   https://dysms.console.aliyun.com
   → 国内消息 → 签名管理 → 复制签名名称
   ```

2. **创建AccessKey**（2分钟）
   ```
   https://ram.console.aliyun.com/manage/ak
   → 继续使用AccessKey → 验证手机 → 创建 → 保存！
   ```

3. **验证模板**（1分钟）
   ```
   https://dysms.console.aliyun.com
   → 国内消息 → 模板管理 → 查看 SMS_501750341
   ```

4. **告诉我配置信息**（1分钟）
   - 我会帮你配置到项目中

---

## 安全提示 ⚠️

1. **AccessKey Secret 只显示一次**
   - 创建后立即保存到安全的地方
   - 不要分享给他人
   - 不要提交到Git仓库

2. **如果泄露了怎么办？**
   - 立即删除该AccessKey
   - 创建新的AccessKey
   - 更新项目配置

3. **生产环境建议**
   - 使用RAM子用户AccessKey
   - 只授予短信服务权限
   - 定期轮换AccessKey

---

## 下一步

获取到以上信息后，告诉我：
1. AccessKey ID
2. AccessKey Secret
3. 短信签名

我会帮你：
1. 配置到 application.yml
2. 创建短信服务类
3. 修改相关Controller
4. 编译部署
5. 测试真实短信发送

**准备好了吗？** 🚀
