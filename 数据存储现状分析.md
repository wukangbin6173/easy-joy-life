# 数据存储现状分析

## 当前数据存储情况

### ❌ 目前测试数据**不在**MySQL数据库中
当前所有的门店、房间、用户等测试数据都是硬编码在前端JavaScript文件中的**模拟数据**，并没有存储在MySQL数据库里。

## 数据存储位置分析

### 1. 前端模拟数据 (当前使用)
**位置**: 硬编码在JavaScript文件中
- `miniprogram/pages/stores/stores.js` - 门店列表数据
- `miniprogram/pages/store-detail/store-detail.js` - 门店详情数据
- `miniprogram/pages/index/index.js` - 首页门店数据
- `miniprogram/utils/mockData.js` - 通用模拟数据

**特点**:
- ✅ 开发调试方便，无需数据库连接
- ✅ 数据结构灵活，可随时修改
- ❌ 数据无法持久化
- ❌ 无法实现真实的增删改查
- ❌ 多用户数据隔离问题

### 2. MySQL数据库 (已准备但未使用)
**位置**: `backend/src/main/resources/schema.sql`
- 已定义完整的数据库表结构
- 包含门店、房间、订单、用户等所有表
- 已插入少量初始测试数据

**表结构**:
- `stores` - 门店表 (2条测试数据)
- `rooms` - 房间表 (6条测试数据)  
- `users` - 用户表
- `orders` - 订单表
- `payments` - 支付记录表
- `devices` - 设备表
- `admins` - 管理员表
- `system_config` - 系统配置表

## 当前系统架构

### 前端 (微信小程序)
- **数据模式**: `mockMode: true` (模拟数据模式)
- **API地址**: `http://localhost:8080/api` (已配置但未使用)
- **数据来源**: 本地JavaScript对象

### 后端 (Spring Boot)
- **状态**: 已启动运行 (端口8080)
- **数据库**: H2/MySQL (已配置)
- **API接口**: 仅有测试接口，无业务接口
- **现有接口**:
  - `GET /test/hello` - 测试接口
  - `GET /test/health` - 健康检查

## 问题分析

### 1. 缺少业务API接口
后端只有测试接口，没有门店、房间、订单等业务API接口：
- 无 `/api/stores` 门店接口
- 无 `/api/rooms` 房间接口  
- 无 `/api/orders` 订单接口
- 无 `/api/users` 用户接口

### 2. 前端未调用后端API
小程序代码中没有 `wx.request` 调用后端API，全部使用本地模拟数据。

### 3. 数据不同步
- 前端模拟数据: 5个门店 (万达店、中心店、西单店、国贸店、三里屯店)
- 数据库测试数据: 2个门店 (总店、分店)

## 解决方案建议

### 方案1: 继续使用模拟数据 (推荐用于演示)
**适用场景**: 快速演示、原型展示
- ✅ 无需额外开发
- ✅ 数据完整丰富
- ✅ 功能演示完整

### 方案2: 迁移到真实数据库 (推荐用于生产)
**适用场景**: 正式部署、多用户使用

**需要完成的工作**:
1. **后端开发** (约2-3小时):
   - 创建门店Controller和Service
   - 创建房间Controller和Service  
   - 创建订单Controller和Service
   - 实现CRUD接口

2. **数据迁移** (约1小时):
   - 将前端模拟数据导入数据库
   - 更新图片路径字段
   - 同步数据结构

3. **前端改造** (约1-2小时):
   - 修改数据获取方式
   - 添加wx.request调用
   - 处理异步加载和错误

## 当前推荐

**对于演示和测试**: 继续使用当前的模拟数据模式，功能完整且稳定。

**对于生产部署**: 需要完成后端API开发和前端改造，实现真正的数据库驱动。

## 文件清单

### 模拟数据文件
- `miniprogram/app.js` - 全局配置 (mockMode: true)
- `miniprogram/pages/stores/stores.js` - 门店列表模拟数据
- `miniprogram/pages/store-detail/store-detail.js` - 门店详情模拟数据
- `miniprogram/pages/index/index.js` - 首页门店模拟数据
- `miniprogram/utils/mockData.js` - 通用模拟数据

### 数据库相关文件
- `backend/src/main/resources/schema.sql` - 数据库表结构和初始数据
- `backend/src/main/java/com/qiupai/controller/TestController.java` - 测试接口

**当前状态**: 模拟数据模式 ✅ 运行正常
**数据库状态**: 已准备 ⏳ 待开发API接口