# 微信支付平台证书配置说明

## 🎯 重要说明

**微信支付平台证书路径已从配置中移除**，原因如下：

### ✅ 推荐方案：自动证书管理

使用 `RSAAutoCertificateConfig` 的优势：

1. **自动下载**: SDK自动从微信服务器下载平台证书
2. **自动更新**: 证书过期前自动更新
3. **无需手动管理**: 不需要手动下载和配置证书文件
4. **更安全**: 避免证书过期导致的服务中断

### 🔧 当前配置

```yaml
wechat:
  pay:
    app-id: wxe6ea8185cf046cfb  # 小程序AppID
    mch-id: 1554487931  # 商户号
    private-key-path: classpath:cert/apiclient_key.pem  # 商户私钥
    merchant-serial-number: 68EFFAD8D54020146A1372141B6EEBA1B52D6B64  # 商户证书序列号
    api-v3-key: idNmbaZ0EPgwbvYbJDGp2EGlwQDezU6d  # API v3密钥
    notify-url: https://xx.aieo.cn/api/payment/wechat/notify  # 回调地址
```

### 📁 证书文件结构

```
backend/src/main/resources/cert/
├── apiclient_key.pem      # 商户私钥（必需）
├── apiclient_cert.pem     # 商户证书（可选，用于验证）
└── [平台证书自动管理]      # SDK自动处理，无需手动配置
```

### 🔍 工作原理

1. **初始化时**: SDK使用商户私钥和API v3密钥
2. **首次请求**: 自动调用微信API获取平台证书
3. **证书缓存**: 将平台证书缓存在内存中
4. **自动更新**: 定期检查并更新证书

### 🚨 如果需要手动管理证书

如果因为特殊需求需要手动管理平台证书，可以：

1. **使用下载工具**: 运行 `download-wechat-platform-cert.py`
2. **手动配置**: 添加 `wechat-pay-certificate-path` 配置项
3. **使用RSAConfig**: 替换 `RSAAutoCertificateConfig`

### ⚙️ 手动配置示例（不推荐）

```yaml
wechat:
  pay:
    # ... 其他配置
    wechat-pay-certificate-path: classpath:cert/wechatpay_certificate.pem
```

```java
// 使用手动证书配置
this.config = new RSAConfig.Builder()
    .merchantId(wechatPayConfig.getMchId())
    .privateKeyFromPath(wechatPayConfig.getPrivateKeyPath())
    .merchantSerialNumber(wechatPayConfig.getMerchantSerialNumber())
    .wechatPayCertificatesFromPath(wechatPayConfig.getWechatPayCertificatePath())
    .build();
```

### 🎉 配置完成检查

确认以下配置项已正确设置：

- [x] 小程序AppID: `wxe6ea8185cf046cfb`
- [x] 商户号: `1554487931`
- [x] 商户私钥文件: `apiclient_key.pem`
- [x] 商户证书序列号: `68EFFAD8D54020146A1372141B6EEBA1B52D6B64`
- [x] API v3密钥: `idNmbaZ0EPgwbvYbJDGp2EGlwQDezU6d`
- [x] 回调地址: `https://xx.aieo.cn/api/payment/wechat/notify`
- [x] 自动证书管理: 已启用

### 🔧 验证配置

启动应用后，查看日志确认：

```
✅ 微信支付服务初始化成功 - 使用自动证书管理
✅ 小程序AppID: wxe6ea8185cf046cfb
✅ 商户号: 1554487931
✅ API v3密钥: idNm****zU6d
✅ 商户证书序列号: 68EFFAD8****2D6B64
```

### 📞 常见问题

**Q: 为什么不需要平台证书路径？**
A: 使用RSAAutoCertificateConfig时，SDK会自动管理平台证书。

**Q: 如何确认平台证书获取成功？**
A: 查看应用日志，SDK会输出证书下载和验证信息。

**Q: 平台证书过期怎么办？**
A: SDK会自动检测并更新过期证书，无需人工干预。

---

**总结**: 当前配置使用自动证书管理，无需手动配置微信支付平台证书路径。这是最佳实践，既安全又便于维护。