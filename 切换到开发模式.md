# 切换到开发模式（临时方案）

## 问题说明

由于签名 "东阳市耕琅坊包装材料" 可能还在审核中，导致短信发送失败。

在等待签名审核通过期间，可以暂时切换回开发模式继续测试其他功能。

## 快速切换步骤

### 方法一：修改代码（推荐）

**1. 修改 SmsController.java**
```java
// 将 DEV_MODE 改为 true
private static final boolean DEV_MODE = true;
```

**2. 修改 BankCardController.java**
```java
// 将 DEV_MODE 改为 true
private static final boolean DEV_MODE = true;
```

**3. 重新编译部署**
```bash
# 本地编译
cd backend
.\mvnw.cmd clean package -DskipTests

# 上传到服务器
scp target/easy-joy-life-system-1.0.0.jar root@121.43.96.127:/opt/easy-joy-life/backend/target/

# 重启服务
ssh root@121.43.96.127 "systemctl restart easy-joy-life-backend"
```

### 方法二：使用 go.bat（最快）

```bash
.\go.bat 1
```

选择提交消息方式，然后等待自动部署。

## 开发模式特点

### 优点
- ✅ 无需真实短信服务
- ✅ 使用固定验证码：`888888`
- ✅ 前端会弹窗显示验证码
- ✅ 可以继续测试其他功能

### 缺点
- ❌ 不是真实短信
- ❌ 无法测试真实短信发送
- ❌ 验证码固定，安全性低

## 开发模式下的测试流程

1. 打开小程序
2. 进入"添加银行卡"
3. 填写手机号
4. 点击"发送验证码"
5. **前端会弹窗显示验证码：888888**
6. 输入验证码：`888888`
7. 完成银行卡添加

## 何时切换回生产模式

当满足以下条件时，切换回生产模式：

1. ✅ 签名审核通过
2. ✅ 模板审核通过
3. ✅ AccessKey 配置正确
4. ✅ 账户余额充足

**切换方法：**
```java
// SmsController.java 和 BankCardController.java
private static final boolean DEV_MODE = false;
```

然后重新编译部署。

## 当前建议

### 立即行动
1. **检查签名状态**
   - 登录：https://dysms.console.aliyun.com
   - 查看：国内消息 → 签名管理
   - 确认：签名是否审核通过

2. **如果签名还在审核中**
   - 暂时切换到开发模式
   - 继续测试其他功能
   - 等待签名审核通过（2-24小时）

3. **如果签名已审核通过**
   - 检查签名名称是否完全一致
   - 再次测试短信发送
   - 查看日志确认错误原因

### 联系支持
如果签名审核超过24小时，可以联系阿里云客服：
- **电话**：95187
- **说明**：需要加急审核短信签名

## 总结

**当前状态：**
- ✅ 服务器配置已更新
- ✅ 服务已重启
- ⏳ 等待确认签名审核状态

**下一步：**
1. 检查阿里云控制台签名状态
2. 如果还在审核中，切换到开发模式
3. 如果已审核通过，再次测试并查看日志

**请告诉我签名的审核状态！** 📱
