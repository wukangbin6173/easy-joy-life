# 支付宝支付集成指南

## 🎯 功能概述

已成功集成支付宝支付充值功能，包括：

- ✅ 支付宝SDK集成
- ✅ 充值订单管理
- ✅ 支付回调处理
- ✅ 钱包余额管理
- ✅ 交易记录追踪
- ✅ 小程序支付界面

## 📋 已实现功能

### 后端功能
1. **支付订单管理** - `PaymentOrder` 实体类
2. **用户钱包系统** - `UserWallet` 实体类
3. **交易记录** - `WalletTransaction` 实体类
4. **支付宝服务** - `AlipayService` 支付处理
5. **支付控制器** - `PaymentController` API接口
6. **数据库表结构** - 完整的支付相关表

### 前端功能
1. **充值页面** - 支持支付宝/微信支付选择
2. **支付webview** - 支付宝H5支付页面
3. **钱包页面** - 显示真实余额数据
4. **订单状态轮询** - 自动检查支付结果

## 🔧 配置说明

### 1. 支付宝开发者配置

需要在支付宝开放平台配置以下信息：

```yaml
# application.yml
alipay:
  app-id: 2021000122671431  # 替换为你的应用ID
  private-key: [你的商户私钥]  # RSA2私钥
  public-key: [支付宝公钥]    # 支付宝RSA2公钥
  gateway-url: https://openapi.alipaydev.com/gateway.do  # 沙箱网关
  return-url: https://xx.aieo.cn/payment/return
  notify-url: https://xx.aieo.cn/api/payment/alipay/notify
  sandbox: true  # 生产环境改为false
```

### 2. 获取支付宝密钥

1. **登录支付宝开放平台**
   - 访问：https://open.alipay.com/
   - 创建应用并获取AppID

2. **生成RSA密钥对**
   ```bash
   # 生成私钥
   openssl genrsa -out rsa_private_key.pem 2048
   
   # 生成公钥
   openssl rsa -in rsa_private_key.pem -pubout -out rsa_public_key.pem
   ```

3. **配置应用公钥**
   - 在支付宝开放平台上传你的公钥
   - 获取支付宝公钥用于验签

### 3. 沙箱环境测试

支付宝提供沙箱环境用于测试：

- **沙箱网关**: https://openapi.alipaydev.com/gateway.do
- **沙箱账号**: 在开放平台获取测试买家账号
- **测试金额**: 沙箱环境支持任意金额测试

## 🚀 API接口说明

### 创建充值订单
```http
POST /api/payment/recharge/create
Content-Type: application/json

{
  "userId": 1,
  "amount": 100.00,
  "paymentMethod": "ALIPAY"
}
```

### 支付宝支付
```http
POST /api/payment/alipay/pay
Content-Type: application/json

{
  "orderNo": "PAY20260123001"
}
```

### 查询订单状态
```http
GET /api/payment/order/{orderNo}
```

### 获取用户钱包
```http
GET /api/payment/wallet/{userId}
```

### 获取交易记录
```http
GET /api/payment/transactions/{userId}
```

## 💡 使用流程

### 用户充值流程
1. 用户选择充值金额和支付方式
2. 系统创建充值订单
3. 调用支付宝支付接口
4. 跳转到支付宝支付页面
5. 用户完成支付
6. 支付宝异步回调通知
7. 系统更新订单状态和用户余额
8. 记录交易流水

### 小程序支付流程
1. 用户在充值页面选择支付宝
2. 创建订单并获取支付表单
3. 使用webview打开支付宝支付页面
4. 轮询订单状态检查支付结果
5. 支付成功后返回钱包页面

## 🔒 安全措施

### 1. 签名验证
- 所有支付宝回调都进行RSA2签名验证
- 防止恶意请求和数据篡改

### 2. 订单防重
- 订单号唯一性约束
- 订单状态检查防止重复处理

### 3. 金额校验
- 回调金额与订单金额对比
- 防止金额篡改攻击

### 4. 超时处理
- 订单30分钟自动过期
- 定时任务清理过期订单

## 📊 数据库表结构

### payment_orders (支付订单表)
- `order_no` - 系统订单号
- `trade_no` - 支付宝交易号
- `amount` - 支付金额
- `status` - 订单状态
- `notify_status` - 回调状态

### user_wallets (用户钱包表)
- `balance` - 账户余额
- `frozen_amount` - 冻结金额
- `total_recharge` - 累计充值
- `total_consume` - 累计消费

### wallet_transactions (交易记录表)
- `transaction_no` - 交易流水号
- `transaction_type` - 交易类型
- `amount` - 交易金额
- `balance_before/after` - 交易前后余额

## 🛠️ 部署注意事项

### 1. 生产环境配置
```yaml
alipay:
  gateway-url: https://openapi.alipay.com/gateway.do  # 生产网关
  sandbox: false  # 关闭沙箱模式
```

### 2. 回调地址配置
- 确保回调地址可以被支付宝访问
- 使用HTTPS协议
- 配置正确的域名和端口

### 3. 数据库优化
- 为订单号、用户ID等字段添加索引
- 定期清理过期订单数据
- 备份重要交易数据

## 🔍 测试验证

### 1. 沙箱测试
- 使用沙箱环境测试完整支付流程
- 验证回调处理逻辑
- 测试异常情况处理

### 2. 压力测试
- 并发充值测试
- 大金额充值测试
- 网络异常恢复测试

### 3. 安全测试
- 签名验证测试
- 重放攻击测试
- 金额篡改测试

## 📞 技术支持

### 开发文档
- [支付宝开放平台](https://open.alipay.com/)
- [手机网站支付文档](https://opendocs.alipay.com/open/203/105285)
- [异步通知说明](https://opendocs.alipay.com/open/203/105286)

### 常见问题
1. **签名验证失败** - 检查密钥配置和编码格式
2. **回调接收不到** - 检查网络和域名配置
3. **订单状态异常** - 检查业务逻辑和数据库事务

---

## ✅ 部署完成清单

- [x] 后端支付服务开发完成
- [x] 数据库表结构创建完成
- [x] 支付宝SDK集成完成
- [x] 小程序支付页面完成
- [x] 订单状态轮询完成
- [x] 钱包余额同步完成
- [ ] 支付宝密钥配置（需要实际申请）
- [ ] 生产环境回调地址配置
- [ ] 支付流程完整测试

**当前状态**: 开发完成，等待支付宝账号配置和测试验证 🎉