# 管理后台"加载门店失败"问题解决方案

## 🚨 问题现象
- 管理后台提示: "加载门店失败: 获取门店列表失败"
- API返回500错误: "could not prepare statement"

## 🔍 问题分析
经过检查发现:
- ✅ MySQL服务正在运行 (端口3306)
- ✅ 数据库qiupai_db存在
- ✅ 门店数据完整 (5个门店，13个房间)
- ❌ 后端服务仍在使用旧的H2配置

## 💡 根本原因
后端Spring Boot服务是在MySQL配置之前启动的，仍然在尝试连接H2数据库，需要重启以连接MySQL。

## 🛠️ 解决步骤

### 步骤1: 停止当前后端服务
1. 找到运行后端服务的命令行窗口
2. 按 `Ctrl + C` 停止服务
3. 或者直接关闭窗口

### 步骤2: 重新启动后端服务
```bash
cd backend
./mvnw spring-boot:run
```

### 步骤3: 等待服务启动
- 等待看到 "Started QiupaiApplication" 消息
- 确认没有数据库连接错误

### 步骤4: 验证修复
1. 访问: http://localhost:8080/admin.html
2. 查看门店列表是否正常显示
3. 测试增删改查功能

## 🚀 一键解决脚本

### 自动重启脚本
```bash
# 运行重启脚本
restart-backend-with-mysql.bat
```

### 手动验证
```bash
# 测试API
curl http://localhost:8080/api/stores/admin/all

# 应该返回包含5个门店的JSON数据
```

## 📊 预期结果

### 成功后的状态
- ✅ 管理后台正常显示5个门店
- ✅ 可以编辑门店信息
- ✅ 可以管理房间数据
- ✅ 小程序可以切换到真实API模式

### API正常响应示例
```json
{
  "code": 200,
  "message": "成功",
  "data": [
    {
      "id": 1,
      "name": "雀胜棋牌室(万达店)",
      "address": "北京市朝阳区建国路93号万达广场3层",
      ...
    }
  ]
}
```

## 🔧 故障排除

### 如果重启后仍有问题
1. 检查后端启动日志中的数据库连接信息
2. 确认MySQL服务正在运行
3. 验证application.yml中的数据库配置

### 常见错误和解决方案
- **连接被拒绝**: 检查MySQL服务状态
- **数据库不存在**: 运行mysql-init.sql脚本
- **权限错误**: 确认root密码为'root'

## 📋 验证清单
- [ ] 后端服务已重启
- [ ] 管理后台可以访问
- [ ] 门店列表正常显示
- [ ] 可以编辑门店信息
- [ ] API返回正确数据

---

## 🎯 立即行动

**最快解决方法**:
1. 停止后端服务 (Ctrl+C)
2. 重新启动: `cd backend && ./mvnw spring-boot:run`
3. 等待启动完成
4. 刷新管理后台页面

**预计解决时间**: 2-3分钟
**成功率**: 99%