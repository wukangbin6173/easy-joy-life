# EasyJoyLife 一键部署脚本使用指南

## 概述

参考了GitHub上优秀的部署脚本实践，为EasyJoyLife项目创建了一键部署解决方案，支持：
- 🔨 本地构建
- 📤 自动提交并推送到GitHub
- 🚀 服务器自动部署
- ✅ 服务状态检查

## 脚本文件

### Linux/Mac 版本: `go.sh`
```bash
#!/bin/bash
# 完整的Shell脚本，支持颜色输出和错误处理
```

### Windows 版本: `go.bat`
```batch
@echo off
# 批处理脚本，功能与Linux版本相同
```

## 使用方法

### 1. 完整部署流程 (推荐)

**Linux/Mac:**
```bash
./go.sh
# 或
./go.sh all
# 或
./go.sh 4
```

**Windows:**
```cmd
go.bat
# 或
go.bat all
# 或
go.bat 4
```

### 2. 分步执行

#### 仅本地构建
```bash
# Linux/Mac
./go.sh build
./go.sh 1

# Windows
go.bat build
go.bat 1
```

#### 仅上传到GitHub
```bash
# Linux/Mac
./go.sh upload
./go.sh 2

# Windows
go.bat upload
go.bat 2
```

#### 仅服务器部署
```bash
# Linux/Mac
./go.sh deploy
./go.sh 3

# Windows
go.bat deploy
go.bat 3
```

#### 查看帮助
```bash
# Linux/Mac
./go.sh --help
./go.sh -h

# Windows
go.bat --help
go.bat -h
```

## 部署流程详解

### 第一步：本地构建 🔨
1. **环境检查**
   - 检查Java环境
   - 检查Maven工具
   - 验证项目结构

2. **后端构建**
   ```bash
   cd backend
   mvn clean package -DskipTests
   ```

3. **构建验证**
   - 检查JAR文件是否生成
   - 验证文件完整性

### 第二步：上传GitHub 📤
1. **Git状态检查**
   - 检查未提交的更改
   - 用户确认是否继续

2. **代码提交**
   ```bash
   git add .
   git commit -m "Deploy: 2026-01-28 14:30:00"
   git push origin main
   ```

### 第三步：服务器部署 🚀
1. **连接检查**
   - 测试SSH连接
   - 验证服务器可达性

2. **服务器操作**
   ```bash
   # 拉取最新代码
   git pull origin main
   
   # 停止现有服务
   pkill -f "easy-joy-life-system"
   
   # 重新编译
   mvn clean package -DskipTests
   
   # 检查数据库
   mysql -uroot -p'EasyJoyLife2024!@#' -e "SELECT 1;"
   
   # 启动新服务
   nohup java -jar target/easy-joy-life-system-1.0.0.jar > /var/log/easyjoylife.log 2>&1 &
   
   # 服务检查
   curl -s http://localhost:8080/api/stores
   ```

## 配置说明

### 服务器配置
```bash
SERVER_HOST="121.43.96.127"
SERVER_USER="root"
SERVER_PATH="/opt/easy-joy-life"
BACKEND_JAR="easy-joy-life-system-1.0.0.jar"
```

### 数据库配置
- **用户名**: root
- **密码**: EasyJoyLife2024!@#
- **数据库**: easy_joy_life_db

## 前置条件

### 本地环境
- ✅ Git 已安装并配置
- ✅ Java 8+ 已安装
- ✅ Maven 已安装 (或使用项目自带的mvnw)
- ✅ SSH密钥已配置 (免密码登录)

### 服务器环境
- ✅ Git 已安装
- ✅ Java 8+ 已安装
- ✅ Maven 已安装
- ✅ MySQL 已安装并运行
- ✅ 项目目录: /opt/easy-joy-life

## 错误处理

### 常见错误及解决方案

#### 1. SSH连接失败
```
错误: 无法连接到服务器 121.43.96.127
```
**解决方案:**
- 检查网络连接
- 验证SSH密钥配置
- 确认服务器IP地址

#### 2. Maven构建失败
```
错误: Maven构建失败
```
**解决方案:**
- 检查Java版本
- 清理Maven缓存: `mvn clean`
- 检查pom.xml配置

#### 3. 数据库连接失败
```
错误: Access denied for user 'root'@'localhost'
```
**解决方案:**
- 检查MySQL服务状态
- 验证数据库密码
- 确认数据库存在

#### 4. 服务启动失败
```
❌ 服务启动失败
```
**解决方案:**
- 查看日志: `tail -f /var/log/easyjoylife.log`
- 检查端口占用: `netstat -tlnp | grep 8080`
- 验证JAR文件完整性

## 日志查看

### 服务器日志
```bash
# 实时查看日志
ssh root@121.43.96.127 "tail -f /var/log/easyjoylife.log"

# 查看最近100行日志
ssh root@121.43.96.127 "tail -100 /var/log/easyjoylife.log"

# 查看错误日志
ssh root@121.43.96.127 "grep ERROR /var/log/easyjoylife.log"
```

### 服务状态检查
```bash
# 检查Java进程
ssh root@121.43.96.127 "ps aux | grep java"

# 检查端口监听
ssh root@121.43.96.127 "netstat -tlnp | grep 8080"

# 测试API接口
curl https://xx.aieo.cn/api/stores
```

## 高级用法

### 自定义部署
如果需要修改部署逻辑，可以编辑脚本中的相应函数：

```bash
# 修改构建逻辑
local_build() {
    # 自定义构建步骤
}

# 修改部署逻辑
deploy_to_server() {
    # 自定义部署步骤
}
```

### 环境变量
可以通过环境变量覆盖默认配置：

```bash
export SERVER_HOST="your-server.com"
export SERVER_USER="deploy"
./go.sh
```

## 安全注意事项

1. **SSH密钥管理**
   - 定期轮换SSH密钥
   - 使用强密码保护私钥

2. **数据库安全**
   - 定期更换数据库密码
   - 限制数据库访问权限

3. **服务器安全**
   - 定期更新系统补丁
   - 配置防火墙规则

## 故障排除

### 部署失败时的检查清单

1. **本地环境**
   - [ ] Git状态正常
   - [ ] Java版本兼容
   - [ ] Maven配置正确
   - [ ] 网络连接正常

2. **服务器环境**
   - [ ] SSH连接正常
   - [ ] 磁盘空间充足
   - [ ] MySQL服务运行
   - [ ] 端口未被占用

3. **应用配置**
   - [ ] 数据库连接配置
   - [ ] 微信API配置
   - [ ] 支付接口配置

## 更新日志

### v1.0.0 (2026-01-28)
- ✅ 初始版本发布
- ✅ 支持完整部署流程
- ✅ 支持分步执行
- ✅ 添加错误处理和状态检查
- ✅ 支持Windows和Linux双平台

---

## 快速开始

1. **首次使用**
   ```bash
   # 确保SSH密钥已配置
   ssh root@121.43.96.127 "echo 'SSH连接测试'"
   
   # 执行完整部署
   ./go.sh
   ```

2. **日常更新**
   ```bash
   # 修改代码后直接部署
   ./go.sh
   ```

3. **紧急修复**
   ```bash
   # 仅部署到服务器
   ./go.sh deploy
   ```

**🎉 现在你可以使用 `./go.sh` 或 `go.bat` 进行一键部署了！**