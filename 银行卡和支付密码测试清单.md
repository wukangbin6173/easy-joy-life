# 银行卡和支付密码功能测试清单

## 部署前检查

- [ ] MySQL服务已启动
- [ ] 数据库 `easy_joy_life_db` 存在
- [ ] 后端服务可以正常启动
- [ ] 小程序可以正常编译

---

## 数据库部署

### 1. 执行SQL脚本
```bash
# 方法1：使用命令行
mysql -u root -p easy_joy_life_db < backend/src/main/resources/db-init-bank-card-paypassword.sql

# 方法2：使用MySQL客户端
USE easy_joy_life_db;
SOURCE backend/src/main/resources/db-init-bank-card-paypassword.sql;
```

### 2. 验证表创建
```sql
-- 查看表
SHOW TABLES LIKE 'user_%';

-- 查看表结构
DESC user_bank_cards;
DESC user_pay_passwords;

-- 查看表数据
SELECT COUNT(*) FROM user_bank_cards;
SELECT COUNT(*) FROM user_pay_passwords;
```

**预期结果**：
- [ ] `user_bank_cards` 表已创建
- [ ] `user_pay_passwords` 表已创建
- [ ] 表结构正确
- [ ] 索引已创建

---

## 后端服务测试

### 1. 重启服务
```bash
cd backend
mvn spring-boot:run
```

### 2. 检查日志
```bash
tail -f logs/spring.log
```

**预期结果**：
- [ ] 服务启动成功
- [ ] 无错误日志
- [ ] 端口8080已监听

### 3. API测试

#### 支付密码API
```bash
# 检查是否设置密码
curl http://localhost:8080/api/user/has-pay-password/1

# 设置密码
curl -X POST http://localhost:8080/api/user/pay-password \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"newPassword":"123456"}'
```

**预期结果**：
- [ ] 返回 `{"success":true}`
- [ ] 无错误信息

#### 银行卡API
```bash
# 获取银行卡列表
curl http://localhost:8080/api/user/bank-cards/1

# 添加银行卡
curl -X POST http://localhost:8080/api/user/bank-cards \
  -H "Content-Type: application/json" \
  -d '{"userId":1,"holderName":"张三","cardNo":"6222021234567890","phone":"13800138000","isDefault":true}'
```

**预期结果**：
- [ ] 返回 `{"success":true}`
- [ ] 卡号已脱敏
- [ ] 银行已识别

---

## 小程序功能测试

### 1. 编译小程序
- [ ] 在微信开发者工具中点击"编译"
- [ ] 无编译错误
- [ ] 无控制台错误

### 2. 支付密码功能

#### 首次设置密码
1. [ ] 进入"我的" -> "钱包"
2. [ ] 点击"支付密码"
3. [ ] 页面显示"设置支付密码"
4. [ ] 输入6位数字密码
5. [ ] 确认密码
6. [ ] 点击"确认"
7. [ ] 显示"设置成功"
8. [ ] 自动返回钱包页面

#### 修改密码
1. [ ] 再次进入"支付密码"
2. [ ] 页面显示"修改支付密码"
3. [ ] 需要输入原密码
4. [ ] 输入新密码
5. [ ] 确认新密码
6. [ ] 点击"确认"
7. [ ] 显示"修改成功"

#### 错误处理
1. [ ] 原密码错误时提示
2. [ ] 两次密码不一致时提示
3. [ ] 简单密码（123456）被拦截
4. [ ] 连续数字（123456）被拦截

### 3. 银行卡管理功能

#### 查看银行卡列表
1. [ ] 进入"我的" -> "钱包"
2. [ ] 点击"银行卡管理"
3. [ ] 显示银行卡列表（或空状态）
4. [ ] 卡号已脱敏显示
5. [ ] 默认卡有标记

#### 添加银行卡
1. [ ] 点击"添加银行卡"
2. [ ] 输入持卡人姓名
3. [ ] 输入银行卡号
4. [ ] 自动识别银行名称
5. [ ] 输入手机号
6. [ ] 点击"获取验证码"
7. [ ] 倒计时60秒
8. [ ] 输入验证码
9. [ ] 选择"设为默认"
10. [ ] 点击"确认添加"
11. [ ] 显示"添加成功"
12. [ ] 返回银行卡列表

#### 设置默认银行卡
1. [ ] 点击非默认卡的"设为默认"
2. [ ] 显示"设置成功"
3. [ ] 默认标记已更新
4. [ ] 原默认卡标记已移除

#### 删除银行卡
1. [ ] 点击"删除"按钮
2. [ ] 显示确认对话框
3. [ ] 点击"确定"
4. [ ] 显示"删除成功"
5. [ ] 银行卡已从列表移除
6. [ ] 如果删除默认卡，其他卡自动成为默认

#### 错误处理
1. [ ] 卡号格式错误时提示
2. [ ] 手机号格式错误时提示
3. [ ] 验证码错误时提示
4. [ ] 重复卡号时提示
5. [ ] 超过5张卡时提示

---

## 边界情况测试

### 支付密码
- [ ] 密码长度不足6位
- [ ] 密码包含非数字字符
- [ ] 连续错误5次后锁定
- [ ] 锁定期间无法验证
- [ ] 锁定30分钟后自动解锁

### 银行卡
- [ ] 添加第6张卡时拒绝
- [ ] 删除唯一的卡
- [ ] 删除默认卡
- [ ] 重复添加同一张卡
- [ ] 卡号长度异常

---

## 性能测试

### 响应时间
- [ ] 获取银行卡列表 < 500ms
- [ ] 添加银行卡 < 1s
- [ ] 设置密码 < 500ms
- [ ] 验证密码 < 300ms

### 并发测试
- [ ] 同时添加多张银行卡
- [ ] 同时设置默认卡
- [ ] 同时验证密码

---

## 安全测试

### 数据加密
- [ ] 数据库中卡号已加密
- [ ] 数据库中密码已加密
- [ ] 前端不显示完整卡号
- [ ] 密码输入为点状显示

### 权限控制
- [ ] 用户只能查看自己的银行卡
- [ ] 用户只能操作自己的银行卡
- [ ] 用户只能修改自己的密码

### 日志记录
- [ ] 敏感操作有日志
- [ ] 日志中卡号已脱敏
- [ ] 日志中无明文密码

---

## 兼容性测试

### 设备测试
- [ ] iOS真机
- [ ] Android真机
- [ ] 微信开发者工具

### 网络环境
- [ ] WiFi
- [ ] 4G/5G
- [ ] 弱网环境

---

## 回归测试

### 原有功能
- [ ] 钱包余额显示正常
- [ ] 充值功能正常
- [ ] 交易记录正常
- [ ] 其他功能未受影响

---

## 问题记录

| 序号 | 问题描述 | 严重程度 | 状态 | 备注 |
|------|---------|---------|------|------|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

---

## 测试结论

### 通过标准
- [ ] 所有核心功能正常
- [ ] 无严重bug
- [ ] 性能符合要求
- [ ] 安全性符合要求

### 测试结果
- 测试日期：__________
- 测试人员：__________
- 测试结果：⏳ 待测试 / ✅ 通过 / ❌ 不通过

### 备注
```
记录测试过程中的问题和建议
```

---

## 上线检查清单

- [ ] 所有测试通过
- [ ] 代码已提交
- [ ] 数据库脚本已备份
- [ ] 生产环境配置已更新
- [ ] 回滚方案已准备
- [ ] 监控已配置
- [ ] 文档已更新

---

## 快速命令

### 数据库
```sql
-- 查看银行卡
SELECT * FROM user_bank_cards WHERE user_id = 1;

-- 查看支付密码
SELECT user_id, error_count, locked_until FROM user_pay_passwords WHERE user_id = 1;

-- 重置错误次数
UPDATE user_pay_passwords SET error_count = 0, locked_until = NULL WHERE user_id = 1;

-- 删除测试数据
DELETE FROM user_bank_cards WHERE user_id = 1;
DELETE FROM user_pay_passwords WHERE user_id = 1;
```

### 后端
```bash
# 查看日志
tail -f backend/logs/spring.log | grep -i "bank\|password"

# 重启服务
cd backend
mvn spring-boot:run
```

### 小程序
```javascript
// 控制台查看用户ID
getApp().globalData.userId

// 清除缓存
wx.clearStorageSync()
```
