# 易享生活棋牌室小程序 - 新人开发指南

> 欢迎加入团队！本文档将帮助你快速了解项目的全貌，包括技术架构、已完成功能、开发环境配置等。

## 📋 目录

1. [项目概述](#项目概述)
2. [技术架构](#技术架构)
3. [项目结构](#项目结构)
4. [核心功能](#核心功能)
5. [开发环境配置](#开发环境配置)
6. [数据库设计](#数据库设计)
7. [API 接口文档](#api-接口文档)
8. [支付系统](#支付系统)
9. [部署架构](#部署架构)
10. [开发规范](#开发规范)
11. [常见问题](#常见问题)

---

## 项目概述

### 项目简介
易享生活棋牌室小程序是一个**无人值守智能棋牌室预订系统**，用户可以通过微信小程序：
- 浏览门店和房间信息
- 在线预订房间
- 钱包充值和消费
- 智能门锁开门
- 查看订单和交易记录

### 业务模式
- **预付费模式**：用户先充值到钱包，消费时从钱包扣款
- **无人值守**：通过智能门锁实现自助开门
- **按时计费**：根据使用时长自动计费

### 项目状态
✅ **已上线运行**
- 服务器：121.43.96.127
- 域名：https://xx.aieo.cn
- 小程序：已发布（AppID: wx9f4a33e5f2b31a6d）

---

## 技术架构

### 前端技术栈
- **微信小程序原生开发**
  - WXML + WXSS + JavaScript
  - 微信小程序 API
  - 微信支付 JSAPI

### 后端技术栈
- **Spring Boot 3.x**
  - Spring Data JPA
  - Spring Security
  - Spring Web
- **MySQL 8.0** - 数据存储
- **Redis** - 缓存（已配置但未启用）
- **Druid** - 数据库连接池

### 第三方服务
- **微信支付** - 商户号：1671050768
- **支付宝支付** - 沙箱环境（AppID: 2021006128663181）
- **微信小程序登录** - 静默登录已实现

### 部署架构
```
用户 → Nginx (443/80) → 后端服务 (8080) → MySQL (3306)
         ↓
      SSL证书
      静态文件
```

---

## 项目结构

### 整体目录结构
```
EasyJoyLife/
├── backend/                    # 后端 Spring Boot 项目
│   ├── src/main/java/         # Java 源代码
│   │   └── com/easyjoylife/
│   │       ├── controller/    # 控制器层
│   │       ├── service/       # 业务逻辑层
│   │       ├── repository/    # 数据访问层
│   │       ├── entity/        # 实体类
│   │       └── utils/         # 工具类
│   ├── src/main/resources/    # 配置文件
│   │   ├── application.yml    # 开发环境配置
│   │   ├── application-prod.yml # 生产环境配置
│   │   ├── cert/              # 微信支付证书
│   │   └── *.sql              # 数据库初始化脚本
│   └── pom.xml                # Maven 依赖配置
│
├── miniprogram/               # 微信小程序前端
│   ├── pages/                 # 页面目录
│   │   ├── index/            # 首页
│   │   ├── stores/           # 门店列表
│   │   ├── store-detail/     # 门店详情
│   │   ├── room-detail/      # 房间详情
│   │   ├── booking/          # 预订页面
│   │   ├── orders/           # 订单列表
│   │   ├── wallet/           # 钱包页面
│   │   ├── recharge/         # 充值页面
│   │   ├── transaction-records/ # 交易记录
│   │   ├── profile/          # 个人中心
│   │   └── unlock/           # 开门页面
│   ├── utils/                # 工具类
│   │   ├── api.js           # API 封装
│   │   └── config.js        # 配置文件
│   ├── images/              # 图片资源
│   ├── app.js               # 小程序入口
│   ├── app.json             # 小程序配置
│   └── app.wxss             # 全局样式
│
├── deploy/                   # 部署脚本和配置
│   ├── deploy.sh            # 一键部署脚本
│   ├── nginx.conf           # Nginx 配置
│   ├── .env.production      # 生产环境变量
│   └── *.sh                 # 各种运维脚本
│
├── docs/                    # 项目文档
└── *.md                     # 各种说明文档
```

### 关键文件说明

#### 后端关键文件
- `PaymentController.java` - 支付相关接口（充值、回调、查询）
- `WechatPayService.java` - 微信支付服务（已完成）
- `AlipayService.java` - 支付宝支付服务（已配置）
- `PaymentService.java` - 支付业务逻辑（充值、交易记录）
- `UserController.java` - 用户相关接口
- `StoreController.java` - 门店相关接口

#### 小程序关键文件
- `utils/config.js` - API 基础配置（域名、AppID）
- `utils/api.js` - API 请求封装
- `app.js` - 全局状态管理（用户信息、登录状态）

---

## 核心功能

### ✅ 已完成功能

#### 1. 用户系统
- **微信静默登录**：用户打开小程序自动登录
- **用户信息管理**：昵称、头像、手机号
- **用户信息自动获取**：使用微信新版组件自动获取用户信息

#### 2. 门店管理
- **门店列表展示**：支持地图定位、距离计算
- **门店详情**：营业时间、设施信息、联系方式
- **房间列表**：按门店展示可用房间
- **房间详情**：房间类型、价格、设施、容量

#### 3. 钱包系统
- **钱包余额查询**：实时显示余额、冻结金额、累计充值/消费
- **充值功能**：
  - 支持微信支付（已上线）
  - 支持支付宝支付（已配置，未启用）
  - 充值金额：0.1元、0.2元、0.3元、0.5元、1元、2元、5元、10元、20元、50元、100元
- **交易记录**：
  - 支持按类型筛选（全部/收入/支出）
  - 按日期分组显示
  - 显示本月收入/支出统计
  - 实时从数据库获取数据

#### 4. 支付系统
- **微信支付**：
  - ✅ 充值功能已完成
  - ✅ 支付回调处理已完成
  - ✅ 订单状态查询已完成
  - ✅ 使用微信支付公钥模式（避免证书过期问题）
  - 商户号：1671050768
  - 商户证书序列号：1A24641C1CD3C0034643DEB9467B246F57A1AF37
- **支付宝支付**：
  - ✅ 配置已完成（沙箱环境）
  - ⏳ 未启用（需要正式环境配置）

#### 5. 订单系统
- **订单列表**：显示用户所有订单
- **订单详情**：订单状态、金额、时间等信息
- **订单状态**：待支付、已支付、已取消、已退款

#### 6. 智能门锁（预留）
- 开门页面已创建
- 蓝牙开门功能待对接硬件

### 🚧 待开发功能

1. **房间预订功能**：完整的预订流程
2. **订单支付**：房间预订后的支付
3. **智能门锁对接**：蓝牙开门功能
4. **退款功能**：订单取消后的退款处理
5. **优惠券系统**：优惠券发放和使用
6. **会员系统**：VIP 会员权益

---

## 开发环境配置

### 前置要求
- **JDK 17+**
- **Maven 3.8+**
- **MySQL 8.0+**
- **Node.js 14+**（可选，用于测试脚本）
- **微信开发者工具**

### 后端环境配置

#### 1. 克隆项目
```bash
git clone <repository-url>
cd EasyJoyLife
```

#### 2. 配置数据库
```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE IF NOT EXISTS easy_joy_life_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 导入初始数据
USE easy_joy_life_db;
SOURCE backend/src/main/resources/mysql-init.sql;
```

#### 3. 修改配置文件
编辑 `backend/src/main/resources/application.yml`：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/easy_joy_life_db?...
    username: root
    password: your_password  # 修改为你的密码
```

#### 4. 启动后端服务
```bash
cd backend
mvn clean install
mvn spring-boot:run
```

服务启动后访问：http://localhost:8080

#### 5. 验证服务
```bash
# 测试门店接口
curl http://localhost:8080/api/stores

# 测试钱包接口
curl http://localhost:8080/api/payment/wallet/1
```

### 小程序环境配置

#### 1. 安装微信开发者工具
下载地址：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

#### 2. 导入项目
- 打开微信开发者工具
- 选择"导入项目"
- 选择 `miniprogram` 目录
- AppID：wx9f4a33e5f2b31a6d（测试号）

#### 3. 配置后端地址
编辑 `miniprogram/utils/config.js`：
```javascript
const config = {
  // 开发环境使用本地地址
  baseUrl: 'http://localhost:8080',
  apiBaseUrl: 'http://localhost:8080/api',
  
  // 生产环境
  // baseUrl: 'https://xx.aieo.cn',
  // apiBaseUrl: 'https://xx.aieo.cn/api',
};
```

#### 4. 配置合法域名（开发阶段）
在微信开发者工具中：
- 点击右上角"详情"
- 勾选"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"

#### 5. 编译运行
点击"编译"按钮，小程序即可运行

### 测试脚本使用

项目提供了多个测试脚本，用于快速测试 API：

```bash
# 测试钱包 API
node test-wallet-api.js

# 测试交易记录
node test-transaction-records.js

# 测试充值流程
node test-01-yuan-recharge.js
```

---

## 数据库设计

### 核心数据表

#### 1. users - 用户表
```sql
字段说明：
- id: 用户ID（主键）
- openid: 微信 openid（唯一）
- unionid: 微信 unionid
- nickname: 用户昵称
- avatar: 头像 URL
- gender: 性别（0-未知，1-男，2-女）
- phone: 手机号
- status: 用户状态（ACTIVE-正常，DISABLED-禁用）
- last_login_time: 最后登录时间
- created_time: 创建时间
- updated_time: 更新时间
```

#### 2. stores - 门店表
```sql
字段说明：
- id: 门店ID（主键）
- name: 门店名称
- address: 门店地址
- longitude: 经度
- latitude: 纬度
- phone: 联系电话
- description: 门店描述
- image: 门店图片
- business_hours: 营业时间
- facilities: 设施信息
- status: 状态（1-启用，0-禁用）
- created_at: 创建时间
- updated_at: 更新时间
```

#### 3. rooms - 房间表
```sql
字段说明：
- id: 房间ID（主键）
- store_id: 所属门店ID（外键）
- room_no: 房间号
- name: 房间名称
- type: 房间类型
- capacity: 容量
- area: 面积（平方米）
- hourly_rate: 每小时价格
- image: 房间图片
- facilities: 设施信息
- status: 状态（1-启用，0-禁用）
- created_at: 创建时间
- updated_at: 更新时间
```

#### 4. user_wallets - 用户钱包表
```sql
字段说明：
- id: 钱包ID（主键）
- user_id: 用户ID（唯一，外键）
- balance: 账户余额
- frozen_amount: 冻结金额
- total_recharge: 累计充值金额
- total_consume: 累计消费金额
- status: 钱包状态（ACTIVE-正常，FROZEN-冻结，DISABLED-禁用）
- created_time: 创建时间
- updated_time: 更新时间
```

#### 5. payment_orders - 支付订单表
```sql
字段说明：
- id: 订单ID（主键）
- order_no: 订单号（唯一）
- trade_no: 第三方支付订单号
- user_id: 用户ID
- payment_type: 支付类型（RECHARGE-充值，BOOKING-预订）
- payment_method: 支付方式（ALIPAY-支付宝，WECHAT-微信）
- amount: 支付金额
- subject: 订单标题
- body: 订单描述
- status: 订单状态（PENDING-待支付，PAID-已支付，CANCELLED-已取消，REFUNDED-已退款）
- paid_time: 支付时间
- expire_time: 过期时间
- notify_status: 回调通知状态
- notify_count: 回调通知次数
- extra_data: 扩展信息（JSON）
- created_time: 创建时间
- updated_time: 更新时间
```

#### 6. wallet_transactions - 钱包交易记录表
```sql
字段说明：
- id: 交易ID（主键）
- transaction_no: 交易流水号（唯一）
- user_id: 用户ID
- order_no: 关联订单号
- transaction_type: 交易类型（RECHARGE-充值，CONSUME-消费，REFUND-退款，FREEZE-冻结，UNFREEZE-解冻）
- amount: 交易金额（正数为收入，负数为支出）
- balance_before: 交易前余额
- balance_after: 交易后余额
- description: 交易描述
- status: 交易状态（SUCCESS-成功，FAILED-失败，PENDING-处理中）
- created_time: 创建时间
```

### 表关系图
```
users (用户)
  ├─→ user_wallets (钱包) [1:1]
  ├─→ payment_orders (支付订单) [1:N]
  └─→ wallet_transactions (交易记录) [1:N]

stores (门店)
  └─→ rooms (房间) [1:N]

payment_orders (支付订单)
  └─→ wallet_transactions (交易记录) [1:1] (通过 order_no 关联)
```

### 数据库连接信息

**生产环境：**
- 主机：121.43.96.127
- 端口：3306
- 数据库：easy_joy_life_db
- 用户：root
- 密码：EasyJoyLife2024!@#

**开发环境：**
- 主机：localhost
- 端口：3306
- 数据库：easy_joy_life_db
- 用户：root
- 密码：root（根据本地配置）

---

## API 接口文档

### 基础信息
- **Base URL（生产）**: https://xx.aieo.cn/api
- **Base URL（开发）**: http://localhost:8080/api
- **Content-Type**: application/json

### 用户相关接口

#### 1. 微信登录
```http
POST /users/wechat/login
Content-Type: application/json

Request:
{
  "code": "微信登录code"
}

Response:
{
  "success": true,
  "openid": "用户openid",
  "userId": 1,
  "isNewUser": false
}
```

#### 2. 更新用户信息
```http
PUT /users/{userId}
Content-Type: application/json

Request:
{
  "nickname": "用户昵称",
  "avatar": "头像URL",
  "gender": 1,
  "phone": "13800138000"
}

Response:
{
  "success": true,
  "user": { ... }
}
```

### 门店相关接口

#### 1. 获取门店列表
```http
GET /stores

Response:
{
  "success": true,
  "stores": [
    {
      "id": 1,
      "name": "易享生活棋牌室(万达店)",
      "address": "北京市朝阳区建国路93号万达广场3层",
      "longitude": 116.447587,
      "latitude": 39.937075,
      "phone": "010-12345678",
      "businessHours": "09:00-02:00",
      "image": "/images/store-logo-1.jpg",
      "facilities": "智能门锁,中央空调,免费WiFi,茶水服务,停车位"
    }
  ]
}
```

#### 2. 获取门店详情
```http
GET /stores/{storeId}

Response:
{
  "success": true,
  "store": { ... },
  "rooms": [ ... ]
}
```

### 钱包相关接口

#### 1. 获取钱包信息
```http
GET /payment/wallet/{userId}

Response:
{
  "success": true,
  "wallet": {
    "id": 1,
    "userId": 1,
    "balance": 168.50,
    "frozenAmount": 0.00,
    "totalRecharge": 200.00,
    "totalConsume": 31.50,
    "status": "ACTIVE",
    "availableBalance": 168.50
  }
}
```

#### 2. 获取交易记录
```http
GET /payment/transactions/{userId}

Response:
{
  "success": true,
  "transactions": [
    {
      "id": 1,
      "transactionNo": "TXN202601230001",
      "userId": 1,
      "orderNo": "PAY202601230001",
      "transactionType": "RECHARGE",
      "amount": 100.00,
      "balanceBefore": 68.50,
      "balanceAfter": 168.50,
      "description": "钱包充值",
      "status": "SUCCESS",
      "createdTime": "2026-01-23T10:15:30"
    }
  ]
}
```

### 支付相关接口

#### 1. 创建充值订单
```http
POST /payment/recharge
Content-Type: application/json

Request:
{
  "userId": 1,
  "amount": 100.00,
  "paymentMethod": "WECHAT"
}

Response:
{
  "success": true,
  "orderNo": "PAY202601230001",
  "amount": 100.00,
  "paymentParams": {
    "timeStamp": "1234567890",
    "nonceStr": "abc123",
    "package": "prepay_id=wx123456",
    "signType": "RSA",
    "paySign": "signature"
  }
}
```

#### 2. 查询订单状态
```http
GET /payment/order/{orderNo}

Response:
{
  "success": true,
  "order": {
    "orderNo": "PAY202601230001",
    "status": "PAID",
    "amount": 100.00,
    "paidTime": "2026-01-23T10:16:00"
  }
}
```

#### 3. 微信支付回调（内部接口）
```http
POST /payment/wechat/notify
Content-Type: application/json

# 由微信支付服务器调用，无需手动调用
```

#### 4. 支付宝支付回调（内部接口）
```http
POST /payment/alipay/notify
Content-Type: application/x-www-form-urlencoded

# 由支付宝服务器调用，无需手动调用
```

### 订单相关接口

#### 1. 获取用户订单列表
```http
GET /orders/user/{userId}

Response:
{
  "success": true,
  "orders": [ ... ]
}
```

---

## 支付系统

### 微信支付配置

#### 商户信息
- **AppID**: wx9f4a33e5f2b31a6d
- **商户号**: 1671050768
- **商户名称**: 上海信辉联智信息咨询有限公司
- **商户证书序列号**: 1A24641C1CD3C0034643DEB9467B246F57A1AF37
- **API v3密钥**: SAgZHyBzjNwaqUWtp6shLinmuCMYPfXd

#### 证书文件位置
```
backend/src/main/resources/cert/
├── apiclient_key.pem          # 商户私钥
├── apiclient_cert.pem         # 商户证书（未使用）
└── wechatpay_public_key.pem  # 微信支付公钥
```

#### 回调地址
- **异步回调**: https://xx.aieo.cn/api/payment/wechat/notify
- **回调格式**: JSON
- **验签方式**: 使用微信支付公钥验签（避免证书过期问题）

#### 支付流程
```
1. 小程序调用 POST /api/payment/recharge
   ↓
2. 后端创建支付订单（payment_orders 表）
   ↓
3. 调用微信支付统一下单 API
   ↓
4. 返回支付参数给小程序
   ↓
5. 小程序调用 wx.requestPayment() 拉起支付
   ↓
6. 用户完成支付
   ↓
7. 微信支付服务器回调 /api/payment/wechat/notify
   ↓
8. 后端验证签名、更新订单状态
   ↓
9. 钱包充值（user_wallets 表）
   ↓
10. 创建交易记录（wallet_transactions 表）
```

#### 重要说明
- ✅ 使用微信支付公钥模式，避免平台证书过期问题
- ✅ 回调处理支持幂等性，可重复接收相同回调
- ✅ 订单状态机制完善，防止重复充值
- ⚠️ 微信支付公钥 ID: PUB_KEY_ID_0115544879312026012300191784001402

### 支付宝支付配置

#### 商户信息（沙箱环境）
- **AppID**: 2021006128663181
- **网关**: https://openapi.alipaydev.com/gateway.do
- **签名方式**: RSA2
- **字符集**: UTF-8

#### 回调地址
- **异步回调**: https://xx.aieo.cn/api/payment/alipay/notify
- **同步回调**: https://xx.aieo.cn/payment/return
- **回调格式**: Form 表单

#### 状态说明
- ✅ 配置已完成
- ⏳ 沙箱环境，未启用
- 📝 需要正式环境配置后才能使用

### 充值金额配置

小程序支持的充值金额（单位：元）：
```javascript
[0.1, 0.2, 0.3, 0.5, 1, 2, 5, 10, 20, 50, 100]
```

**说明**：
- 0.1、0.2、0.3 元用于测试
- 1-100 元为正式充值金额
- 可在 `miniprogram/pages/recharge/recharge.js` 中修改

### 支付测试

#### 微信支付测试
```bash
# 使用测试脚本
node test-01-yuan-recharge.js

# 或手动测试
curl -X POST https://xx.aieo.cn/api/payment/recharge \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "amount": 0.1,
    "paymentMethod": "WECHAT"
  }'
```

#### 查看支付日志
```bash
# 服务器日志
ssh root@121.43.96.127
tail -f /opt/easy-joy-life/logs/application.log | grep -i payment
```

---

## 部署架构

### 服务器信息
- **IP**: 121.43.96.127
- **域名**: xx.aieo.cn
- **操作系统**: Ubuntu 22.04
- **SSH 端口**: 22
- **SSH 用户**: root

### 服务部署

#### 1. Nginx（反向代理 + SSL）
- **端口**: 80 (HTTP), 443 (HTTPS)
- **配置文件**: /etc/nginx/sites-enabled/easy-joy-life
- **SSL 证书**: Let's Encrypt 自动续期
- **功能**:
  - HTTPS 强制跳转
  - 反向代理到后端服务
  - 静态文件服务

#### 2. 后端服务（Spring Boot）
- **端口**: 8080
- **运行方式**: systemd 服务
- **服务名**: easy-joy-life-backend
- **日志位置**: /opt/easy-joy-life/logs/application.log
- **启动命令**:
  ```bash
  systemctl start easy-joy-life-backend
  systemctl status easy-joy-life-backend
  systemctl restart easy-joy-life-backend
  ```

#### 3. MySQL 数据库
- **端口**: 3306
- **数据库**: easy_joy_life_db
- **监听地址**: 0.0.0.0（允许远程连接）
- **配置文件**: /etc/mysql/mysql.conf.d/mysqld.cnf

#### 4. Redis（已配置未启用）
- **端口**: 6379
- **状态**: 已安装但未使用

### 部署目录结构
```
/opt/easy-joy-life/
├── backend/
│   └── target/
│       └── easy-joy-life-system-1.0.0.jar
├── logs/
│   └── application.log
└── deploy/
    ├── .env.production
    └── *.sh
```

### 部署流程

#### 方式一：使用部署脚本（推荐）
```bash
# 1. 本地提交代码
git add .
git commit -m "your message"
git push origin main

# 2. 服务器上拉取代码
ssh root@121.43.96.127
cd /opt/easy-joy-life
git pull origin main

# 3. 编译后端
cd backend
mvn clean package -DskipTests

# 4. 重启服务
systemctl restart easy-joy-life-backend

# 5. 查看日志
tail -f /opt/easy-joy-life/logs/application.log
```

#### 方式二：使用快速更新脚本
```bash
# 服务器上执行
cd /opt/easy-joy-life/deploy
./update-backend-only.sh
```

### 服务管理命令

#### 查看服务状态
```bash
# 检查所有服务
cd /opt/easy-joy-life/deploy
./check-all-services.sh

# 或手动检查
systemctl status easy-joy-life-backend
systemctl status nginx
systemctl status mysql
```

#### 查看日志
```bash
# 后端日志
tail -f /opt/easy-joy-life/logs/application.log

# Nginx 日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# MySQL 日志
tail -f /var/log/mysql/error.log
```

#### 重启服务
```bash
# 重启后端
systemctl restart easy-joy-life-backend

# 重启 Nginx
systemctl restart nginx

# 重启 MySQL
systemctl restart mysql
```

### 域名和 SSL 证书

#### 域名配置
- **主域名**: xx.aieo.cn
- **DNS 解析**: 指向 121.43.96.127
- **备案状态**: 已备案

#### SSL 证书
- **证书类型**: Let's Encrypt 免费证书
- **自动续期**: 已配置 certbot 自动续期
- **证书位置**: /etc/letsencrypt/live/xx.aieo.cn/
- **有效期**: 90 天（自动续期）

#### 手动续期证书
```bash
certbot renew
systemctl reload nginx
```

### 防火墙配置
```bash
# 开放的端口
- 22 (SSH)
- 80 (HTTP)
- 443 (HTTPS)
- 3306 (MySQL - 远程访问)

# 查看防火墙状态
ufw status
```

---

## 开发规范

### 代码规范

#### Java 后端
- **命名规范**:
  - 类名：大驼峰（PascalCase）
  - 方法名：小驼峰（camelCase）
  - 常量：全大写下划线分隔（UPPER_SNAKE_CASE）
- **注释规范**:
  - 类和方法必须有 JavaDoc 注释
  - 复杂逻辑必须有行内注释
- **日志规范**:
  - 使用 Slf4j 的 @Slf4j 注解
  - 日志级别：DEBUG（开发）、INFO（生产）、ERROR（错误）
  - 关键操作必须记录日志

#### 小程序前端
- **命名规范**:
  - 文件名：小写短横线分隔（kebab-case）
  - 变量名：小驼峰（camelCase）
  - 常量：全大写下划线分隔（UPPER_SNAKE_CASE）
- **注释规范**:
  - 函数必须有注释说明用途
  - 复杂逻辑必须有注释
- **API 调用**:
  - 统一使用 wx.request
  - 必须处理 success 和 fail 回调
  - 必须有 loading 提示

### Git 提交规范

#### 提交信息格式
```
<type>(<scope>): <subject>

<body>

<footer>
```

#### Type 类型
- **feat**: 新功能
- **fix**: 修复 bug
- **docs**: 文档更新
- **style**: 代码格式调整
- **refactor**: 重构
- **test**: 测试相关
- **chore**: 构建/工具相关

#### 示例
```bash
feat(payment): 添加微信支付功能

- 实现微信支付统一下单
- 实现支付回调处理
- 添加订单状态查询

Closes #123
```

### 数据库规范

#### 表设计规范
- 表名：小写下划线分隔，复数形式（如 users, payment_orders）
- 字段名：小写下划线分隔（如 user_id, created_time）
- 主键：统一使用 id，BIGINT 类型，自增
- 时间字段：统一使用 DATETIME 类型，命名为 xxx_time
- 必须字段：created_time, updated_time

#### 索引规范
- 外键必须建索引
- 查询频繁的字段建索引
- 唯一字段建唯一索引
- 索引命名：idx_字段名 或 uk_字段名（唯一索引）

### API 设计规范

#### RESTful 风格
- GET: 查询资源
- POST: 创建资源
- PUT: 更新资源
- DELETE: 删除资源

#### 响应格式
```json
{
  "success": true,
  "message": "操作成功",
  "data": { ... },
  "code": 200
}
```

#### 错误响应
```json
{
  "success": false,
  "message": "错误信息",
  "code": 400
}
```

### 测试规范

#### 单元测试
- 关键业务逻辑必须有单元测试
- 测试覆盖率要求 > 70%
- 使用 JUnit 5 + Mockito

#### 接口测试
- 使用提供的测试脚本
- 每次修改后必须测试相关接口
- 记录测试结果

---

## 常见问题

### 开发环境问题

#### Q1: 后端启动失败，提示数据库连接错误
**A**: 检查以下几点：
1. MySQL 服务是否启动：`systemctl status mysql`
2. 数据库是否创建：`SHOW DATABASES;`
3. 用户名密码是否正确：检查 application.yml
4. 端口是否正确：默认 3306

#### Q2: 小程序无法调用后端接口
**A**: 检查以下几点：
1. 后端服务是否启动：访问 http://localhost:8080
2. 小程序配置的域名是否正确：检查 utils/config.js
3. 是否勾选"不校验合法域名"：微信开发者工具 → 详情
4. 网络是否正常：检查防火墙和网络连接

#### Q3: 微信支付测试失败
**A**: 检查以下几点：
1. 是否使用真机测试（模拟器不支持支付）
2. AppID 是否正确配置
3. 商户号是否正确
4. 证书文件是否存在：backend/src/main/resources/cert/
5. 查看后端日志：tail -f logs/application.log

### 数据库问题

#### Q4: 如何重置数据库
```bash
# 删除数据库
mysql -u root -p -e "DROP DATABASE IF EXISTS easy_joy_life_db;"

# 重新创建并导入
mysql -u root -p -e "CREATE DATABASE easy_joy_life_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -u root -p easy_joy_life_db < backend/src/main/resources/mysql-init.sql
```

#### Q5: 如何连接生产环境数据库
```bash
# 方式一：命令行
mysql -h 121.43.96.127 -P 3306 -u root -p'EasyJoyLife2024!@#' easy_joy_life_db

# 方式二：使用 MySQL Workbench 或 Navicat
主机: 121.43.96.127
端口: 3306
用户: root
密码: EasyJoyLife2024!@#
数据库: easy_joy_life_db
```

#### Q6: 如何备份数据库
```bash
# 备份
mysqldump -u root -p easy_joy_life_db > backup_$(date +%Y%m%d).sql

# 恢复
mysql -u root -p easy_joy_life_db < backup_20260207.sql
```

### 部署问题

#### Q7: 如何查看服务器日志
```bash
# 后端应用日志
ssh root@121.43.96.127
tail -f /opt/easy-joy-life/logs/application.log

# 实时过滤关键字
tail -f /opt/easy-joy-life/logs/application.log | grep -i error
tail -f /opt/easy-joy-life/logs/application.log | grep -i payment
```

#### Q8: 服务器部署后无法访问
**A**: 检查以下几点：
1. 服务是否启动：`systemctl status easy-joy-life-backend`
2. 端口是否监听：`netstat -tlnp | grep 8080`
3. Nginx 是否正常：`systemctl status nginx`
4. 防火墙是否开放：`ufw status`
5. 域名解析是否正确：`ping xx.aieo.cn`

#### Q9: SSL 证书过期怎么办
```bash
# 手动续期
certbot renew

# 重启 Nginx
systemctl reload nginx

# 查看证书有效期
certbot certificates
```

### 支付问题

#### Q10: 微信支付回调没有收到
**A**: 检查以下几点：
1. 回调地址是否正确：https://xx.aieo.cn/api/payment/wechat/notify
2. 服务器是否可以从外网访问
3. 查看微信支付商户平台的回调日志
4. 检查后端日志是否有回调记录

#### Q11: 支付成功但余额没有增加
**A**: 检查以下几点：
1. 查看 payment_orders 表，订单状态是否为 PAID
2. 查看 wallet_transactions 表，是否有充值记录
3. 查看 user_wallets 表，余额是否更新
4. 检查后端日志，是否有异常

### 小程序问题

#### Q12: 小程序登录失败
**A**: 检查以下几点：
1. AppID 和 AppSecret 是否正确
2. 后端 /users/wechat/login 接口是否正常
3. 查看小程序控制台的错误信息
4. 查看后端日志

#### Q13: 小程序页面数据不显示
**A**: 检查以下几点：
1. 打开调试模式，查看 Console 日志
2. 检查 API 请求是否成功
3. 检查数据格式是否正确
4. 检查 setData 是否执行

---

## 快速开始指南

### 第一天：环境搭建
1. ✅ 安装开发工具（JDK、Maven、MySQL、微信开发者工具）
2. ✅ 克隆项目代码
3. ✅ 创建数据库并导入初始数据
4. ✅ 启动后端服务
5. ✅ 导入小程序项目
6. ✅ 测试基本功能（门店列表、钱包查询）

### 第二天：熟悉代码
1. ✅ 阅读项目文档（本文档）
2. ✅ 了解数据库表结构
3. ✅ 熟悉后端代码结构
4. ✅ 熟悉小程序代码结构
5. ✅ 运行测试脚本，了解 API 接口

### 第三天：实践开发
1. ✅ 尝试修改一个简单功能
2. ✅ 提交代码到 Git
3. ✅ 部署到测试环境
4. ✅ 测试功能是否正常

---

## 重要提醒

### ⚠️ 安全注意事项
1. **不要泄露敏感信息**：
   - 数据库密码
   - 微信支付密钥
   - 服务器 SSH 密码
   - API 密钥

2. **不要直接操作生产数据库**：
   - 先在开发环境测试
   - 备份数据后再操作
   - 使用事务确保数据一致性

3. **不要随意重启生产服务**：
   - 先确认没有用户在使用
   - 做好备份
   - 通知团队成员

### 📝 开发建议
1. **先看文档，再写代码**
2. **遇到问题先查日志**
3. **提交代码前先测试**
4. **写清楚的提交信息**
5. **及时同步代码**
6. **保持代码整洁**

### 🔗 有用的链接
- **微信小程序文档**: https://developers.weixin.qq.com/miniprogram/dev/framework/
- **微信支付文档**: https://pay.weixin.qq.com/wiki/doc/apiv3/index.shtml
- **Spring Boot 文档**: https://spring.io/projects/spring-boot
- **MySQL 文档**: https://dev.mysql.com/doc/

---

## 联系方式

### 团队成员
- **项目负责人**: [待补充]
- **后端开发**: [待补充]
- **前端开发**: [待补充]

### 技术支持
- **微信支付客服**: 95017
- **支付宝客服**: 95188

---

## 附录

### A. 测试账号
- **测试用户 ID**: 1
- **测试 openid**: test_openid_001

### B. 测试数据
项目已内置测试数据：
- 5 个门店
- 13 个房间
- 3 个测试用户
- 若干交易记录

### C. 常用命令速查

#### 服务器操作
```bash
# SSH 连接
ssh root@121.43.96.127

# 查看服务状态
systemctl status easy-joy-life-backend

# 重启服务
systemctl restart easy-joy-life-backend

# 查看日志
tail -f /opt/easy-joy-life/logs/application.log
```

#### 数据库操作
```bash
# 连接数据库
mysql -h 121.43.96.127 -P 3306 -u root -p'EasyJoyLife2024!@#' easy_joy_life_db

# 查看表
SHOW TABLES;

# 查看用户钱包
SELECT * FROM user_wallets WHERE user_id = 1;

# 查看交易记录
SELECT * FROM wallet_transactions WHERE user_id = 1 ORDER BY created_time DESC LIMIT 10;
```

#### Git 操作
```bash
# 拉取最新代码
git pull origin main

# 提交代码
git add .
git commit -m "feat: 添加新功能"
git push origin main

# 查看状态
git status
```

---

## 更新日志

### 2026-02-07
- ✅ 完成微信支付功能
- ✅ 完成交易记录功能
- ✅ 修复钱包页面数据显示
- ✅ 优化充值流程
- ✅ 配置 MySQL 远程访问
- ✅ 创建新人开发指南

### 2026-02-03
- ✅ 完成项目初始部署
- ✅ 配置 SSL 证书
- ✅ 完成微信小程序登录
- ✅ 完成门店和房间管理

---

**祝你开发顺利！有问题随时联系团队成员。** 🎉
