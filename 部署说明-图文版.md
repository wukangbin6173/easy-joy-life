# 易享生活棋牌室 - 部署说明（图文版）

## 📋 准备工作

### 服务器信息
- **IP地址**: 121.43.96.127
- **用户名**: root
- **密码**: Easy8286173
- **项目路径**: /opt/easy-joy-life

### 需要的工具
- PuTTY（SSH 客户端）
- 或者任何其他 SSH 工具（如 Xshell、MobaXterm）

---

## 🚀 快速部署（推荐）

### 方法一：一键命令

1. **打开 PuTTY**
   - Host Name: `121.43.96.127`
   - Port: `22`
   - Connection type: SSH
   - 点击 "Open"

2. **登录**
   - login as: `root`
   - password: `Easy8286173`（输入时不显示）

3. **复制粘贴以下命令**（一次性执行）

```bash
cd /opt/easy-joy-life && git pull origin main && cd backend && ./mvnw clean package -DskipTests && cd ../deploy && docker-compose -f docker-compose.prod.yml restart backend && docker-compose -f docker-compose.prod.yml logs --tail=50 backend
```

4. **等待完成**
   - 看到 "BUILD SUCCESS" 表示编译成功
   - 看到 "backend" 容器重启成功
   - 看到日志输出表示服务正常运行

---

## 📝 分步部署（详细版）

如果一键命令失败，可以分步执行：

### 步骤 1: 更新代码

```bash
cd /opt/easy-joy-life
git pull origin main
```

**预期输出**:
```
remote: Enumerating objects: 22, done.
remote: Counting objects: 100% (22/22), done.
...
Updating 8840a92..2b5d51e
Fast-forward
 backend/src/main/java/com/easyjoylife/config/RestTemplateConfig.java | 45 +++++++++++++++++++++++++++++++++++++++++++++
 backend/src/main/java/com/easyjoylife/controller/AuthController.java | 80 ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 62 insertions(+), 63 deletions(-)
```

### 步骤 2: 编译后端

```bash
cd backend
./mvnw clean package -DskipTests
```

**预期输出**:
```
[INFO] Scanning for projects...
[INFO] Building easy-joy-life-system 1.0.0
...
[INFO] BUILD SUCCESS
[INFO] Total time: 2.345 s
```

⏱️ **预计耗时**: 2-5 分钟

### 步骤 3: 重启服务

```bash
cd ../deploy
docker-compose -f docker-compose.prod.yml restart backend
```

**预期输出**:
```
Restarting backend ... done
```

### 步骤 4: 查看日志

```bash
docker-compose -f docker-compose.prod.yml logs --tail=50 backend
```

**预期看到**:
```
backend    | 2026-01-28 13:00:00.000  INFO --- [main] c.e.EasyJoyLifeApplication : Started EasyJoyLifeApplication in 5.123 seconds
backend    | 2026-01-28 13:00:00.000  INFO --- [main] o.s.b.w.embedded.tomcat.TomcatWebServer  : Tomcat started on port(s): 8080 (http)
```

---

## ✅ 验证部署

### 1. 检查服务状态

```bash
docker-compose -f docker-compose.prod.yml ps
```

**预期输出**:
```
   Name                 Command               State           Ports
-------------------------------------------------------------------------
backend      java -jar /app/app.jar           Up      0.0.0.0:8080->8080/tcp
```

### 2. 测试 API

```bash
curl https://xx.aieo.cn/api/stores
```

**预期输出**: JSON 格式的门店列表

### 3. 查看登录日志

```bash
docker-compose -f docker-compose.prod.yml logs backend | grep "微信登录"
```

---

## 🧪 真机测试

### 1. 打开微信开发者工具

### 2. 真机调试

- 点击"真机调试"按钮
- 扫码连接手机

### 3. 测试登录

- 打开小程序
- 查看控制台日志
- 应该看到：
  ```
  收到微信登录请求，code: xxx
  调用微信API: https://api.weixin.qq.com/sns/jscode2session?...
  微信API响应: {openid=xxx, session_key=xxx}
  微信登录成功: openid=xxx, userId=xxx
  ```

### 4. 检查首页

- 首页 banner 下方应该显示用户信息卡片
- 显示用户头像（圆形）
- 显示用户昵称
- 显示欢迎语

---

## 🔧 故障排查

### 问题 1: Git pull 失败

**错误**: `Connection timed out` 或 `RPC failed`

**解决**:
```bash
git config --global http.postBuffer 524288000
git config --global http.lowSpeedLimit 0
git config --global http.lowSpeedTime 999999
git pull origin main
```

如果还是失败：
```bash
cd /opt
rm -rf easy-joy-life
git clone https://github.com/wukangbin6173/easy-joy-life.git
```

### 问题 2: Maven 编译失败

**错误**: `BUILD FAILURE`

**解决**:
```bash
# 清理缓存
./mvnw clean

# 检查 Java 版本
java -version

# 重新编译
./mvnw clean package -DskipTests
```

### 问题 3: Docker 容器启动失败

**错误**: 容器状态显示 `Exit` 或 `Restarting`

**解决**:
```bash
# 查看详细日志
docker-compose -f docker-compose.prod.yml logs backend

# 停止并重新启动
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d backend
```

### 问题 4: 端口被占用

**错误**: `Address already in use`

**解决**:
```bash
# 查看占用端口的进程
netstat -tlnp | grep 8080

# 杀死进程（替换 PID）
kill -9 <PID>

# 重启服务
docker-compose -f docker-compose.prod.yml restart backend
```

---

## 📊 本次更新内容

### 1. 新增文件

- ✅ `backend/src/main/java/com/easyjoylife/config/RestTemplateConfig.java`
  - 配置 RestTemplate 支持 text/plain 媒体类型
  - 解决微信 API 响应格式问题

### 2. 修改文件

- ✅ `backend/src/main/java/com/easyjoylife/controller/AuthController.java`
  - 使用依赖注入的 RestTemplate
  - 简化登录逻辑
  - 改进错误处理

- ✅ `miniprogram/pages/index/index.js`
  - 增强用户信息获取逻辑
  - 添加详细调试日志

- ✅ `miniprogram/pages/index/index.wxml`
  - 用户卡片始终显示
  - 兼容多种字段名

### 3. 核心改进

**问题**: 微信 API 返回 `text/plain`，但内容是 JSON，导致 `HttpMessageConverter` 错误

**解决**: 配置 Jackson 转换器支持 `text/plain` 媒体类型

**效果**: 
- ✅ 登录不再报错
- ✅ 成功获取用户信息
- ✅ 首页正常显示用户卡片

---

## 📞 需要帮助？

如果遇到问题：

1. **查看日志**
   ```bash
   docker-compose -f docker-compose.prod.yml logs -f backend
   ```

2. **检查服务状态**
   ```bash
   docker-compose -f docker-compose.prod.yml ps
   ```

3. **重启所有服务**
   ```bash
   docker-compose -f docker-compose.prod.yml restart
   ```

---

## 🎉 部署成功标志

✅ Git pull 成功  
✅ Maven 编译成功（BUILD SUCCESS）  
✅ Docker 容器重启成功  
✅ 服务日志正常  
✅ API 接口可访问  
✅ 真机调试登录成功  
✅ 首页显示用户信息  

---

**部署时间**: 2026-01-28  
**版本**: v1.0.3  
**更新内容**: 修复 HttpMessageConverter 错误，优化用户信息显示
