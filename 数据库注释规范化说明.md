# 数据库注释规范化说明

## 概述

本文档说明如何将现有数据库的表注释和字段注释规范化，使其符合 ZERO 开发规范。

**重要说明**：本次操作**只修改注释**，不改变任何表名和字段名。

## ZERO 规范要点

### 表注释格式

```
中文名称|功能说明
```

示例：
- `用户主表|存储用户基础信息和认证信息`
- `门店主表|存储门店基础信息和位置信息`

### 字段注释格式

```
序号|中文字段分类|中文字段名|详细说明|是否显示
```

参数说明：
- **序号**：字段在表中的逻辑顺序（1, 2, 3...）
- **中文字段分类**：字段所属分类（核心、资料、绑定、状态等）
- **中文字段名**：用于前端表格列头显示
- **详细说明**：详细描述字段用途，枚举值写为 `value1=说明1, value2=说明2`
- **是否显示**：`1`=默认显示，`0`=默认隐藏（可选，默认1）

示例：
```sql
COMMENT '1|核心|用户名|用于登录的唯一标识|1'
COMMENT '2|资料|昵称|用户显示名称|1'
COMMENT '3|状态|激活状态|0=禁用, 1=正常|1'
```

### 常用字段分类

| 分类 | 说明 | 举例 |
|------|------|------|
| 系统 | 系统字段（ID、时间戳） | id, created_at, updated_at |
| 核心 | 核心标识 + 安全凭证 | openid, username, password |
| 资料 | 用户自填信息 | nickname, avatar, real_name |
| 绑定 | 绑定信息 | phone, email |
| 状态 | 状态标识 | status, active |
| 等级 | 等级相关 | level, points |
| 余额 | 余额相关 | balance, amount |
| 位置 | 位置信息 | longitude, latitude |
| 联系 | 联系方式 | phone, email |
| 信息 | 描述信息 | description, remark |
| 价格 | 价格相关 | price, amount |
| 时间 | 时间记录 | start_time, end_time |
| 支付 | 支付相关 | payment_method, payment_status |
| 关联 | 关联字段 | store_id, room_id |

## 执行步骤

### 1. 备份数据库（重要！）

在执行任何操作前，请先备份数据库：

```bash
mysqldump -h 121.43.96.127 -P 3306 -u root -p easy_joy_life_db > backup_$(date +%Y%m%d_%H%M%S).sql
```

### 2. 查看脚本内容

打开 `update-database-comments.sql` 文件，查看将要执行的 SQL 语句。

### 3. 执行更新脚本

#### Windows 系统

双击运行 `update-comments.bat`，按提示输入数据库密码。

#### Linux/Mac 系统

```bash
mysql -h 121.43.96.127 -P 3306 -u root -p -D easy_joy_life_db < update-database-comments.sql
```

### 4. 验证结果

在数据库客户端中查看表结构，确认注释已正确更新。

```sql
-- 查看表注释
SELECT 
    TABLE_NAME as '表名',
    TABLE_COMMENT as '表注释'
FROM 
    information_schema.TABLES 
WHERE 
    TABLE_SCHEMA = 'easy_joy_life_db'
ORDER BY 
    TABLE_NAME;

-- 查看字段注释
SELECT 
    TABLE_NAME as '表名',
    COLUMN_NAME as '字段名',
    COLUMN_COMMENT as '字段注释'
FROM 
    information_schema.COLUMNS 
WHERE 
    TABLE_SCHEMA = 'easy_joy_life_db'
    AND TABLE_NAME = 'users'  -- 替换为要查看的表名
ORDER BY 
    ORDINAL_POSITION;
```

## 涉及的表

本脚本将更新以下表的注释：

1. **users** - 用户主表
2. **stores** - 门店主表
3. **rooms** - 房间主表
4. **orders** - 订单主表（如果存在）
5. **payments** - 支付记录表（如果存在）
6. **payment_orders** - 支付订单表
7. **user_wallets** - 用户钱包表
8. **wallet_transactions** - 钱包交易记录表
9. **devices** - 设备主表（如果存在）
10. **admins** - 管理员主表（如果存在）
11. **system_config** - 系统配置表（如果存在）

## 注意事项

1. **只修改注释**：本脚本不会修改任何表名、字段名、数据类型或数据内容
2. **兼容性处理**：脚本会检查表和字段是否存在，不存在的会自动跳过
3. **可重复执行**：脚本可以多次执行，不会产生副作用
4. **备份优先**：执行前务必备份数据库
5. **应用程序无影响**：由于只修改注释，现有应用程序代码无需修改

## 回滚方法

如果需要回滚，使用之前的备份文件恢复：

```bash
mysql -h 121.43.96.127 -P 3306 -u root -p easy_joy_life_db < backup_YYYYMMDD_HHMMSS.sql
```

## 后续工作

注释规范化完成后，建议：

1. 在数据库设计文档中更新表结构说明
2. 通知团队成员新的注释规范
3. 在后续开发中遵循 ZERO 规范创建新表和字段
4. 定期检查和维护注释的准确性

## 参考文档

- ZERO/docs/standards/02.backend-02.db-tables.md - 数据库表命名规范
- ZERO/docs/standards/02.backend-03.db-fields.md - 数据库字段命名规范
