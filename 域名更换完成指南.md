# 域名更换完成指南 - xx.aieo.cn

## 🎯 当前状态

### ✅ 已完成的更新
1. **小程序配置** - API地址更新为 `https://xx.aieo.cn`
2. **服务器信息** - 更新为新域名
3. **Nginx配置** - 更新为新域名和Let's Encrypt证书路径
4. **部署脚本** - 更新所有域名引用
5. **SSL脚本** - 更新为新域名配置

### ⚠️ 需要手动完成的关键步骤

## 🌐 第一步：DNS解析配置 (最重要)

### 当前DNS状态
```bash
# 当前解析 (错误)
xx.aieo.cn → 198.18.0.95

# 需要修改为
xx.aieo.cn → 121.43.96.127
```

### DNS配置步骤

1. **登录域名管理后台**
   - 访问你的域名注册商管理面板
   - 找到 `xx.aieo.cn` 的DNS管理

2. **修改A记录**
   ```
   类型: A
   主机记录: @
   记录值: 121.43.96.127
   TTL: 600 (10分钟)
   ```

3. **添加www子域名**
   ```
   类型: A
   主机记录: www
   记录值: 121.43.96.127
   TTL: 600
   ```

4. **验证DNS生效**
   ```bash
   # 在本地电脑运行，等待DNS生效
   nslookup xx.aieo.cn
   # 应该返回: 121.43.96.127
   ```

## 🔒 第二步：服务器SSL证书配置

### DNS生效后在服务器上执行

```bash
# 1. 连接服务器
ssh root@121.43.96.127

# 2. 进入项目目录
cd /opt/easy-joy-life/deploy

# 3. 更新Nginx配置
cp nginx.conf /etc/nginx/sites-available/easy-joy-life

# 4. 申请SSL证书
chmod +x ssl-auto-setup.sh
./ssl-auto-setup.sh install

# 5. 验证配置
nginx -t && systemctl reload nginx

# 6. 测试访问
curl -I https://xx.aieo.cn
curl -s https://xx.aieo.cn/api/stores | head -3
```

## 📱 第三步：微信小程序后台配置

### 更新服务器域名白名单

1. **登录微信小程序后台**
   - 访问: https://mp.weixin.qq.com

2. **更新域名配置**
   - 开发 → 开发管理 → 开发设置
   - 服务器域名 → request合法域名
   - 删除旧域名: `https://easyjoylife.xin`
   - 添加新域名: `https://xx.aieo.cn`

3. **保存配置**
   - 点击"保存并提交"
   - 等待审核通过

## 🧪 第四步：完整测试验证

### 服务器端测试
```bash
# 在服务器上测试
curl -I https://xx.aieo.cn
curl -s https://xx.aieo.cn/api/stores
curl -s https://xx.aieo.cn/api/rooms
```

### 小程序端测试
1. **开发者工具测试**
   - 打开微信开发者工具
   - 导入小程序项目
   - 测试API调用是否正常

2. **真机测试**
   - 预览小程序到手机
   - 测试所有功能是否正常

## 📋 验证清单

### DNS解析验证
- [ ] `nslookup xx.aieo.cn` 返回 `121.43.96.127`
- [ ] DNS解析全球生效

### 服务器配置验证
- [ ] HTTP访问: `http://xx.aieo.cn` 自动跳转HTTPS
- [ ] HTTPS访问: `https://xx.aieo.cn` 显示正常页面
- [ ] API测试: `https://xx.aieo.cn/api/stores` 返回门店数据
- [ ] 管理后台: `https://xx.aieo.cn/admin.html` 可正常访问

### SSL证书验证
- [ ] 浏览器显示绿色锁图标
- [ ] 证书有效期正常 (90天)
- [ ] 自动续期已配置

### 小程序验证
- [ ] 微信后台域名白名单已更新
- [ ] 开发者工具API调用正常
- [ ] 真机测试功能完整

## 🚨 故障排除

### 常见问题及解决方案

#### 1. DNS解析不生效
```bash
# 检查DNS传播状态
dig +short xx.aieo.cn @8.8.8.8
dig +short xx.aieo.cn @1.1.1.1

# 清除本地DNS缓存 (Windows)
ipconfig /flushdns
```

#### 2. SSL证书申请失败
```bash
# 检查域名解析
nslookup xx.aieo.cn

# 检查80端口是否开放
curl -I http://xx.aieo.cn

# 查看详细错误日志
certbot --nginx -d xx.aieo.cn --dry-run
```

#### 3. 小程序无法访问新域名
- 确认DNS已完全生效
- 检查微信后台域名配置
- 清除小程序缓存重新测试

## 📞 技术支持

### 自助检查工具
```bash
# 域名解析检查
nslookup xx.aieo.cn

# SSL证书检查
openssl s_client -connect xx.aieo.cn:443 -servername xx.aieo.cn

# API服务检查
curl -s https://xx.aieo.cn/api/stores | jq .
```

### 在线检查工具
- **DNS传播检查**: https://dnschecker.org/
- **SSL证书检查**: https://www.ssllabs.com/ssltest/
- **网站可用性**: https://downforeveryoneorjustme.com/

## 🎉 完成后的访问地址

### 新的生产环境地址
- **主站**: https://xx.aieo.cn
- **门店API**: https://xx.aieo.cn/api/stores
- **房间API**: https://xx.aieo.cn/api/rooms  
- **管理后台**: https://xx.aieo.cn/admin.html

### 小程序配置
- **API基础地址**: https://xx.aieo.cn
- **域名白名单**: https://xx.aieo.cn

## ⏰ 预计完成时间

1. **DNS配置**: 5分钟
2. **DNS生效等待**: 10分钟 - 24小时
3. **SSL证书申请**: 5分钟
4. **微信后台配置**: 5分钟
5. **测试验证**: 10分钟

**总计**: 最快30分钟，最慢24小时（主要等待DNS生效）

---

**下一步操作**: 
1. 立即配置DNS解析 (最重要)
2. 等待DNS生效后申请SSL证书
3. 更新微信小程序后台域名配置
4. 进行完整功能测试

**注意**: DNS解析是关键步骤，必须先完成才能进行后续操作！