---
description: 推送到云效/Codeup 时不提交编译中间文件与最终构建产物
alwaysApply: true
---

# Git / 云效（Codeup）推送约定

- 向阿里云云效（Codeup）等远程仓库推送时：**不要**提交或上传编译产生的中间文件、打包产物、source map、本地私有配置。
- 小程序侧典型需忽略：`miniprogram/miniprogram_npm/`、`miniprogram/node_modules/`、`unpackage/`、`**/*.map`、`project.private.config.json` 等；Java 侧保持 `backend/target/` 等已有规则。
- **不要**在仓库、规则文件或对话中写入个人访问令牌（PAT）、密码、私钥；使用 SSH 或本机凭据管理器认证。
- 若需补充忽略项，优先更新仓库根目录 `.gitignore`，再提交。
