# 更新部署说明

## 🎯 核心原则

**只重启有变化的服务，不影响其他服务**

---

## 📦 服务架构

```
┌─────────────────────────────────────┐
│  Nginx (80/443)                     │  ← 反向代理，很少需要重启
│  - 配置文件: /etc/nginx/...        │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  Spring Boot (8080)                 │  ← 最常更新的服务
│  - 代码、配置、业务逻辑             │
└─────────────────────────────────────┘
       ↓                    ↓
┌──────────────┐    ┌──────────────┐
│ MySQL (3306) │    │ Redis (6379) │  ← 独立运行，不需要重启
│ - 数据存储   │    │ - 缓存       │
└──────────────┘    └──────────────┘
```

---

## 🚀 更新方式对比

### 方式1: 快速更新（推荐）⭐

**使用场景：** 修改了Java代码或配置文件

**Windows本地操作：**
```batch
# 方法A: 仅重启服务（代码已在服务器上）
快速更新后端.bat

# 方法B: 自动上传代码并重启
智能更新后端.bat
```

**Linux/Mac本地操作：**
```bash
# 上传修改的文件
scp backend/src/main/java/com/easyjoylife/xxx.java root@xx.aieo.cn:/opt/easy-joy-life/backend/src/main/java/com/easyjoylife/

# 执行更新
ssh root@xx.aieo.cn "cd /opt/easy-joy-life/deploy && ./update-backend-only.sh"
```

**影响范围：**
- ✅ 仅重启Spring Boot（约30秒）
- ❌ 不重启MySQL
- ❌ 不重启Redis
- ❌ 不重启Nginx

---

### 方式2: 手动更新

**步骤1: SSH到服务器**
```bash
ssh root@xx.aieo.cn
```

**步骤2: 构建应用**
```bash
cd /opt/easy-joy-life/backend
./mvnw clean package -DskipTests
```

**步骤3: 重启服务**
```bash
systemctl restart easy-joy-life-backend
```

**步骤4: 查看日志**
```bash
journalctl -u easy-joy-life-backend -f
```

---

### 方式3: 全量部署（不推荐用于日常更新）

**使用场景：** 首次部署或重大架构变更

```bash
ssh root@xx.aieo.cn
cd /opt/easy-joy-life/deploy
./deploy-backend-systemd.sh
```

**影响范围：**
- ✅ 检查所有服务
- ✅ 重新构建应用
- ✅ 重启Spring Boot
- ⚠️ 可能影响MySQL、Redis（如果配置有变）

---

## 📝 典型更新场景

### 场景1: 修复Bug 🐛

```bash
# 1. 在本地修改代码
# 2. 运行智能更新脚本
智能更新后端.bat
```

**耗时：** ~30秒  
**影响：** Spring Boot短暂重启

---

### 场景2: 添加新功能 ✨

```bash
# 1. 在本地开发新功能
# 2. 测试通过后，运行智能更新脚本
智能更新后端.bat
```

**耗时：** ~30秒  
**影响：** Spring Boot短暂重启

---

### 场景3: 修改配置 ⚙️

```bash
# 1. 修改 application-prod.yml
# 2. 上传配置文件
scp backend/src/main/resources/application-prod.yml root@xx.aieo.cn:/opt/easy-joy-life/backend/src/main/resources/

# 3. 快速更新
快速更新后端.bat
```

**耗时：** ~30秒  
**影响：** Spring Boot短暂重启

---

### 场景4: 修改Nginx配置 🌐

```bash
# 1. 修改 deploy/nginx.conf
# 2. 上传配置
scp deploy/nginx.conf root@xx.aieo.cn:/etc/nginx/sites-available/easy-joy-life

# 3. 测试并重新加载
ssh root@xx.aieo.cn "nginx -t && systemctl reload nginx"
```

**耗时：** ~1秒  
**影响：** 无影响（平滑重载）

---

### 场景5: 更新数据库 💾

```bash
# 1. 准备SQL脚本
# 2. 上传并执行
scp your-update.sql root@xx.aieo.cn:/tmp/
ssh root@xx.aieo.cn "mysql --defaults-file=/etc/mysql/debian.cnf -D easy_joy_life_db < /tmp/your-update.sql"
```

**耗时：** 取决于SQL复杂度  
**影响：** 无需重启任何服务

---

## ⚡ 快速命令速查

### 查看服务状态
```bash
ssh root@xx.aieo.cn "systemctl status easy-joy-life-backend"
```

### 查看实时日志
```bash
ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -f"
```

### 测试API
```bash
curl https://xx.aieo.cn/api/stores
```

### 检查所有服务
```bash
ssh root@xx.aieo.cn "cd /opt/easy-joy-life/deploy && ./final-status-check.sh"
```

---

## 🔍 故障排查

### 更新后服务无法启动

```bash
# 查看错误日志
ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -n 100 --no-pager"

# 查看服务状态
ssh root@xx.aieo.cn "systemctl status easy-joy-life-backend"
```

### 更新后API无法访问

```bash
# 检查端口监听
ssh root@xx.aieo.cn "netstat -tuln | grep 8080"

# 测试本地API
ssh root@xx.aieo.cn "curl http://localhost:8080/api/stores"

# 检查Nginx
ssh root@xx.aieo.cn "systemctl status nginx"
```

---

## 💡 最佳实践

### ✅ 推荐做法

1. **使用快速更新脚本**
   - 自动化流程，减少人为错误
   - 包含健康检查，确保更新成功

2. **更新前备份**
   ```bash
   ssh root@xx.aieo.cn "cp /opt/easy-joy-life/backend/target/easy-joy-life-system-1.0.0.jar /opt/easy-joy-life/backend/target/easy-joy-life-system-1.0.0.jar.backup"
   ```

3. **查看日志**
   - 更新后立即查看日志
   - 确认没有错误

4. **避免高峰期**
   - 选择用户较少的时间更新
   - 减少影响

### ❌ 避免做法

1. **不要全量重启**
   - 除非必要，不要重启MySQL、Redis
   - 这些服务稳定运行，无需频繁重启

2. **不要跳过测试**
   - 本地测试通过后再更新生产环境

3. **不要忽略日志**
   - 更新后必须查看日志
   - 确认服务正常

---

## 📊 更新时间对比

| 更新方式 | 构建时间 | 重启时间 | 总耗时 | 影响服务 |
|---------|---------|---------|--------|---------|
| **快速更新** | ~20秒 | ~10秒 | ~30秒 | 仅Spring Boot |
| **手动更新** | ~20秒 | ~10秒 | ~30秒 | 仅Spring Boot |
| **全量部署** | ~30秒 | ~30秒 | ~2分钟 | 所有服务 |
| **仅重启** | 0秒 | ~10秒 | ~10秒 | 仅Spring Boot |

---

## 🎓 学习资源

### 相关文档
- `快速更新指南.md` - 详细的更新步骤
- `服务状态检查指南.md` - 服务检查方法
- `deploy/` - 所有部署脚本

### 脚本说明
- `update-backend-only.sh` - 服务器端快速更新脚本
- `快速更新后端.bat` - Windows快速更新脚本
- `智能更新后端.bat` - Windows智能更新脚本（自动上传）

---

## 📞 需要帮助？

遇到问题时：

1. **查看日志**
   ```bash
   ssh root@xx.aieo.cn "journalctl -u easy-joy-life-backend -f"
   ```

2. **检查服务**
   ```bash
   ssh root@xx.aieo.cn "cd /opt/easy-joy-life/deploy && ./final-status-check.sh"
   ```

3. **回滚版本**
   ```bash
   ssh root@xx.aieo.cn "cp /opt/easy-joy-life/backend/target/easy-joy-life-system-1.0.0.jar.backup /opt/easy-joy-life/backend/target/easy-joy-life-system-1.0.0.jar && systemctl restart easy-joy-life-backend"
   ```

---

**最后更新：** 2024-02-07  
**维护者：** 易享生活开发团队
