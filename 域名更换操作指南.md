# 域名更换操作指南 - easyjoylife.xin → xx.aieo.cn

## 📋 域名更换清单

### ✅ 已完成的更新

1. **服务器信息文件** - 更新为新域名 `xx.aieo.cn` ✅
2. **小程序配置** - API地址更新为 `https://xx.aieo.cn` ✅
3. **Nginx配置** - 更新为新域名和Let's Encrypt证书路径 ✅
4. **部署脚本** - 更新所有域名引用 ✅
5. **SSL脚本** - 更新为新域名配置 ✅

### ⚠️ 需要手动完成的操作 (关键步骤)

## 🌐 DNS解析配置

### 当前状态
- **新域名**: xx.aieo.cn
- **当前解析**: 198.18.0.95 (需要更改)
- **目标服务器**: 121.43.96.127

### DNS配置步骤

1. **登录域名管理后台**
   - 访问域名注册商的管理面板
   - 找到 `xx.aieo.cn` 的DNS设置

2. **添加/修改A记录**
   ```
   类型: A
   主机记录: @
   记录值: 121.43.96.127
   TTL: 600 (10分钟)
   ```

3. **添加www子域名**
   ```
   类型: A  
   主机记录: www
   记录值: 121.43.96.127
   TTL: 600
   ```

4. **验证DNS解析**
   ```bash
   # 等待DNS生效后测试
   nslookup xx.aieo.cn
   # 应该返回: 121.43.96.127
   ```

## 🔒 SSL证书配置

### 等DNS解析生效后执行

1. **申请Let's Encrypt证书**
   ```bash
   ssh root@121.43.96.127
   certbot --nginx -d xx.aieo.cn --email admin@xx.aieo.cn --agree-tos --non-interactive
   ```

2. **验证证书申请成功**
   ```bash
   curl -I https://xx.aieo.cn
   ```

## 📱 小程序配置更新

### 微信小程序后台配置

1. **登录微信小程序后台**: https://mp.weixin.qq.com

2. **更新服务器域名**
   - 开发 → 开发管理 → 开发设置
   - 删除旧域名: `https://easyjoylife.xin`
   - 添加新域名: `https://xx.aieo.cn`

3. **测试新域名**
   - 在开发者工具中测试API连接
   - 验证所有功能正常

## 🛠️ 完整配置脚本

### DNS解析生效后运行

```bash
#!/bin/bash
# 域名更换完整配置脚本

echo "=== 域名更换配置脚本 ==="

# 1. 申请SSL证书
echo "1. 申请SSL证书..."
certbot --nginx -d xx.aieo.cn \
  --email admin@xx.aieo.cn \
  --agree-tos \
  --non-interactive

# 2. 验证配置
echo "2. 验证配置..."
nginx -t && systemctl reload nginx

# 3. 测试访问
echo "3. 测试访问..."
curl -I https://xx.aieo.cn
curl -s https://xx.aieo.cn/api/stores | head -3

echo "=== 配置完成 ==="
```

## 📊 验证清单

### DNS解析验证
- [ ] `nslookup xx.aieo.cn` 返回 `121.43.96.127`
- [ ] DNS解析全球生效 (可能需要24小时)

### 服务器配置验证
- [ ] HTTP访问: `http://xx.aieo.cn` 正常
- [ ] HTTPS访问: `https://xx.aieo.cn` 正常 (SSL配置后)
- [ ] API测试: `https://xx.aieo.cn/api/stores` 返回数据
- [ ] 管理后台: `https://xx.aieo.cn/admin.html` 可访问

### 小程序验证
- [ ] 开发者工具中API地址更新
- [ ] 微信后台域名白名单更新
- [ ] 真机测试功能正常
- [ ] 网络请求使用新域名

## 🚨 注意事项

### DNS解析时间
- **生效时间**: 通常10分钟到24小时
- **检查方法**: 使用 `nslookup` 或在线DNS检查工具
- **建议**: 等待DNS完全生效后再申请SSL证书

### SSL证书申请
- **前提条件**: DNS必须先解析到正确的服务器
- **自动续期**: Let's Encrypt证书会自动续期
- **验证方法**: 浏览器访问时显示绿色锁图标

### 小程序配置
- **域名格式**: 必须使用 `https://xx.aieo.cn` 格式
- **测试环境**: 先在开发环境测试，确认无误后发布
- **缓存清理**: 可能需要清除小程序缓存

## 📞 故障排除

### 常见问题

1. **DNS解析不生效**
   - 检查域名管理后台配置
   - 等待更长时间 (最多24小时)
   - 使用不同的DNS服务器测试

2. **SSL证书申请失败**
   - 确认DNS已解析到正确IP
   - 检查防火墙设置
   - 查看certbot错误日志

3. **小程序无法访问**
   - 确认域名白名单配置正确
   - 检查API地址格式
   - 验证服务器响应正常

### 回滚方案

如果新域名有问题，可以临时回滚到旧域名：

1. **恢复小程序配置**
   ```javascript
   // miniprogram/utils/config.js
   baseUrl: 'https://easyjoylife.xin'
   ```

2. **恢复Nginx配置**
   ```bash
   # 恢复到easyjoylife.xin配置
   ```

## 📈 完成后的访问地址

### 新的生产环境地址
- **主站**: https://xx.aieo.cn
- **门店API**: https://xx.aieo.cn/api/stores
- **房间API**: https://xx.aieo.cn/api/rooms
- **管理后台**: https://xx.aieo.cn/admin.html

### 小程序配置
- **API基础地址**: https://xx.aieo.cn
- **域名白名单**: https://xx.aieo.cn

---

**下一步**: 配置DNS解析，等待生效后申请SSL证书并更新微信小程序后台域名设置