# 短信服务接入完整指南

## 当前状态

### ✅ 已完成
1. **短信API后端实现** - `SmsController.java`
   - 发送验证码：`POST /api/sms/send-code`
   - 验证验证码：`POST /api/sms/verify-code`
   - 开发模式：使用固定验证码 `888888`
   - 生产模式：需要接入真实短信服务

2. **银行卡验证码集成** - `BankCardController.java`
   - 添加银行卡时验证短信验证码
   - 开发模式：跳过验证码验证
   - 生产模式：必须验证

3. **小程序前端** - `add-bank-card.js`
   - 发送验证码按钮
   - 60秒倒计时
   - 开发模式显示验证码

### 🔄 开发模式配置

**后端配置：**
```java
// SmsController.java
private static final boolean DEV_MODE = true;  // 开发模式
private static final String DEV_CODE = "888888";  // 固定验证码

// BankCardController.java
private static final boolean DEV_MODE = true;  // 跳过验证码验证
```

**开发模式特性：**
- 使用固定验证码：`888888`
- API返回验证码（方便测试）
- 添加银行卡时跳过验证码验证
- 日志显示"【开发模式】"标记

## 短信服务商选择

### 推荐服务商

#### 1. 阿里云短信服务 ⭐⭐⭐⭐⭐
**优势：**
- 稳定可靠，到达率高
- 价格实惠（0.045元/条起）
- 支持国内外短信
- 完善的API文档

**价格：**
- 验证码短信：0.045元/条
- 通知短信：0.045元/条
- 营销短信：0.055元/条

**开通流程：**
1. 注册阿里云账号
2. 实名认证
3. 开通短信服务
4. 创建签名和模板
5. 获取AccessKey

#### 2. 腾讯云短信服务 ⭐⭐⭐⭐⭐
**优势：**
- 与微信生态集成好
- 价格优惠（0.045元/条起）
- 支持国内外短信

**价格：**
- 验证码短信：0.045元/条
- 通知短信：0.045元/条

**开通流程：**
1. 注册腾讯云账号
2. 实名认证
3. 开通短信服务
4. 创建签名和模板
5. 获取SecretId和SecretKey

#### 3. 华为云短信服务 ⭐⭐⭐⭐
**优势：**
- 价格便宜（0.04元/条起）
- 支持国内外短信

**价格：**
- 验证码短信：0.04元/条

## 阿里云短信服务接入详细步骤

### 第一步：注册和认证

1. **注册阿里云账号**
   - 访问：https://www.aliyun.com
   - 点击"免费注册"
   - 填写手机号和邮箱

2. **实名认证**
   - 进入控制台
   - 点击右上角头像 → 实名认证
   - 选择个人认证或企业认证
   - 上传身份证/营业执照

### 第二步：开通短信服务

1. **进入短信服务控制台**
   - 访问：https://dysms.console.aliyun.com
   - 点击"立即开通"
   - 同意服务协议

2. **充值**
   - 点击"充值"
   - 建议首次充值100元（可发送约2000条短信）

### 第三步：创建签名

1. **添加签名**
   - 进入"国内消息" → "签名管理"
   - 点击"添加签名"
   - 填写信息：
     - 签名名称：易享生活（或你的小程序名称）
     - 签名来源：小程序
     - 上传小程序截图
   - 提交审核（通常1-2小时）

### 第四步：创建模板

1. **添加模板**
   - 进入"国内消息" → "模板管理"
   - 点击"添加模板"
   - 选择"验证码"类型
   - 模板内容示例：
     ```
     您的验证码为：${code}，该验证码5分钟内有效，请勿泄漏于他人。
     ```
   - 提交审核（通常1-2小时）

### 第五步：获取AccessKey

1. **创建AccessKey**
   - 点击右上角头像 → AccessKey管理
   - 点击"创建AccessKey"
   - 保存AccessKeyId和AccessKeySecret（重要！）

### 第六步：集成到后端

#### 1. 添加Maven依赖

在 `backend/pom.xml` 中添加：

```xml
<!-- 阿里云短信SDK -->
<dependency>
    <groupId>com.aliyun</groupId>
    <artifactId>dysmsapi20170525</artifactId>
    <version>2.0.24</version>
</dependency>
```

#### 2. 配置application.yml

在 `backend/src/main/resources/application.yml` 中添加：

```yaml
# 阿里云短信配置
aliyun:
  sms:
    access-key-id: YOUR_ACCESS_KEY_ID
    access-key-secret: YOUR_ACCESS_KEY_SECRET
    sign-name: 易享生活
    template-code: SMS_123456789  # 你的模板CODE
    region-id: cn-hangzhou
```

#### 3. 创建短信服务类

创建 `backend/src/main/java/com/easyjoylife/service/AliyunSmsService.java`：

```java
package com.easyjoylife.service;

import com.aliyun.dysmsapi20170525.Client;
import com.aliyun.dysmsapi20170525.models.SendSmsRequest;
import com.aliyun.dysmsapi20170525.models.SendSmsResponse;
import com.aliyun.teaopenapi.models.Config;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;

@Slf4j
@Service
public class AliyunSmsService {

    @Value("${aliyun.sms.access-key-id}")
    private String accessKeyId;

    @Value("${aliyun.sms.access-key-secret}")
    private String accessKeySecret;

    @Value("${aliyun.sms.sign-name}")
    private String signName;

    @Value("${aliyun.sms.template-code}")
    private String templateCode;

    @Value("${aliyun.sms.region-id:cn-hangzhou}")
    private String regionId;

    private Client client;

    /**
     * 初始化客户端
     */
    private Client getClient() throws Exception {
        if (client == null) {
            Config config = new Config()
                    .setAccessKeyId(accessKeyId)
                    .setAccessKeySecret(accessKeySecret)
                    .setEndpoint("dysmsapi.aliyuncs.com");
            client = new Client(config);
        }
        return client;
    }

    /**
     * 发送短信验证码
     */
    public boolean sendSms(String phone, String code) {
        try {
            SendSmsRequest request = new SendSmsRequest()
                    .setPhoneNumbers(phone)
                    .setSignName(signName)
                    .setTemplateCode(templateCode)
                    .setTemplateParam("{\"code\":\"" + code + "\"}");

            SendSmsResponse response = getClient().sendSms(request);
            
            if ("OK".equals(response.getBody().getCode())) {
                log.info("短信发送成功: phone={}, code={}", phone, code);
                return true;
            } else {
                log.error("短信发送失败: phone={}, code={}, message={}", 
                        phone, response.getBody().getCode(), response.getBody().getMessage());
                return false;
            }
            
        } catch (Exception e) {
            log.error("短信发送异常: phone={}", phone, e);
            return false;
        }
    }
}
```

#### 4. 修改SmsController

修改 `SmsController.java` 中的 `sendRealSms` 方法：

```java
@Autowired
private AliyunSmsService aliyunSmsService;

private boolean sendRealSms(String phone, String code, String type) {
    return aliyunSmsService.sendSms(phone, code);
}
```

#### 5. 切换到生产模式

修改 `SmsController.java`：

```java
// 改为生产模式
private static final boolean DEV_MODE = false;
```

修改 `BankCardController.java`：

```java
// 改为生产模式
private static final boolean DEV_MODE = false;
```

### 第七步：测试

1. **重新编译部署**
   ```bash
   cd backend
   mvnw.cmd clean package -DskipTests
   # 部署到服务器
   ```

2. **测试发送短信**
   - 在小程序中点击"发送验证码"
   - 检查手机是否收到短信
   - 查看后端日志确认发送状态

3. **测试验证码验证**
   - 输入收到的验证码
   - 提交添加银行卡
   - 验证是否成功

## 腾讯云短信服务接入

### 开通步骤

1. **注册腾讯云账号**
   - 访问：https://cloud.tencent.com
   - 实名认证

2. **开通短信服务**
   - 访问：https://console.cloud.tencent.com/smsv2
   - 点击"开始使用"

3. **创建签名和模板**
   - 签名：易享生活
   - 模板：您的验证码为：{1}，该验证码5分钟内有效。

4. **获取密钥**
   - 访问：https://console.cloud.tencent.com/cam/capi
   - 创建密钥

### Maven依赖

```xml
<!-- 腾讯云短信SDK -->
<dependency>
    <groupId>com.tencentcloudapi</groupId>
    <artifactId>tencentcloud-sdk-java-sms</artifactId>
    <version>3.1.880</version>
</dependency>
```

### 配置

```yaml
tencent:
  sms:
    secret-id: YOUR_SECRET_ID
    secret-key: YOUR_SECRET_KEY
    sdk-app-id: YOUR_SDK_APP_ID
    sign-name: 易享生活
    template-id: YOUR_TEMPLATE_ID
```

## 成本估算

### 月度成本（按使用量）

| 场景 | 月发送量 | 单价 | 月成本 |
|------|---------|------|--------|
| 小规模 | 1000条 | 0.045元 | 45元 |
| 中规模 | 5000条 | 0.045元 | 225元 |
| 大规模 | 10000条 | 0.045元 | 450元 |

### 优化建议

1. **限制发送频率**
   - 同一手机号60秒内只能发送一次
   - 同一IP每天最多发送10次

2. **验证码有效期**
   - 设置5分钟有效期
   - 过期自动删除

3. **防刷机制**
   - 图形验证码
   - 设备指纹识别
   - IP黑名单

## 测试清单

### 开发模式测试
- [x] 发送验证码（返回固定验证码888888）
- [x] 验证码倒计时
- [x] 添加银行卡（跳过验证码验证）
- [x] 验证码过期（5分钟）
- [x] 发送频率限制（60秒）

### 生产模式测试
- [ ] 发送真实短信
- [ ] 收到短信验证码
- [ ] 验证码验证成功
- [ ] 验证码验证失败
- [ ] 验证码过期提示
- [ ] 发送频率限制

## 常见问题

### Q1: 短信发送失败
**原因：**
- AccessKey配置错误
- 签名或模板未审核通过
- 余额不足
- 手机号格式错误

**解决：**
- 检查配置
- 等待审核通过
- 充值
- 验证手机号格式

### Q2: 收不到短信
**原因：**
- 手机号错误
- 被运营商拦截
- 短信延迟

**解决：**
- 确认手机号正确
- 检查垃圾短信
- 等待1-2分钟

### Q3: 验证码验证失败
**原因：**
- 验证码过期
- 验证码输入错误
- Redis连接失败

**解决：**
- 重新发送验证码
- 仔细核对验证码
- 检查Redis服务

## 部署状态

### 当前部署
- ✅ 短信API已部署
- ✅ 开发模式已启用
- ✅ 固定验证码：888888
- ✅ 小程序已集成

### 生产部署前检查
- [ ] 开通短信服务
- [ ] 配置AccessKey
- [ ] 创建签名和模板
- [ ] 修改DEV_MODE为false
- [ ] 重新编译部署
- [ ] 测试真实短信发送

## 相关文件

### 后端
- `backend/src/main/java/com/easyjoylife/controller/SmsController.java`
- `backend/src/main/java/com/easyjoylife/controller/BankCardController.java`
- `backend/src/main/resources/application.yml`

### 前端
- `miniprogram/pages/add-bank-card/add-bank-card.js`
- `miniprogram/pages/add-bank-card/add-bank-card.wxml`

## 下一步

1. **立即可用（开发模式）**
   - 使用固定验证码888888测试功能
   - 验证整个流程是否正常

2. **准备上线（生产模式）**
   - 选择短信服务商（推荐阿里云）
   - 完成开通和配置
   - 切换到生产模式
   - 充分测试

3. **持续优化**
   - 监控短信发送成功率
   - 优化验证码模板
   - 添加防刷机制
   - 降低成本
