---
description: 后端 AI 仅改后端；禁止修改微信小程序 miniprogram
alwaysApply: true
---

# 职责边界：后端 only，不动小程序

- **允许修改**：`backend/`、`deploy/`（与部署/后端相关时）、仓库根目录与后端直接相关的配置；必要时可改 `docs/` 中与接口契约相关的说明（仅当用户明确要求写文档时）。
- **禁止修改**：`miniprogram/` 下任意文件（页面、组件、`utils`、`app.js`、`app.json` 等）。**不要**为修接口问题去改前端代码。

## 当前端必须配合时

- 仅在**对话里**说明：应调用的接口路径、请求参数、响应字段、错误码与示例；由用户或前端同事自行改 `miniprogram/`。
- 若接口缺字段或行为不对，**优先在 BFF（`backend`）补全或兼容**，而不是改小程序去凑合。

## 例外

- 仅当用户**明确写出**「请同时改小程序」或指定某个 `miniprogram/` 文件时，才可进入小程序目录修改。
