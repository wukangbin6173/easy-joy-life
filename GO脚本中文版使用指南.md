# GO 脚本使用指南

## 概述

本项目提供两套部署脚本：
- **go.bat** - Windows 原生批处理脚本（本地开发使用）
- **go.sh** - Linux Shell 脚本（服务器端使用）

## Windows 用户快速开始

### 基本用法
```cmd
go.bat          # 显示交互式菜单（直接回车选择选项1）
go.bat 1        # 快速部署
go.bat 2        # 仅构建后端
go.bat help     # 查看帮助
```

### 特点
✅ **纯 Windows 原生** - 不依赖 Git Bash
✅ **完全中文化** - 所有提示都是中文
✅ **智能默认** - 直接回车使用最常用的选项
✅ **自动检测** - 自动使用 Maven Wrapper

## 系统要求

### Windows 本地开发
- ✅ Windows 10/11
- ✅ Java 11+ (已安装)
- ✅ Git (已安装)
- ✅ Maven Wrapper (项目自带)
- ⚠️ 可选: jq (用于 AI 生成提交消息)

### Linux 服务器
- ✅ CentOS/Ubuntu
- ✅ Java 11+
- ✅ Maven
- ✅ MySQL
- ✅ Nginx

### 选项 0：启动本地开发环境
```cmd
go.bat 0
```
- 启动本地开发服务器
- 适合本地开发和测试

### 选项 1：Git 提交并部署到服务器（默认）
```cmd
go.bat 1
```

**完整流程：**
1. ✅ 检查 Git 状态
2. ✅ 本地构建后端
3. ✅ Git 提交（支持 AI 生成提交消息）
4. ✅ 推送到 GitHub
5. ✅ 部署到生产服务器
6. ✅ 验证部署

**提交消息选项：**
- **选项 1（推荐）**：使用 DeepSeek AI 自动生成
  - 自动分析代码变更
  - 生成符合规范的中文提交消息
  - 需要安装 `jq` 工具
  
- **选项 2**：手动输入提交消息
  - 自己编写提交消息
  
- **选项 3**：使用默认时间戳
  - 格式：`部署: 2026-02-07 14:30:00`

### 选项 2：仅构建后端
```cmd
go.bat 2
```
- 只构建 Java 后端项目
- 不提交代码，不部署

### 选项 3：清理缓存
```cmd
go.bat 3
```
- 清理构建缓存和临时文件

## 交互式菜单

直接运行 `go.bat` 会显示：

```
================================
    EasyJoyLife - 一键部署
================================

请选择操作:
0. 启动本地开发环境
1. Git 提交并部署到服务器 (默认)
2. 仅构建后端
3. 清理缓存
4. 退出

请输入选择 (10秒后自动选择 1):
```

- 直接回车 → 自动选择选项 1
- 10 秒不输入 → 自动选择选项 1
- 输入 0-4 → 执行对应操作

## 部署流程详解

### 1. 检查 Git 状态
```
🔹 检查 Git 状态...
```
- 如果有未提交的更改，会询问是否继续
- 提示：`仍然继续? (y/n, 默认 y):`
- 直接回车 → 自动选择 `y`（继续）
- 输入 `n` → 取消操作

### 2. 本地构建后端
```
🔹 本地构建后端...
🔹 构建后端...
```
- 使用 Maven 构建 Java 项目
- 生成 JAR 文件

### 3. Git 提交
```
🔹 提交到 Git...

提交消息选项:
  1. 使用 DeepSeek AI 自动生成 (推荐)
  2. 手动输入提交消息
  3. 使用默认时间戳

选择方式 (1-3, 默认: 1):
```

### 4. 部署到服务器
```
🔹 部署到服务器...
ℹ️  服务器: 121.43.96.127
ℹ️  路径: /opt/easy-joy-life

确认部署到生产服务器? (y/n, 默认 y):
```
- 直接回车 → 自动选择 `y`（确认部署）
- 输入 `n` → 取消部署

**服务器上执行的操作：**
1. 进入项目目录
2. 拉取最新代码
3. 停止现有服务
4. 重新构建项目
5. 检查数据库连接
6. 启动新服务
7. 重载 Nginx
8. 测试 API

### 5. 验证部署
```
🔹 验证部署...
✅ 部署验证成功 - API 正常响应
ℹ️  访问地址: https://xx.aieo.cn
```

### 6. 完成
```
✅ 部署完成!

部署摘要:
  提交: feat: 添加新功能
  服务器: 121.43.96.127
  网址: https://xx.aieo.cn

后续步骤:
  1. 测试应用: https://xx.aieo.cn
  2. 查看日志: ssh root@121.43.96.127 'tail -f /var/log/easyjoylife.log'
  3. 监控服务: ssh root@121.43.96.127 'ps aux | grep easy-joy-life'

⏱️  总耗时: 2m 35s
```

## 配置信息

### 服务器配置
- **主机**: 121.43.96.127
- **用户**: root
- **路径**: /opt/easy-joy-life
- **网址**: https://xx.aieo.cn
- **分支**: main

### DeepSeek AI 配置
- **API Key**: sk-a1374a0606a744c3888ee224b5b8252c
- **API URL**: https://api.deepseek.com/v1/chat/completions
- **需要工具**: jq（用于解析 JSON）

## 常见问题

### Q: 如何安装 jq？
**Windows (使用 Scoop):**
```powershell
scoop install jq
```

**或运行项目中的安装脚本:**
```powershell
.\install-jq.ps1
```

### Q: Maven 未找到怎么办？
项目自带 Maven Wrapper，会自动使用 `./mvnw`

### Q: 如何查看服务器日志？
```bash
ssh root@121.43.96.127 'tail -f /var/log/easyjoylife.log'
```

### Q: 如何检查服务状态？
```bash
ssh root@121.43.96.127 'ps aux | grep easy-joy-life'
```

### Q: 部署失败怎么办？
1. 查看错误信息
2. 检查服务器日志
3. 确认服务器连接正常
4. 确认数据库连接正常

## 提示符号说明

- 🔹 **步骤提示** - 当前执行的步骤
- ✅ **成功** - 操作成功完成
- ❌ **错误** - 操作失败
- ⚠️ **警告** - 需要注意的信息
- ℹ️ **信息** - 一般性信息
- ⏱️ **时间** - 耗时统计

## 最佳实践

1. **快速部署流程**
   - 运行 `go.bat`
   - 等待 10 秒或直接回车（自动选择选项 1）
   - 再次直接回车确认继续（如有未提交更改）
   - 选择提交消息方式（推荐选 1 使用 AI）
   - 直接回车确认部署
   - 全程只需按 3-4 次回车！

2. **使用 AI 生成提交消息**
   - 更规范、更清晰
   - 自动分析代码变更
   - 符合团队规范

3. **部署前先测试**
   - 确保本地构建成功
   - 检查代码变更

4. **定期查看日志**
   - 及时发现问题
   - 监控服务状态

5. **使用交互模式**
   - 更安全，有确认步骤
   - 适合重要部署

## 更新日志

### v2.0 - 2026-02-07
- ✅ 全面中文化
- ✅ 默认选项改为 1（部署）
- ✅ 优化交互体验
- ✅ 添加 Windows 批处理文件
- ✅ 完善帮助文档
